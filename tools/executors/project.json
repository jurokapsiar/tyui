{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tools-executors",
  "sourceRoot": "tools/executors/src",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests",
        "cwd": "tools/executors"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "tools/executors"
      }
    }
  },
  "tags": ["scope:tools"]
}
