{
  "name": "vanilla-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 0.0.0.0",
    "serve": "vite --host 0.0.0.0",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.app.json --noEmit"
  },
  "dependencies": {
    "@toyu-ui/define": "workspace:*",
    "@toyu-ui/elements": "workspace:*"
  },
  "devDependencies": {
    "@toyu-ui/tools-executors": "workspace:*"
  }
}
