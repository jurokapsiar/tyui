{
  "name": "vanilla-example",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/vanilla-example/src",
  "implicitDependencies": ["define", "elements", "core"],
  "targets": {
    "build": {
      "executor": "@toyu-ui/tools-executors:build"
    },
    "serve": {
      "executor": "@toyu-ui/tools-executors:serve",
      "options": {
        "port": 4210
      }
    },
    "test": {
      "executor": "@toyu-ui/tools-executors:test",
      "options": {
        "browser": true,
        "watch": false,
        "coverage": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/vanilla-example",
        "command": "tsc -p tsconfig.app.json --noEmit"
      }
    }
  },
  "tags": ["scope:apps", "framework:vanilla"]
}
