import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Message Bar',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Message Bar</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-message-bar</code> (+ optional <code>tyui-message-bar-group</code>)</li>\n<li>Define: <code>@tyui/define/message-bar</code></li>\n<li>Status: draft</li>\n<li>Native substrate: semantic <code>&lt;div&gt;</code> container with an ARIA live role; icon, body, and actions are slotted native elements (<code>tyui-button</code> for actions, <code>tyui-link</code> for inline links).</li>\n<li>Shadow DOM: minimal. Shadow root holds the layout grid (icon / body / actions) with <code>part</code>s and <code>&lt;slot&gt;</code>s. The live-region role sits on the host (or an inner wrapper) so announcements work.</li>\n<li>Category: status / notification</li>\n<li>Component family: status surface</li>\n<li>Pattern type: alert/status depending on intent</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Communicate persistent, surface-or-app-level status (info/success/warning/error) that the user should notice but not necessarily act on immediately, and that persists until resolved/dismissed.</p>\n<p>Do <strong>not</strong> use for transient toasts (→ <code>tyui-toast</code>), for inline field validation (→ <code>tyui-field</code>), or for blocking decisions (→ <code>tyui-dialog</code>).</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: page/region-level status, warning, error, or success message.</li>\n<li>Do not use when: inline validation for one control, badge counts, or modal confirmations.</li>\n<li>Prefer instead: Field validation for form errors, Dialog for blocking decisions, Badge for decorative counts.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Dismiss and actions are separate targets; clicking message body does nothing.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>intent</code></td><td><code>info | success | warning | error</code></td><td>yes</td><td><code>info</code></td><td>preset icon + color + default politeness</td></tr><tr><td><code>politeness</code></td><td><code>polite | assertive</code></td><td>yes</td><td>derived from intent</td><td>overrides live setting</td></tr><tr><td><code>layout</code></td><td><code>auto | singleline | multiline</code></td><td>yes</td><td><code>auto</code></td><td>CSS/container-query driven</td></tr><tr><td><code>shape</code></td><td><code>square | rounded</code></td><td>yes</td><td><code>rounded</code></td><td>square = page-level, rounded = component-level</td></tr></tbody></table>\n<h3>Properties</h3>\n<p>Mirror attributes. No imperative API on the bar; the group may expose <code>dismiss(id)</code> helpers in the consumer layer.</p>\n<h3>Events</h3>\n<p><code>dismiss</code> — composed, emitted by the bar <strong>only if</strong> the component provides a built-in dismiss affordance; otherwise the consumer's slotted dismiss <code>tyui-button</code> emits its own <code>activate</code> and the app removes the bar. Prefer the latter (consumer-owned) to keep the bar logic-free. Document whichever is chosen.</p>\n<h3>Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>icon</code></td><td>intent icon (default provided)</td></tr><tr><td>default / <code>body</code></td><td>message text</td></tr><tr><td><code>title</code></td><td>bold lead-in</td></tr><tr><td><code>actions</code></td><td>action buttons + dismiss (the <code>containerAction</code>/dismiss is the last button)</td></tr></tbody></table>\n<h3>CSS parts</h3>\n<p><code>icon</code>, <code>body</code>, <code>title</code>, <code>actions</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-message-bar-background</code>, <code>--ty-message-bar-foreground</code>, <code>--ty-message-bar-icon-color</code>, <code>--ty-message-bar-border-color</code>, <code>--ty-message-bar-radius</code>. Each intent remaps these.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: <code>dismiss</code> — composed, emitted by the bar <strong>only if</strong> the component provides a built-in dismiss affordance; otherwise the consumer's slotted dismiss <code>tyui-button</code> emits its own <code>activate</code> and the app removes the bar. Prefer the latter (consumer-owned) to keep the bar logic-free. Document whichever is chosen.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>icon</code></td><td>intent icon (default provided)</td></tr><tr><td>default / <code>body</code></td><td>message text</td></tr><tr><td><code>title</code></td><td>bold lead-in</td></tr><tr><td><code>actions</code></td><td>action buttons + dismiss (the <code>containerAction</code>/dismiss is the last button)</td></tr></tbody></table>\n<h2>CSS Parts</h2>\n<p><code>icon</code>, <code>body</code>, <code>title</code>, <code>actions</code>.</p>\n<h2>Events</h2>\n<p><code>dismiss</code> — composed, emitted by the bar <strong>only if</strong> the component provides a built-in dismiss affordance; otherwise the consumer's slotted dismiss <code>tyui-button</code> emits its own <code>activate</code> and the app removes the bar. Prefer the latter (consumer-owned) to keep the bar logic-free. Document whichever is chosen.</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: <code>dismiss</code> — composed, emitted by the bar <strong>only if</strong> the component provides a built-in dismiss affordance; otherwise the consumer's slotted dismiss <code>tyui-button</code> emits its own <code>activate</code> and the app removes the bar. Prefer the latter (consumer-owned) to keep the bar logic-free. Document whichever is chosen.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: icon, title/body text, optional actions, optional dismiss target.</li>\n<li>Required parent: page/section/container announcing status.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n</ul>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>icon</code></td><td>intent icon (default provided)</td></tr><tr><td>default / <code>body</code></td><td>message text</td></tr><tr><td><code>title</code></td><td>bold lead-in</td></tr><tr><td><code>actions</code></td><td>action buttons + dismiss (the <code>containerAction</code>/dismiss is the last button)</td></tr></tbody></table>\n<ul>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-message-bar onclick=&quot;retry()&quot;&gt;Retry failed&lt;/tyui-message-bar&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: page/section/container announcing status.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Message itself is not focusable; links/actions/dismiss buttons follow native Tab order.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>It's a styled container + ARIA live region — almost no JS. Layout (<code>singleline</code>/<code>multiline</code>/<code>auto</code>) is CSS grid + container query (layout.md), not measurement JS.</li>\n<li>Intent → preset icon + token color, set via attribute, in CSS.</li>\n<li>Dismiss and actions are <strong>separate native buttons</strong> the consumer slots in — the bar itself owns no click behavior.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Message itself is not focusable; links/actions/dismiss buttons follow native Tab order.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: page/section/container announcing status.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Message itself is not focusable; links/actions/dismiss buttons follow native Tab order.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Message itself is not focusable; links/actions/dismiss buttons follow native Tab order.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Dismiss and actions are separate targets; clicking message body does nothing.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — MessageBar is rendered status, not a popup.</li>\n<li>Closes on: optional dismiss button or auto-dismiss timer only when configured.</li>\n<li>DOM focus while open: message body is not focusable; actions/dismiss button are normal tab stops.</li>\n<li>Next Tab behavior: native order through links/actions/dismiss button.</li>\n<li>Arrow-key entry behavior: N/A.</li>\n<li>Outside click / pointerdown behavior: does not dismiss.</li>\n<li>Escape behavior: does not dismiss unless focus is on a control that documents Escape.</li>\n<li>Focus restoration on close: if dismissed from the dismiss button, move focus to the next logical region or invoking control when one exists.</li>\n<li>Behavior while enter / exit motion is running: live-region state follows logical visibility, not animation progress.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Dismiss/action listeners and optional auto-dismiss timers are cleared on dismiss/disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: the bar does not steal focus on appearance (behavior.md: focus moves only on explicit user action). Actions/dismiss are normal tab stops in DOM order. Removing a bar should move focus sensibly (e.g. to the next bar or a documented anchor) — define this.</li>\n<li><strong>Hit targets</strong>: the bar surface itself is <strong>not</strong> clickable. Each action and the dismiss button is its own focusable target (behavior.md: secondary actions need separate focusable targets). Never make the whole bar a button.</li>\n<li><strong>Native behavior</strong>: actions are native buttons/links; no custom key handling on the container.</li>\n<li><strong>State &amp; events</strong>: appearance/intent reflected via attributes; dismissal is user-initiated → emits a public event (from the dismiss button or the bar).</li>\n<li><strong>Disabled/readonly/loading</strong>: N/A for the bar. It is itself a status surface; use <code>role=&quot;status&quot;</code>/<code>alert</code> per politeness (behavior.md loading/status rule).</li>\n<li><strong>Motion</strong>: enter/exit handled by <code>tyui-message-bar-group</code>, tokenized, <code>prefers-reduced-motion</code>-safe, and <strong>no enter animation on page load</strong> (Fluent rule). During exit motion the bar is logically removed — focus/AT must already treat it as gone (behavior.md delayed-unmount rule).</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: block/flex status bar with icon/content/actions/dismiss regions.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>icon</code></td><td>intent icon (default provided)</td></tr><tr><td>default / <code>body</code></td><td>message text</td></tr><tr><td><code>title</code></td><td>bold lead-in</td></tr><tr><td><code>actions</code></td><td>action buttons + dismiss (the <code>containerAction</code>/dismiss is the last button)</td></tr></tbody></table>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-message-bar-background</code>, <code>--ty-message-bar-foreground</code>, <code>--ty-message-bar-icon-color</code>, <code>--ty-message-bar-border-color</code>, <code>--ty-message-bar-radius</code>. Each intent remaps these.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>icon</code>, <code>body</code>, <code>title</code>, <code>actions</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li><code>intent=&quot;error&quot;</code>/<code>&quot;warning&quot;</code> → assertive-leaning; <code>intent=&quot;info&quot;</code>/<code>&quot;success&quot;</code> → polite. Mapped to <code>politeness</code> → <code>aria-live</code> (<code>polite</code>/<code>assertive</code>) + <code>role=&quot;status&quot;</code> (polite) or <code>role=&quot;alert&quot;</code> (assertive). behavior.md: <code>role=&quot;status&quot;</code> polite, <code>role=&quot;alert&quot;</code> assertive.</li>\n<li>A shared <strong>AriaLive announcer</strong> near the app root is recommended (Fluent) for reliable announcements when bars mount dynamically.</li>\n<li>Dismiss button needs <code>aria-label=&quot;Dismiss&quot;</code> (icon-only).</li>\n<li>Icon is <code>aria-hidden</code> (decorative; meaning is in the text).</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: [APG Alert](https://www.w3.org/WAI/ARIA/apg/patterns/alert/) for assertive/important message-bar updates. Polite informational updates use status semantics from ARIA even though APG has no separate Status pattern.</li>\n<p>  - Direct requirements:</p>\n<p>    - Alert displays a brief important message without interrupting the user's task.</p>\n<p>    - Alert appearance must not move keyboard focus.</p>\n<p>    - Dynamically rendered alerts are announced by most screen readers, but alerts present before page load may not be announced.</p>\n<p>    - Alerts should not disappear automatically too quickly and should not interrupt frequently.</p>\n<p>    - If workflow interruption is required, use Alert Dialog, not MessageBar.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>MessageBar</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Configure an <code>AriaLiveAnnouncer</code> near the top of the app for reliable <code>aria-live</code> announcements.</p>\n<p>    - Use MessageBar inside MessageBarGroup when possible.</p>\n<p>    - Include a dismiss button as the container action.</p>\n<p>    - Reduce number of actions and keep content under about 100 characters.</p>\n<p>    - Use preset intents and do not customize politeness without accessibility review.</p>\n<p>    - Do not use enter animations on page load; group animation works only when direct children are MessageBars.</p>\n<p>    - <code>bottomReflowSpacer</code> is discouraged because a MessageBar without actions is not recommended.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li>Reliable SR announcement needs the live region present <strong>before</strong> the text changes; dynamically mounting a fresh <code>role=alert</code> can be missed — hence the recommended app-level AriaLive announcer.</li>\n<li><code>multiline</code> layout reserves an actions grid area even with no actions (Fluent <code>bottomReflowSpacer</code>) — but a bar with no actions is discouraged accessibility-wise; warn.</li>\n<li>Keep content &lt; ~100 chars (Fluent) — long messages belong elsewhere.</li>\n<li>Group animation only works if <code>tyui-message-bar-group</code>'s direct children are bars (no wrappers) — document the limitation.</li>\n<li>Don't override politeness casually — assertive interrupts; reserve for errors.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: block/flex status bar with icon/content/actions/dismiss regions remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: severity icon is decorative when text names severity; otherwise provide label.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: severity icon is decorative when text names severity; otherwise provide label.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/message-bar.md</code>): &quot;Use for persistent surface/app status. Square shape for page-level, rounded for component-level. Always include a dismiss action; keep messages short.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>transient → tyui-toast</code>, <code>field error → tyui-field</code>, <code>blocking choice → tyui-dialog</code>, <code>inline decoration → tyui-badge</code>.</li>\n<li><strong>Layout ownership</strong>: bar owns the icon/body/actions grid + responsive single/multi-line switch (container query); <strong>parent owns width and vertical stacking</strong> (via the group). layout.md: container-query, not viewport media query.</li>\n<li><strong>Token usage</strong>: intent colors via <code>--ty-message-bar-*</code>; intent must be conveyed by icon + text, never color alone.</li>\n<li><strong>Anti-patterns to reject</strong>: clickable bar surface; &gt;3 actions; enter animation on initial page load; color-only intent; long body text; missing dismiss <code>aria-label</code>.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;status-surface&quot;,</p>\n<p>    &quot;focusable&quot;: false,</p>\n<p>    &quot;roles&quot;: { &quot;polite&quot;: &quot;status&quot;, &quot;assertive&quot;: &quot;alert&quot; },</p>\n<p>    &quot;accessibility&quot;: { &quot;needsAriaLiveAnnouncer&quot;: true, &quot;dismissNeedsLabel&quot;: true },</p>\n<p>    &quot;alternatives&quot;: { &quot;transient&quot;: &quot;tyui-toast&quot;, &quot;fieldError&quot;: &quot;tyui-field&quot; }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag clickable bar; flag color-only intent; flag missing dismiss label; flag enter animation flagged for first paint.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td><code>role=status</code> vs <code>role=alert</code> mapping</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Live text announced on dynamic insertion/update</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Dismiss button label and event</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Actions remain separate tab stops</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>No clickable host</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Color intent paired with icon/text</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>No first-paint enter animation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Reduced-motion and forced-colors coverage</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: No focus theft</td><td>Dynamically add an error MessageBar while focus is in an input.</td><td>Wait for announcement.</td><td>Focus remains in the input; message exposes alert/status semantics</td></tr><tr><td>APG: Live region reliability</td><td>Configure a shared announcer before adding bars.</td><td>Add and update message text.</td><td>Announcement occurs once and does not depend on a freshly mounted hidden node</td></tr><tr><td>APG: Persistent status</td><td>Render a warning MessageBar with auto-dismiss.</td><td>Advance fake timers.</td><td>Validation fails unless dismissal policy satisfies time-limit requirements and is user controllable</td></tr><tr><td>APG: Blocking alternative</td><td>Generate a destructive confirmation MessageBar.</td><td>Run component selection validation.</td><td>Generator selects <code>tyui-dialog modal-type=&quot;alert&quot;</code> instead of MessageBar</td></tr><tr><td>Fluent UI: Dismiss affordance</td><td>Render a MessageBar with icon-only dismiss.</td><td>Run accessibility validation.</td><td>Dismiss button has an accessible label and is the container action, not a host click</td></tr><tr><td>Fluent UI: Preset intent mapping</td><td>Render info, success, warning, and error intents.</td><td>Inspect roles/live settings and visuals.</td><td>Intent maps to documented tokens and live politeness without color-only meaning</td></tr><tr><td>Fluent UI: First-paint animation guard</td><td>Render MessageBarGroup on initial page load.</td><td>Inspect animation state.</td><td>Enter animation is disabled on initial paint; exit/lifecycle animation remains allowed</td></tr><tr><td>Fluent UI: Content/action budget</td><td>Render long body text and four actions.</td><td>Run design validation.</td><td>Validation flags long content and excessive actions with dialog/page-content alternatives</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>No focus theft on appearance.</td></tr><tr><td>behavior.md</td><td>Bar surface inert; each action/dismiss is a separate target.</td></tr><tr><td>behavior.md</td><td>Status semantics via <code>role=status</code>/<code>alert</code> + <code>aria-live</code>.</td></tr><tr><td>behavior.md</td><td>Native buttons/links for actions.</td></tr><tr><td>behavior.md</td><td>Dismissal user-initiated → public event.</td></tr><tr><td>behavior.md</td><td>Motion tokenized, no load-time enter, reduced-motion safe, logical-closed during exit.</td></tr></tbody></table>",
    )}
  </article>`,
};
