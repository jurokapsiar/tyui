import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Link',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Link</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-link</code></li>\n<li>Define: <code>@toyu-ui/define/link</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>native <code>&lt;a href&gt;</code></strong> when navigating; <strong>native <code>&lt;button&gt;</code></strong> when there is no <code>href</code> (action styled as a link). <code>&lt;span role=button&gt;</code> only for the inline-wrapping edge case.</li>\n<li>Shadow DOM: optional/minimal. Prefer light DOM or a shadow root that renders the chosen native element with <code>part=&quot;control&quot;</code>; native <code>href</code>, focus, context menu, middle-click, and &quot;open in new tab&quot; must be preserved on a real <code>&lt;a&gt;</code>.</li>\n<li>Category: navigation / action</li>\n<li>Component family: navigation/action text</li>\n<li>Pattern type: native link; button only for no-href action form</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Navigate to another location (primary use → real <code>&lt;a href&gt;</code>).</p>\n<p>A link styled as text may also trigger an in-page action when there is no destination (→ <code>&lt;button&gt;</code>), but <strong>prefer <code>tyui-button</code> for actions</strong> and reserve Link for navigation or inline-text affordances.</p>\n<p>Alternatives: <code>tyui-button</code> (actions), <code>tyui-menu</code> item link (<code>MenuItemLink</code>).</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: navigate to a URL with native anchor behavior.</li>\n<li>Do not use when: commands without href, disabled links that still navigate, or nested interactive content.</li>\n<li>Prefer instead: Button for commands, MenuItemLink inside menus.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: One link target; hover may style but not navigate until activation.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>href</code></td><td><code>string</code></td><td>→a</td><td>—</td><td>presence selects <code>&lt;a&gt;</code> vs <code>&lt;button&gt;</code></td></tr><tr><td><code>target</code> / <code>rel</code> / <code>download</code></td><td><code>string</code></td><td>→a</td><td>—</td><td>native; auto-add <code>rel=&quot;noopener&quot;</code> for <code>target=_blank</code></td></tr><tr><td><code>appearance</code></td><td><code>default | subtle</code></td><td>yes</td><td><code>default</code></td><td></td></tr><tr><td><code>inline</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>underline + inline-flow styling</td></tr><tr><td><code>disabled</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td></td></tr><tr><td><code>disabled-focusable</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>keeps focus, sets <code>aria-disabled</code></td></tr></tbody></table>\n<h3>Properties</h3>\n<p>Mirror attributes. No methods.</p>\n<h3>Events</h3>\n<p>Default: rely on native navigation/<code>click</code>. Expose a composed <code>activate</code> event <strong>only</strong> for the no-<code>href</code> action form, mirroring <code>tyui-button</code>, so app code can hear it across the boundary. Navigation links emit no custom event.</p>\n<h3>Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>link text (and optional inline icon)</td></tr></tbody></table>\n<h3>CSS parts</h3>\n<p><code>control</code> — the native <code>&lt;a&gt;</code>/<code>&lt;button&gt;</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-link-color</code>, <code>--ty-link-color-hover</code>, <code>--ty-link-color-pressed</code>, <code>--ty-link-underline</code> (<code>none</code>/<code>currentColor</code>), <code>--ty-link-focus-color</code>. Defaults from semantic tokens; <code>appearance=subtle</code> remaps color.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: Default: rely on native navigation/<code>click</code>. Expose a composed <code>activate</code> event <strong>only</strong> for the no-<code>href</code> action form, mirroring <code>tyui-button</code>, so app code can hear it across the boundary. Navigation links emit no custom event.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>link text (and optional inline icon)</td></tr></tbody></table>\n<h2>CSS Parts</h2>\n<p><code>control</code> — the native <code>&lt;a&gt;</code>/<code>&lt;button&gt;</code>.</p>\n<h2>Events</h2>\n<p>Default: rely on native navigation/<code>click</code>. Expose a composed <code>activate</code> event <strong>only</strong> for the no-<code>href</code> action form, mirroring <code>tyui-button</code>, so app code can hear it across the boundary. Navigation links emit no custom event.</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: Default: rely on native navigation/<code>click</code>. Expose a composed <code>activate</code> event <strong>only</strong> for the no-<code>href</code> action form, mirroring <code>tyui-button</code>, so app code can hear it across the boundary. Navigation links emit no custom event.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: phrasing content and optional icon; no interactive descendants.</li>\n<li>Required parent: none; navigation containers may group links.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n</ul>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>link text (and optional inline icon)</td></tr></tbody></table>\n<ul>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-link onclick=&quot;save()&quot;&gt;Save&lt;/tyui-link&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: none; navigation containers may group links.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>With href: native Tab/Enter link behavior. Without href/action form: native button Space/Enter behavior.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>With <code>href</code>: emit <code>&lt;a href&gt;</code> — navigation, <code>target</code>, <code>rel</code>, keyboard (Enter), context menu, status-bar URL, and modified-click are <strong>all native, zero JS</strong>.</li>\n<li>Without <code>href</code>: emit <code>&lt;button type=button&gt;</code> so Space/Enter activation and disabled semantics are native (Fluent does exactly this).</li>\n<li><code>inline</code>/<code>appearance</code>/underline policy = pure CSS. No scripted hover/focus state.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: With href: native Tab/Enter link behavior. Without href/action form: native button Space/Enter behavior.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: none; navigation containers may group links.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: With href: native Tab/Enter link behavior. Without href/action form: native button Space/Enter behavior.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>With href: native Tab/Enter link behavior. Without href/action form: native button Space/Enter behavior.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: One link target; hover may style but not navigate until activation.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A for component-owned overlays. Links navigate using native anchor behavior; overlay destinations are app-owned.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: native sequential focus.</li>\n<li>Arrow-key entry behavior: N/A.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: visual transitions never delay navigation activation semantics.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: No external listeners for normal navigation; optional action-mode listener cleans up on disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: native focusability of <code>&lt;a href&gt;</code>/<code>&lt;button&gt;</code>. Visible <code>:focus-visible</code> ring (document this in the contract). <code>disabled-focusable</code> keeps the element in tab order with <code>aria-disabled=&quot;true&quot;</code> (behavior.md disabled-focusable parity case).</li>\n<li><strong>Hit targets</strong>: exactly one action — navigate <strong>or</strong> act, never both. Do not attach an extra hover/secondary behavior.</li>\n<li><strong>Native behavior</strong>: Enter activates link; Space+Enter activate button; modified-click, context menu, middle-click preserved. No scripted key handling except the <code>&lt;span role=button&gt;</code> fallback.</li>\n<li><strong>State &amp; events</strong>: navigation needs no event. The action form emits <code>activate</code> (bubbles, composed). Programmatic focus changes emit nothing.</li>\n<li><strong>Disabled/readonly/loading</strong>: <code>disabled</code> removes from interaction/tab order (native on <code>&lt;button&gt;</code>; on <code>&lt;a&gt;</code> remove <code>href</code> + <code>aria-disabled</code>). <code>disabled-focusable</code> retains focus, suppresses activation, sets <code>aria-disabled</code>. No loading state.</li>\n<li><strong>Motion</strong>: none beyond a tokenized color/underline transition; reduced-motion safe.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: inline text by default; block/standalone variants preserve readable hit target.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>link text (and optional inline icon)</td></tr></tbody></table>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-link-color</code>, <code>--ty-link-color-hover</code>, <code>--ty-link-color-pressed</code>, <code>--ty-link-underline</code> (<code>none</code>/<code>currentColor</code>), <code>--ty-link-focus-color</code>. Defaults from semantic tokens; <code>appearance=subtle</code> remaps color.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>control</code> — the native <code>&lt;a&gt;</code>/<code>&lt;button&gt;</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<table><thead><tr><th>Render</th><th>Role</th><th>Notes</th></tr></thead><tbody><tr><td><code>&lt;a href&gt;</code></td><td><code>link</code> (native)</td><td>preferred</td></tr><tr><td><code>&lt;button&gt;</code> (no href)</td><td><code>button</code> (native)</td><td>action-as-link</td></tr><tr><td><code>&lt;span&gt;</code> (inline wrap)</td><td><code>role=&quot;button&quot;</code> + <code>tabindex=0</code> + key handling</td><td>only when inline text wrapping across lines is required</td></tr></tbody></table>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Patterns: [APG Link](https://www.w3.org/WAI/ARIA/apg/patterns/link/) for navigation and [APG Button](https://www.w3.org/WAI/ARIA/apg/patterns/button/) only for the no-<code>href</code> action form.</li>\n<p>  - Direct requirements:</p>\n<p>    - Navigation uses a native <code>&lt;a href&gt;</code> whenever possible.</p>\n<p>    - Applying <code>role=&quot;link&quot;</code> to a non-link does not add navigation, context menu, or browser link behavior; authors would own all missing behavior, so TYUI should avoid it.</p>\n<p>    - Link and button functions must not be confused: a link references a resource; a button triggers an action.</p>\n<p>    - Action-styled-as-link uses a native button and Enter/Space activation.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Link</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Underlines are required except in discrete link-only or interactive-only regions, icon-marked links, or clear surrounding context.</p>\n<p>    - Without <code>href</code>, Link renders as an HTML button.</p>\n<p>    - <code>as=&quot;span&quot;</code> may be used for inline wrapping and requires <code>role=&quot;button&quot;</code>.</p>\n<p>    - <code>disabled</code> and <code>disabledFocusable</code> are supported; disabled focusable remains in tab order but inert.</p>\n<p>    - <code>appearance</code> supports default and subtle; <code>inline</code> changes text-flow styling.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><strong>Underline accessibility</strong>: a non-underlined link is only allowed in the discrete contexts Fluent lists (nav region, all-interactive area, icon-marked, clear context). The spec must require <code>inline</code>/underline by default and treat no-underline as an explicit, justified choice.</li>\n<li>A disabled native <code>&lt;a&gt;</code> is not truly inert — must drop <code>href</code> and add <code>aria-disabled</code> (and prevent default) rather than rely on a non-existent <code>disabled</code> attribute.</li>\n<li><code>&lt;span role=button&gt;</code> wraps across lines (Fluent's <code>as=&quot;span&quot;</code>); it loses native activation, so it requires manual Space/Enter + <code>tabindex</code> — flag as the heaviest variant, use sparingly.</li>\n<li>Auto-apply <code>rel=&quot;noopener noreferrer&quot;</code> whenever <code>target=&quot;_blank&quot;</code>.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: inline text by default; block/standalone variants preserve readable hit target remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: icons are decorative unless link text is absent, then accessible name is required.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: icons are decorative unless link text is absent, then accessible name is required.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/link.md</code>): &quot;Use <code>tyui-link</code> for navigation. For in-page actions prefer <code>tyui-button</code>; only use the no-<code>href</code> Link for inline-text affordances. Links must be underlined unless in a discrete all-interactive region.&quot;</li>\n<li><strong>Alternatives map</strong> (<code>x-design-system.alternatives</code>): <code>action → tyui-button</code>, <code>menu navigation → tyui-menu-item[href]</code>, <code>toggle → tyui-toggle-button</code>.</li>\n<li><strong>Layout ownership</strong>: component owns intrinsic inline sizing + wrapping policy; parent owns placement. Inline links flow with text (<code>display:inline</code>), standalone links are <code>inline-block</code> — document the wrapping difference (Fluent <code>as=span</code> note).</li>\n<li><strong>Token usage</strong>: color/underline via <code>--ty-link-*</code>; reject literal colors.</li>\n<li><strong>Anti-patterns to reject</strong>: <code>tyui-link</code> with an <code>activate</code>/click handler used purely for navigation (use <code>href</code>); non-underlined inline link in body text; <code>target=_blank</code> without <code>rel</code>; overloading a link with a second action.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;navigation&quot;,</p>\n<p>    &quot;focusable&quot;: true,</p>\n<p>    &quot;alternatives&quot;: { &quot;action&quot;: &quot;tyui-button&quot; },</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;underlineRequiredOutsideDiscreteRegions&quot;: true,</p>\n<p>      &quot;requiresAccessibleName&quot;: true</p>\n<p>    }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag navigation links implemented as <code>&lt;button&gt;</code>+JS; flag missing underline on inline links; flag <code>_blank</code> without <code>rel</code>.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Native link navigation attributes</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Action mode emits composed <code>activate</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Programmatic attribute changes are silent</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled removes from Tab and suppresses click</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled-focusable keeps focus but suppresses activation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td><code>_blank</code> rel hardening</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Inline link underline validation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Focus-visible distinct from hover</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Native link behavior</td><td>Render <code>tyui-link href=&quot;/settings&quot; target=&quot;_blank&quot;</code>.</td><td>Middle-click, open context menu, press Enter.</td><td>Browser link behaviors work; host forwards <code>href</code>, <code>target</code>, and hardened <code>rel</code></td></tr><tr><td>APG: No button-based navigation</td><td>Render a no-<code>href</code> link with a navigation click handler.</td><td>Run design validation.</td><td>Validation rejects JS navigation and requires <code>href</code></td></tr><tr><td>APG: Action form is button</td><td>Render <code>tyui-link</code> with no <code>href</code>.</td><td>Press <code>Enter</code> and <code>Space</code>.</td><td>Internal native button activates for both keys and emits the documented action event</td></tr><tr><td>APG: One semantic target</td><td>Render a link with secondary hover action.</td><td>Run validation.</td><td>Secondary behavior is rejected; use a separate control</td></tr><tr><td>Fluent UI: Inline underline rule</td><td>Render inline body-text link with underline disabled.</td><td>Run accessibility/style validation.</td><td>Validation fails unless one of the documented discrete-context exceptions is present</td></tr><tr><td>Fluent UI: Span action fallback</td><td>Render <code>as=&quot;span&quot; inline</code> action link.</td><td>Press <code>Enter</code> and <code>Space</code>.</td><td>Role, tabindex, and key handling provide button-equivalent activation; generator warns it is the heaviest variant</td></tr><tr><td>Fluent UI: Disabled link states</td><td>Render disabled and disabled-focusable links.</td><td>Press Tab and activate by keyboard/mouse.</td><td>Disabled link is skipped and inert; disabled-focusable is reachable, announces disabled, and suppresses activation</td></tr><tr><td>Fluent UI: Subtle appearance</td><td>Render default and subtle links.</td><td>Inspect styles.</td><td>Appearance remaps tokens only and preserves accessible name/role</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → <code>&lt;a&gt;</code> for nav, <code>&lt;button&gt;</code> for action.</td></tr><tr><td>behavior.md</td><td>One target = one action → navigate xor act.</td></tr><tr><td>behavior.md</td><td>Focus visible; disabled-focusable handled with <code>aria-disabled</code>.</td></tr><tr><td>behavior.md</td><td>Events: navigation silent; action emits composed <code>activate</code>.</td></tr><tr><td>behavior.md</td><td>Disabled removes from tab order; readonly N/A.</td></tr><tr><td>behavior.md</td><td>Motion tokenized, reduced-motion safe.</td></tr></tbody></table>")}
  </article>`,
};
