import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Popover',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Popover</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tags: <code>tyui-popover</code> (+ <code>tyui-popover-trigger</code>, <code>tyui-popover-surface</code>)</li>\n<li>Define: <code>@tyui/define/popover</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>HTML Popover API</strong> (<code>popover</code> attribute + <code>popovertarget</code>) for the surface; <strong>CSS Anchor Positioning</strong> for placement; native <code>&lt;dialog&gt;</code>-style focus trap only when <code>trap-focus</code> is set.</li>\n<li>Shadow DOM: minimal. Host renders <code>&lt;div popover part=&quot;surface&quot;&gt;</code>; trigger projected via slot. Top-layer, light-dismiss, and Esc come from the platform.</li>\n<li>Category: overlay (non-modal contextual content)</li>\n<li>Component family: popup/overlay</li>\n<li>Pattern type: disclosure; dialog only when focus-trapped</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Show contextual, non-essential content on top of other content, anchored to a trigger.</p>\n<p>Do <strong>not</strong> use for required decisions (→ <code>tyui-dialog</code>), simple text hints (→ <code>tyui-tooltip</code>), or action lists (→ <code>tyui-menu</code>). Don't pour large/primary content into a popover.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: non-modal contextual content opened from an explicit trigger.</li>\n<li>Do not use when: menus, tooltips, blocking workflows, or passive-only hover content.</li>\n<li>Prefer instead: Menu for commands, Tooltip for descriptions, Dialog for modal tasks.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Outside click/light dismiss closes when configured; hover preview never steals focus.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-popover</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>open</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>controlled</td></tr><tr><td><code>default-open</code></td><td><code>boolean</code></td><td>no</td><td><code>false</code></td><td>uncontrolled</td></tr><tr><td><code>trap-focus</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>native inert trap for interactive content</td></tr><tr><td><code>open-on-hover</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>hover-open (must not steal focus)</td></tr><tr><td><code>open-on-context</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>context-menu open; disables other triggers</td></tr><tr><td><code>close-on-scroll</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td></td></tr><tr><td><code>with-arrow</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>anchor-positioned arrow</td></tr><tr><td><code>size</code></td><td><code>small | medium | large</code></td><td>yes</td><td><code>medium</code></td><td>padding + arrow</td></tr><tr><td><code>appearance</code></td><td><code>brand | inverted</code></td><td>yes</td><td>—</td><td>styling</td></tr><tr><td><code>positioning</code></td><td>property (<code>PositioningShorthand</code>)</td><td>—</td><td><code>above</code>/auto</td><td>→ CSS anchor <code>position-area</code></td></tr><tr><td><code>mouse-leave-delay</code></td><td>number</td><td>—</td><td>—</td><td>hover close delay</td></tr></tbody></table>\n<h3>Events</h3>\n<ul>\n<li><code>open-change</code> — composed; <code>{ open, reason }</code> (<code>triggerClick \\| escapeKeyDown \\| outsideClick \\| scroll \\| hover</code>). User-initiated.</li>\n</ul>\n<h3>Slots</h3>\n<p><code>trigger</code>, default (surface content).</p>\n<h3>CSS parts</h3>\n<p><code>surface</code>, <code>arrow</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-popover-background</code>, <code>--ty-popover-foreground</code>, <code>--ty-popover-radius</code>, <code>--ty-popover-padding</code>, <code>--ty-popover-shadow</code>, <code>--ty-popover-arrow-size</code>, <code>--ty-popover-max-inline-size</code>.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: see Events above, including <code>open-change</code> — composed; <code>{ open, reason }</code> (<code>triggerClick \\| escapeKeyDown \\| outsideClick \\| scroll \\| hover</code>). User-initiated.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p><code>trigger</code>, default (surface content).</p>\n<h2>CSS Parts</h2>\n<p><code>surface</code>, <code>arrow</code>.</p>\n<h2>Events</h2>\n<ul>\n<li><code>open-change</code> — composed; <code>{ open, reason }</code> (<code>triggerClick \\| escapeKeyDown \\| outsideClick \\| scroll \\| hover</code>). User-initiated.</li>\n</ul>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: see Events above, including <code>open-change</code> — composed; <code>{ open, reason }</code> (<code>triggerClick \\| escapeKeyDown \\| outsideClick \\| scroll \\| hover</code>). User-initiated.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: trigger plus popover surface content; actions inside surface must be separate controls. The trigger may be a native <code>&lt;button&gt;</code> or a <code>tyui-button</code> only when <code>tyui-button</code> exposes native-equivalent button semantics on the host (focusable, Enter/Space activation, disabled behavior, accessible name) and forwards/accepts the required trigger ARIA.</li>\n<li>Required parent: trigger/controller owns open state; popover owns surface lifecycle.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  <code>trigger</code>, default (surface content).</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-popover open-on-hover trap-focus&gt;Hover-only modal&lt;/tyui-popover&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: trigger/controller owns open state; popover owns surface lifecycle.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Trigger opens with Enter/Space/click; Escape closes; next Tab path into or past surface is explicit; trapped mode cycles focus.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li><strong>Popover API</strong> gives top-layer rendering, <strong>light-dismiss</strong> (click outside), <strong>Esc-to-close</strong>, and one-open-at-a-time <strong>for free</strong> — replacing a popover JS controller + portal + outside-click listener. Core Oat win.</li>\n<li><strong><code>popovertarget</code></strong> on the trigger button declaratively toggles the surface — <strong>zero JS</strong> for the basic open/close.</li>\n<li><strong>CSS Anchor Positioning</strong> (<code>anchor()</code>, <code>position-area</code>, <code>position-try</code>) replaces Floating UI for <code>positioning</code>/flip/arrow. Progressive enhancement: small JS fallback where anchor positioning is unsupported.</li>\n<li><code>trap-focus</code> (interactive content) uses the native dialog-style inert trap; default popover is non-trapping.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Trigger opens with Enter/Space/click; Escape closes; next Tab path into or past surface is explicit; trapped mode cycles focus.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: trigger/controller owns open state; popover owns surface lifecycle.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Trigger opens with Enter/Space/click; Escape closes; next Tab path into or past surface is explicit; trapped mode cycles focus.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Trigger opens with Enter/Space/click; Escape closes; next Tab path into or past surface is explicit; trapped mode cycles focus.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Outside click/light dismiss closes when configured; hover preview never steals focus.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: trigger click/Enter/Space, programmatic <code>open</code>, optional hover/focus preview when explicitly configured.</li>\n<li>Closes on: Escape, outside click/light-dismiss, trigger toggle, blur/hover delay when configured, or programmatic close.</li>\n<li>DOM focus while open: non-modal popover does not steal focus; trapped mode moves/cycles focus only after explicit activation.</li>\n<li>Next Tab behavior: default path is from trigger into the surface when interactive content exists, otherwise past it; trapped mode cycles within the surface.</li>\n<li>Arrow-key entry behavior: N/A unless slotted content owns a composite pattern.</li>\n<li>Outside click / pointerdown behavior: closes when light-dismiss is enabled without activating the surface.</li>\n<li>Escape behavior: closes and returns focus to trigger when focus was inside.</li>\n<li>Focus restoration on close: return to trigger when opened by keyboard or when focus is inside the surface.</li>\n<li>Behavior while enter / exit motion is running: logical closed state disables pointer/focus/accessibility before exit animation completes.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated; forms inside use native semantics.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated; forms inside use native semantics.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Anchor, toggle, outside-pointer, Escape, hover/focus delay, and positioning listeners clean up on close/disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: click-open → if <code>trap-focus</code>, move focus to first focusable; else leave focus on trigger and let the next Tab move into the surface (behavior.md: define next-keyboard-action; hover-open must NOT steal focus). On close, focus returns to the trigger. <code>unstable_disableAutoFocus</code> parity = a <code>no-autofocus</code> option.</li>\n<li><strong>Hit targets</strong>: trigger toggles; surface controls are their own targets. Hover-open is a preview affordance and must not select/dismiss/navigate by hover alone (behavior.md hover rule), except the explicit context-menu mode.</li>\n<li><strong>Native behavior</strong>: Popover API light-dismiss + Esc; native button activation. No hand-rolled outside-click.</li>\n<li><strong>Composite widgets</strong>: not a composite itself, but may contain one; it owns only open state. Nested popovers ≤2 levels (Fluent), each a separate component.</li>\n<li><strong>State &amp; events</strong>: <code>open</code> reflected + <code>aria-expanded</code> on trigger; user open/close emits <code>open-change</code>; programmatic <code>open</code> set is silent.</li>\n<li><strong>Disabled/readonly/loading</strong>: a disabled trigger doesn't open. Loading content inside uses <code>aria-busy</code>. No readonly.</li>\n<li><strong>Motion</strong>: surface fade/scale via tokens, reduced-motion safe. During exit (delayed unmount) the surface is logically closed — not focusable, <code>aria-hidden</code>, pointer-inert (behavior.md).</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: anchored top-layer or positioned surface; parent owns anchor and component owns surface sizing/collision.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p><code>trigger</code>, default (surface content).</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-popover-background</code>, <code>--ty-popover-foreground</code>, <code>--ty-popover-radius</code>, <code>--ty-popover-padding</code>, <code>--ty-popover-shadow</code>, <code>--ty-popover-arrow-size</code>, <code>--ty-popover-max-inline-size</code>.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>surface</code>, <code>arrow</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Surface: a generic container; if non-interactive, set <code>tabindex=&quot;-1&quot;</code> (Fluent). For interactive content with a trap, treat like a dialog (<code>role=&quot;dialog&quot;</code> + label).</li>\n<li>Trigger: native <code>&lt;button&gt;</code> or native-equivalent <code>tyui-button</code> with <code>aria-expanded</code> reflecting open state and <code>aria-controls</code>/surface relationship. Use native <code>popovertarget</code> only for a real button in the same tree; for <code>tyui-button</code>, the popover trigger wrapper must wire click/keydown/open state through JS or a documented trigger-control API.</li>\n<li><code>appearance=&quot;inverted&quot;/&quot;brand&quot;</code> is styling only.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: [APG Disclosure](https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/) for non-modal show/hide behavior. If <code>trap-focus=true</code> makes the surface dialog-like, also apply [APG Dialog (Modal)](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) focus containment requirements.</li>\n<p>  - Direct requirements:</p>\n<p>    - The trigger is a button that controls whether content is visible.</p>\n<p>    - Trigger activation toggles visibility and reflects <code>aria-expanded</code>; <code>aria-controls</code> links the trigger to the surface when IDs resolve.</p>\n<p>    - Non-modal disclosure content does not steal focus on hover.</p>\n<p>    - If the surface traps focus, focus moves inside on open, Tab/Shift+Tab are contained, Escape closes, and focus returns to the trigger.</p>\n<p>    - Popover does not claim Menu, Tooltip, or Dialog semantics unless its interaction model actually matches those patterns.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Popover</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Use <code>trapFocus</code> when focusable elements are in the Popover.</p>\n<p>    - Create nested Popovers as separate components; do not use more than two nested levels.</p>\n<p>    - If there are no interactive items, set <code>tabIndex={-1}</code> on the surface.</p>\n<p>    - Use Popover for non-essential information and avoid too much content.</p>\n<p>    - Controlled open state requires extra accessibility care.</p>\n<p>    - Trigger children must receive required interaction/accessibility props; custom triggers forward refs/activation and accept ARIA state. <code>tyui-button</code> is valid only if it satisfies that custom-trigger contract.</p>\n<p>    - <code>openOnContext</code> disables other trigger interactions; <code>openOnHover</code>, <code>closeOnScroll</code>, <code>mouseLeaveDelay</code>, <code>withArrow</code>, <code>size</code>, and <code>appearance</code> are explicit behavior/styling controls.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><strong>Hover-open + focus</strong>: the classic behavior.md tension. Hover opens visually but focus stays on trigger; the <strong>next Tab</strong> must have a documented path into the surface. Don't trap focus for hover popovers.</li>\n<li><strong><code>popovertarget</code> id resolution across shadow DOM</strong>: an invoker outside the surface's tree can't reference it. Either keep trigger + surface in the same root (component owns both) or fall back to the JS <code>showPopover()/hidePopover()</code> API. Document the convention (shared with Dialog/Tooltip/Menu).</li>\n<li><strong>Anchor positioning support</strong>: where unsupported, fall back to a minimal JS positioner; arrow likewise. Keep it progressive.</li>\n<li>Non-interactive surface should be <code>tabindex=&quot;-1&quot;</code> (Fluent) so it isn't an empty tab stop.</li>\n<li><code>close-on-scroll</code> and <code>mouse-leave-delay</code> are enhancements over the native baseline.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: anchored top-layer or positioned surface; parent owns anchor and component owns surface sizing/collision remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: no media policy except slotted-content responsibilities.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: no media policy except slotted-content responsibilities.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/popover.md</code>): &quot;Use for non-essential contextual content anchored to a trigger. Trap focus only when it contains focusable controls. Hover-open must not steal focus. ≤2 nesting levels.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>required decision → tyui-dialog</code>, <code>text hint → tyui-tooltip</code>, <code>actions → tyui-menu</code>, <code>inline detail → expand in page</code>.</li>\n<li><strong>Layout ownership</strong>: popover owns padding/arrow/max-inline-size; <strong>placement owned by anchor positioning</strong> relative to the trigger (layout.md: prefer CSS anchoring over JS measurement). Content scrolls inside when overflowing (<code>autoSize</code> parity).</li>\n<li><strong>Token usage</strong>: surface/arrow via <code>--ty-popover-*</code>.</li>\n<li><strong>Anti-patterns to reject</strong>: popover for primary/large content; hover-open with focus theft or hover-dismiss-only; &gt;2 nesting; interactive content without <code>trap-focus</code>; non-interactive surface left as a tab stop.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;contextual-overlay&quot;,</p>\n<p>    &quot;nativeApi&quot;: &quot;popover + anchor-positioning&quot;,</p>\n<p>    &quot;topLayer&quot;: true,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;triggerAriaExpanded&quot;: true,</p>\n<p>      &quot;trapFocusWhenInteractive&quot;: true,</p>\n<p>      &quot;hoverDoesNotStealFocus&quot;: true</p>\n<p>    },</p>\n<p>    &quot;alternatives&quot;: { &quot;modal&quot;: &quot;tyui-dialog&quot;, &quot;hint&quot;: &quot;tyui-tooltip&quot; }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag interactive popover without trap-focus; flag hover popover that traps/steals focus; flag missing <code>aria-expanded</code> on trigger; flag &gt;2 nesting.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Click/context/hover open reasons</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Hover never steals focus and next Tab path is deterministic</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Trap-focus path moves/restores focus</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Outside click/Esc/scroll close</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td><code>aria-expanded</code> and <code>open</code> sync</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Anchor-position fallback path</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Delayed close logically inert</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>&gt;2 nesting and interactive-without-trap validation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr><tr><td><code>tyui-button</code> trigger end-to-end</td><td>Render <code>&lt;tyui-popover&gt;</code> with <code>&lt;tyui-button slot=&quot;trigger&quot;&gt;Details&lt;/tyui-button&gt;</code> and passive plus interactive surface variants.</td><td>Tab to the button, press Enter, Space, click, then Escape/outside click.</td><td>Popover opens/closes, <code>aria-expanded</code>/surface relationship update on the accessible button, focus restoration targets <code>tyui-button</code>, and disabled/loading buttons do not open.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Disclosure trigger state</td><td>Render a non-modal popover with one trigger and passive content.</td><td>Click trigger, press <code>Enter</code>, press <code>Escape</code>.</td><td><code>aria-expanded</code> and <code>open</code> sync; Escape closes; focus returns to or remains on trigger according to mode</td></tr><tr><td>APG: Hover path</td><td>Render <code>open-on-hover</code>.</td><td>Hover trigger, then press <code>Tab</code>.</td><td>Hover opens visually without stealing focus; next Tab path is deterministic and documented</td></tr><tr><td>APG: Trap-focus dialog path</td><td>Render <code>trap-focus</code> with two controls.</td><td>Open, press <code>Tab</code>, <code>Shift+Tab</code>, and <code>Escape</code>.</td><td>Focus is contained while open, closes on Escape, and returns to trigger</td></tr><tr><td>APG: Pattern selection guard</td><td>Render action list or required decision inside Popover.</td><td>Run design validation.</td><td>Validation redirects action lists to Menu and required decisions to Dialog</td></tr><tr><td>Fluent UI: Interactive content trap</td><td>Render popover content with a link/button and no <code>trap-focus</code>.</td><td>Run validation.</td><td>Validation fails or requires documented non-trapping Tab path; recommended fix is <code>trap-focus</code></td></tr><tr><td>Fluent UI: Passive surface tab index</td><td>Render passive content only.</td><td>Press <code>Tab</code> after opening.</td><td>Surface is <code>tabindex=&quot;-1&quot;</code> and does not become an empty tab stop</td></tr><tr><td>Fluent UI: Nested limit</td><td>Render three nested popovers.</td><td>Run design validation.</td><td>Validation rejects depth greater than two</td></tr><tr><td>Fluent UI: Custom trigger ref</td><td>Render custom trigger component.</td><td>Open via keyboard and pointer.</td><td>Trigger receives required ARIA/open handlers and focus restoration works</td></tr><tr><td>Fluent UI: <code>tyui-button</code> trigger contract</td><td>Render <code>tyui-button</code> as the custom trigger.</td><td>Open and close with keyboard and pointer, then inspect accessibility tree.</td><td><code>tyui-button</code> exposes native-equivalent button role/name, trigger ARIA, event forwarding, and focus restoration without private shadow DOM access</td></tr><tr><td>Fluent UI: Controlled open accessibility</td><td>Control <code>open</code> externally.</td><td>Toggle via external state and keyboard.</td><td><code>aria-expanded</code>, focus policy, and close reasons remain consistent with uncontrolled mode</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → Popover API light-dismiss/Esc/top-layer; anchor positioning.</td></tr><tr><td>behavior.md</td><td>Hover-open does not steal focus; next-Tab path defined; click-open focuses only when trapping.</td></tr><tr><td>behavior.md</td><td>Focus returns to trigger on close.</td></tr><tr><td>behavior.md</td><td>Trigger toggle vs surface controls = separate targets; hover never dismisses/selects alone.</td></tr><tr><td>behavior.md</td><td>User open/close → composed <code>open-change</code>; programmatic silent; <code>aria-expanded</code> reflected.</td></tr><tr><td>behavior.md</td><td>Disabled trigger inert; loading via <code>aria-busy</code>.</td></tr><tr><td>behavior.md</td><td>Motion tokenized; logically closed during exit; reduced-motion safe.</td></tr></tbody></table>",
    )}
  </article>`,
};
