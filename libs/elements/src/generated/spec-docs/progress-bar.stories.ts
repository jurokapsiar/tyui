import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Progress Bar',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Progress Bar</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-progress-bar</code></li>\n<li>Define: <code>@tyui/define/progress-bar</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>native <code>&lt;progress&gt;</code></strong> (determinate). Indeterminate also uses <code>&lt;progress&gt;</code> with no <code>value</code>, restyled.</li>\n<li>Shadow DOM: minimal. Shadow root holds one <code>&lt;progress part=&quot;bar&quot;&gt;</code> (or a track <code>&lt;div part=&quot;track&quot;&gt;</code> + <code>&lt;div part=&quot;bar&quot;&gt;</code> when <code>&lt;progress&gt;</code> styling proves too limited — see quirks). Status semantics stay native.</li>\n<li>Category: status / loading</li>\n<li>Component family: status/loading</li>\n<li>Pattern type: native/ARIA progressbar</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Show determinate or indeterminate progress of a single operation.</p>\n<p>Do <strong>not</strong> use for an interactive value (→ <code>tyui-slider</code>) or for one-word status. Pair with <code>tyui-field</code> for a description/validation message (Fluent guidance).</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: task progress or indeterminate loading state.</li>\n<li>Do not use when: scalar measurements unrelated to progress, live alert messages, or decorative loading dots.</li>\n<li>Prefer instead: MessageBar for status text, Meter-like component for scalar measurements.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: No pointer action.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>value</code></td><td><code>number (0..max)</code></td><td>→progress</td><td>—</td><td>omit ⇒ indeterminate</td></tr><tr><td><code>max</code></td><td><code>number</code></td><td>→progress</td><td><code>1</code></td><td></td></tr><tr><td><code>thickness</code></td><td><code>medium | large</code></td><td>yes</td><td><code>medium</code></td><td>CSS height</td></tr><tr><td><code>shape</code></td><td><code>square | rounded</code></td><td>yes</td><td><code>rounded</code></td><td>CSS radius</td></tr><tr><td><code>color</code></td><td><code>brand | error | warning | success</code></td><td>yes</td><td><code>brand</code></td><td>token remap</td></tr></tbody></table>\n<h3>Properties</h3>\n<p><code>value</code>, <code>max</code> (numbers). No methods.</p>\n<h3>Events</h3>\n<p>None. Progress changes are programmatic; per behavior.md, programmatic state change emits no user-facing event.</p>\n<h3>Slots</h3>\n<p>None (label/description live in <code>tyui-field</code>).</p>\n<h3>CSS parts</h3>\n<p><code>bar</code> (filled portion / native progress value), <code>track</code> (background, if using the div fallback).</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-progress-track</code>, <code>--ty-progress-bar-color</code>, <code>--ty-progress-thickness</code>, <code>--ty-progress-radius</code>. <code>color</code> remaps <code>--ty-progress-bar-color</code>.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: None. Progress changes are programmatic; per behavior.md, programmatic state change emits no user-facing event.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p>None (label/description live in <code>tyui-field</code>).</p>\n<h2>CSS Parts</h2>\n<p><code>bar</code> (filled portion / native progress value), <code>track</code> (background, if using the div fallback).</p>\n<h2>Events</h2>\n<p>None. Progress changes are programmatic; per behavior.md, programmatic state change emits no user-facing event.</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: None. Progress changes are programmatic; per behavior.md, programmatic state change emits no user-facing event.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: optional label/value text outside or slotted; bar itself has no interactive children.</li>\n<li>Required parent: region/task that owns pending work.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  None (label/description live in <code>tyui-field</code>).</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-progress-bar onclick=&quot;cancel()&quot; value=&quot;40&quot;&gt;&lt;/tyui-progress-bar&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: region/task that owns pending work.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>No keyboard behavior; progress bar is not focusable.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Native <code>&lt;progress&gt;</code> gives <code>role=&quot;progressbar&quot;</code>, <code>aria-valuenow/min/max</code> mapping, and the indeterminate state <strong>for free</strong> — Oat's &quot;platform is the component.&quot;</li>\n<li>Determinate: set <code>value</code> + <code>max</code> on <code>&lt;progress&gt;</code>. Indeterminate: omit <code>value</code>; animation via CSS keyframes gated by <code>prefers-reduced-motion</code>.</li>\n<li>The only JS is reflecting <code>value</code>/<code>max</code> to the inner element. No measurement loop.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: No keyboard behavior; progress bar is not focusable.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: region/task that owns pending work.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: No keyboard behavior; progress bar is not focusable.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>No keyboard behavior; progress bar is not focusable.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: No pointer action.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — ProgressBar is inline status, not an overlay.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A; progress bar is not focusable.</li>\n<li>Next Tab behavior: skipped in sequential focus.</li>\n<li>Arrow-key entry behavior: N/A.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: indeterminate/value animation never changes progress semantics.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated; required/readonly disabled do not apply.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated; required/readonly disabled do not apply.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: No external listeners unless value polling/animation observers are implemented; those stop on disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: not focusable, not in tab order (it's status, not a control).</li>\n<li><strong>Hit targets</strong>: no action; never make a progress bar clickable.</li>\n<li><strong>Native behavior</strong>: native <code>&lt;progress&gt;</code> semantics preserved; no role override.</li>\n<li><strong>State &amp; events</strong>: <code>value</code>/<code>max</code> update silently; <code>aria-busy</code> on the surrounding region (owned by the consumer, not the bar) for pending content (behavior.md loading rule).</li>\n<li><strong>Disabled/readonly/loading</strong>: this <strong>is</strong> the loading indicator. behavior.md &quot;loading/status must expose meaningful status semantics&quot; → satisfied by native <code>role=&quot;progressbar&quot;</code>. No disabled state.</li>\n<li><strong>Motion</strong>: indeterminate animation is decorative and must have a <code>prefers-reduced-motion</code> fallback (Fluent ships a reduced-motion gradient + recommends a text status). Determinate transitions on <code>value</code> are tokenized.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: block/inline bar with track and indicator; parent owns label placement.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p>None (label/description live in <code>tyui-field</code>).</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-progress-track</code>, <code>--ty-progress-bar-color</code>, <code>--ty-progress-thickness</code>, <code>--ty-progress-radius</code>. <code>color</code> remaps <code>--ty-progress-bar-color</code>.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>bar</code> (filled portion / native progress value), <code>track</code> (background, if using the div fallback).</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Native <code>role=&quot;progressbar&quot;</code> from <code>&lt;progress&gt;</code>; <code>aria-valuenow/min/max</code> derived from <code>value</code>/<code>max</code> (determinate). Indeterminate <code>&lt;progress&gt;</code> correctly omits <code>aria-valuenow</code>.</li>\n<li>Accessible name comes from the associated <code>tyui-field</code>/<code>aria-label</code>/<code>aria-labelledby</code> (a progressbar has no intrinsic label).</li>\n<li>Reduced-motion indeterminate: Fluent recommends a <code>Field</code> validation/hint message so non-animated users still get status — surface this as a contract note.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: N/A. APG has no ProgressBar pattern in the referenced pattern index; do not substitute Meter because Meter represents a scalar measurement, not task progress.</li>\n<p>  - Direct requirements:</p>\n<p>    - Use native <code>&lt;progress&gt;</code> or an equivalent <code>role=&quot;progressbar&quot;</code> only if native styling is insufficient.</p>\n<p>    - Determinate progress exposes current value, min, and max; indeterminate progress omits current value.</p>\n<p>    - ProgressBar is status, not an interactive control, so it is not focusable and has no keyboard behavior.</p>\n<p>    - The operation must have an accessible name/description outside the bar.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>ProgressBar</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Use indeterminate progress when total units are unknown.</p>\n<p>    - Provide a clear description of the progress operation; do not use a single-word description.</p>\n<p>    - Show text above and/or below the bar, not to the left or right.</p>\n<p>    - Combine steps of one operation into one bar and do not rewind progress to show new steps.</p>\n<p>    - Use Field validation/hint text for indeterminate ProgressBar when reduced motion is active.</p>\n<p>    - <code>value</code> is between <code>0</code> and <code>max</code> with default <code>max=1</code>; undefined value means indeterminate.</p>\n<p>    - <code>indeterminateMotion</code> can be customized or disabled.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><code>&lt;progress&gt;</code> cross-browser styling needs vendor pseudo-elements (<code>::-webkit-progress-value</code>, <code>::-moz-progress-bar</code>). If the design needs effects those can't express (e.g. custom indeterminate sweep with arrow), fall back to <code>&lt;div role=&quot;progressbar&quot;&gt;</code> track+bar — <strong>but</strong> then you must set <code>aria-valuenow/min/max</code> and <code>aria-busy</code> manually. Document which path the component takes; prefer native <code>&lt;progress&gt;</code>.</li>\n<li>Fluent's <code>value</code> is a <strong>fraction 0–1</strong> by default (max=1), unlike HTML <code>&lt;progress&gt;</code> defaulting max=1 too — align the scales; document that <code>value</code> is <code>0..max</code>.</li>\n<li>Reduced-motion users lose the indeterminate animation → require an accompanying text status (via <code>tyui-field</code>).</li>\n<li>Do not &quot;rewind&quot; progress (Fluent anti-pattern).</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: block/inline bar with track and indicator; parent owns label placement remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: visible percentage text should match aria-valuenow.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: visible percentage text should match aria-valuenow.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/progress-bar.md</code>): &quot;Use for progress of one operation; always give it an accessible name via <code>tyui-field</code>. Indeterminate when total is unknown.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>interactive value → tyui-slider</code>, <code>spinner/short wait → tyui-spinner</code>, <code>multi-step → single bar, not rewound</code>.</li>\n<li><strong>Layout ownership</strong>: component owns thickness/track; <strong>parent owns width</strong> (typically full-width). layout.md intrinsic rule: width from container, height from <code>thickness</code> token.</li>\n<li><strong>Token usage</strong>: color/thickness via <code>--ty-progress-*</code>; reject literal colors; color must not be the only status signal (pair with text).</li>\n<li><strong>Anti-patterns to reject</strong>: focusable/clickable progress; role override; indeterminate bar with no text fallback under reduced motion; single-word description; rewinding progress.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;status&quot;,</p>\n<p>    &quot;focusable&quot;: false,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;nativeRole&quot;: &quot;progressbar&quot;,</p>\n<p>      &quot;needsAccessibleName&quot;: true,</p>\n<p>      &quot;reducedMotionRequiresTextStatus&quot;: true</p>\n<p>    }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag <code>tyui-progress-bar</code> without an associated label/field; flag color-only status; flag <code>tabindex</code>.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Determinate value/max reflection and native <code>position</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Indeterminate has no <code>value</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Accessible name required</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Not tabbable</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Programmatic updates silent</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Reduced-motion fallback includes text/status guidance</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Forced-colors visible track/value</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Design validation rejects color-only/focusable/clickable progress</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Determinate semantics</td><td>Render <code>value=&quot;0.5&quot; max=&quot;1&quot;</code> with a Field label.</td><td>Inspect accessibility tree.</td><td>Progressbar exposes name and correct value range; no extra focus stop exists</td></tr><tr><td>APG: Indeterminate semantics</td><td>Render with no <code>value</code>.</td><td>Inspect attributes and accessibility tree.</td><td>Internal <code>&lt;progress&gt;</code> has no <code>value</code>; no <code>aria-valuenow</code> is emitted</td></tr><tr><td>APG: Meter substitution guard</td><td>Generate a task-upload progress indicator.</td><td>Run design validation.</td><td>Generator uses ProgressBar, not Meter, for task completion</td></tr><tr><td>APG: No interactivity</td><td>Render <code>tabindex</code> or click handler on ProgressBar.</td><td>Run validation and press Tab.</td><td>Validation rejects focus/click behavior; cancellation must be a separate button</td></tr><tr><td>Fluent UI: Description quality</td><td>Render a progress bar labelled only &quot;Loading&quot;.</td><td>Run design validation.</td><td>Validation rejects single-word description and asks for operation-specific text</td></tr><tr><td>Fluent UI: Reduced-motion fallback</td><td>Enable reduced motion and render indeterminate progress.</td><td>Inspect visible status text and animation.</td><td>Animation is removed or reduced; Field hint/validation text still communicates status</td></tr><tr><td>Fluent UI: No rewind</td><td>Simulate values 0.8, 0.2 for one operation.</td><td>Observe state updates.</td><td>Validation flags rewinding unless a new operation identity is documented</td></tr><tr><td>Fluent UI: Text placement</td><td>Render description beside the bar.</td><td>Run layout validation.</td><td>Validation suggests text above/below according to Fluent guidance</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → <code>&lt;progress&gt;</code>, <code>role=progressbar</code> free.</td></tr><tr><td>behavior.md</td><td>Not focusable; no action.</td></tr><tr><td>behavior.md</td><td>Loading/status semantics exposed (native).</td></tr><tr><td>behavior.md</td><td>Programmatic updates emit no events.</td></tr><tr><td>behavior.md</td><td>Motion decorative + reduced-motion fallback (text status).</td></tr><tr><td>behavior.md</td><td>Disabled/readonly N/A, documented.</td></tr></tbody></table>",
    )}
  </article>`,
};
