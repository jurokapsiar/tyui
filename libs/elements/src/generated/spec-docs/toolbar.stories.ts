import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Toolbar',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Toolbar</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tags: <code>tyui-toolbar</code> (+ <code>tyui-toolbar-group</code>, <code>tyui-toolbar-divider</code>; buttons are <code>tyui-button</code>/<code>tyui-toggle-button</code> with a toolbar context)</li>\n<li>Define: <code>@tyui/define/toolbar</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <code>&lt;div role=&quot;toolbar&quot;&gt;</code> containing native buttons/links; grouping via <code>&lt;div role=&quot;group&quot;&gt;</code>; dividers decorative.</li>\n<li>Shadow DOM: minimal (root renders <code>role=&quot;toolbar&quot;</code> wrapper + <code>&lt;slot&gt;</code>). Controls are light-DOM native buttons.</li>\n<li>Category: composite command container (roving tabindex via <strong>focusgroup polyfill</strong>)</li>\n<li>Component family: composite command group</li>\n<li>Pattern type: toolbar</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Group <strong>3+</strong> related controls (buttons, toggle/menu buttons, checkboxes) into a single keyboard-efficient command surface.</p>\n<p>Do <strong>not</strong> use for 1–2 controls (just place them) or for primary page navigation (→ <code>tyui-nav</code>).</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: group related always-visible commands.</li>\n<li>Do not use when: menus, tabs, forms, or arbitrary layout rows.</li>\n<li>Prefer instead: Menu for overflow/transient commands, Tabs for views, Fieldset for form groups.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Each command is a separate target; separators are inert.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-toolbar</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>size</code></td><td><code>small | medium | large</code></td><td>yes</td><td><code>medium</code></td><td>propagates to children</td></tr><tr><td><code>vertical</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>layout + <code>aria-orientation</code></td></tr><tr><td><code>checked-values</code></td><td>property <code>Record&lt;string,string[]&gt;</code></td><td>—</td><td>—</td><td>for toggle/radio groups</td></tr></tbody></table>\n<h3>Events</h3>\n<ul>\n<li><code>checked-value-change</code> — composed; emitted when a toggle/radio child changes, payload <code>{ name, checkedItems }</code>. This is the one coordinated public event (behavior.md: parent owns shared composite state).</li>\n</ul>\n<h3>Slots</h3>\n<p><code>default</code> (controls/groups/dividers).</p>\n<h3>CSS parts</h3>\n<p><code>root</code>, <code>divider</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-toolbar-gap</code>, <code>--ty-toolbar-padding</code>, <code>--ty-toolbar-background</code>, <code>--ty-toolbar-divider-color</code>, <code>--ty-toolbar-min-block-size</code>.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: - <code>checked-value-change</code> — composed; emitted when a toggle/radio child changes, payload <code>{ name, checkedItems }</code>. This is the one coordinated public event (behavior.md: parent owns shared composite state).</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p><code>default</code> (controls/groups/dividers).</p>\n<h2>CSS Parts</h2>\n<p><code>root</code>, <code>divider</code>.</p>\n<h2>Events</h2>\n<ul>\n<li><code>checked-value-change</code> — composed; emitted when a toggle/radio child changes, payload <code>{ name, checkedItems }</code>. This is the one coordinated public event (behavior.md: parent owns shared composite state).</li>\n</ul>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: - <code>checked-value-change</code> — composed; emitted when a toggle/radio child changes, payload <code>{ name, checkedItems }</code>. This is the one coordinated public event (behavior.md: parent owns shared composite state).</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: peer buttons/links/toggles and optional separators/groups; no nested composites unless documented.</li>\n<li>Required parent: page/editor/region that owns command effects.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  <code>default</code> (controls/groups/dividers).</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-toolbar&gt;&lt;tyui-menu&gt;&lt;tyui-menu-item&gt;Nested&lt;/tyui-menu-item&gt;&lt;/tyui-menu&gt;&lt;/tyui-toolbar&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: page/editor/region that owns command effects.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Toolbar is one Tab stop; arrows/Home/End rove among enabled controls; Enter/Space activates current command.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>It's a flex <code>role=&quot;toolbar&quot;</code> row/column of <strong>native buttons</strong>. The only widget logic is <strong>roving tabindex</strong> so the whole toolbar is one tab stop with arrow-key traversal — implemented with the <strong><code>focusgroup</code> polyfill</strong> (native attribute when supported; ~30-line roving fallback otherwise). No bespoke key code.</li>\n<li><code>size</code>/<code>vertical</code>/<code>appearance</code> propagate to child buttons via attribute/inherited CSS, not JS re-render.</li>\n<li>Wrapping/overflow = CSS cluster (layout.md) + an optional overflow menu.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Toolbar is one Tab stop; arrows/Home/End rove among enabled controls; Enter/Space activates current command.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: page/editor/region that owns command effects.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Toolbar is one Tab stop; arrows/Home/End rove among enabled controls; Enter/Space activates current command.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Toolbar is one Tab stop; arrows/Home/End rove among enabled controls; Enter/Space activates current command.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Each command is a separate target; separators are inert.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — Toolbar is persistent, not an overlay.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: toolbar is one tab stop; next Tab leaves the toolbar after roving entry.</li>\n<li>Arrow-key entry behavior: arrow keys rove among peer controls inside the toolbar.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A unless a child control owns Escape.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: layout/overflow transitions never change roving-focus ownership.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated; controls inside may be form controls only if toolbar explicitly supports them.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated; controls inside may be form controls only if toolbar explicitly supports them.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Roving focus and slotchange listeners initialize on connect and clean up on disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: <strong>roving tabindex</strong> — exactly one child is tabbable; <code>ArrowLeft/Right</code> (or <code>Up/Down</code> when vertical) move active focus; <code>Home</code>/<code>End</code> jump to ends (W3C toolbar pattern). Implemented via focusgroup. Visible <code>:focus-visible</code> on the active control. Focus enters on Tab only (explicit action).</li>\n<li><strong>Hit targets</strong>: each control is one action with its own focusable target; dividers/groups are not targets. Do not overload a toolbar button with a second hover/secondary action — use a split/menu button (separate targets).</li>\n<li><strong>Native behavior</strong>: children are native buttons (Enter/Space native). Toolbar adds navigation, not activation.</li>\n<li><strong>Composite widgets</strong>: the toolbar <strong>owns</strong> roving focus and grouped checked state; children dispatch narrow internal change events the toolbar aggregates. It must ignore events from nested composites it doesn't own (e.g. a menu opened from a menu button).</li>\n<li><strong>State &amp; events</strong>: toggle/radio state reflected on children (<code>aria-pressed</code>/<code>aria-checked</code>); toolbar emits <code>checked-value-change</code> on user change only.</li>\n<li><strong>Disabled/readonly/loading</strong>: disabled commands in a toolbar should use <strong><code>disabled-focusable</code></strong> (kept in roving order, <code>aria-disabled</code>) so the tab/arrow sequence stays stable (behavior.md disabled-focusable case + Fluent commandbar guidance).</li>\n<li><strong>Motion</strong>: none beyond tokenized hover/focus; overflow expand/collapse is reduced-motion safe.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: inline-flex command row/column with wrapping or overflow policy.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p><code>default</code> (controls/groups/dividers).</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-toolbar-gap</code>, <code>--ty-toolbar-padding</code>, <code>--ty-toolbar-background</code>, <code>--ty-toolbar-divider-color</code>, <code>--ty-toolbar-min-block-size</code>.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>root</code>, <code>divider</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Root: <code>role=&quot;toolbar&quot;</code> + <code>aria-label</code>/<code>aria-labelledby</code> (required when &gt;1 toolbar). <code>aria-orientation</code> when <code>vertical</code>.</li>\n<li>Groups: <code>role=&quot;group&quot;</code>.</li>\n<li>Dividers: <code>role=&quot;separator&quot;</code> + <code>aria-orientation</code>, non-focusable.</li>\n<li>Toggle/radio controls keep their native/ARIA pressed/checked semantics; radio sets use <code>ToolbarRadioGroup</code> semantics (<code>role=&quot;radiogroup&quot;</code>).</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: [APG Toolbar](https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/).</li>\n<p>  - Direct requirements:</p>\n<p>    - Toolbar groups a set of controls and communicates grouping/purpose to screen readers.</p>\n<p>    - Use toolbar only when the group contains three or more controls.</p>\n<p>    - Keyboard tab sequence includes one stop for the toolbar; arrow keys move focus among controls.</p>\n<p>    - Horizontal toolbar uses Left/Right arrows; vertical toolbar uses Up/Down arrows.</p>\n<p>    - Tab and Shift+Tab move focus into and out of the toolbar.</p>\n<p>    - Home/End optionally move to first/last control.</p>\n<p>    - Avoid controls whose operation needs the same arrow keys used for toolbar navigation.</p>\n<p>    - Toolbar has <code>role=&quot;toolbar&quot;</code> and a label when visible label text is unavailable; vertical toolbar sets <code>aria-orientation=&quot;vertical&quot;</code>.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Toolbar</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Use toolbar only for groups of three or more controls.</p>\n<p>    - Label each toolbar when the application contains more than one.</p>\n<p>    - <code>size</code> controls toolbar control spacing; <code>vertical</code> changes orientation.</p>\n<p>    - <code>checkedValues</code>, <code>defaultCheckedValues</code>, and <code>onCheckedValueChange</code> coordinate toolbar toggle/radio state.</p>\n<p>    - ToolbarButton/ToggleButton/RadioButton respect toolbar sizing.</p>\n<p>    - ToolbarDivider is not interactive.</p>\n<p>    - Overflow uses Menu and the overflow trigger participates in toolbar navigation.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li>Roving focus is the heart of the component and the part that must degrade gracefully if focusgroup polyfill is absent → fall back to all-tabbable (still usable, just more tab stops). Document the enhancement boundary.</li>\n<li>Mixing menu buttons: opening a menu moves focus into the menu (a separate composite); on close, focus returns to the menu button — the toolbar must not treat the menu's arrow keys as its own.</li>\n<li>Vertical toolbars must swap arrow axis and set <code>aria-orientation=&quot;vertical&quot;</code>.</li>\n<li>Overflow: collapse least-important commands into a <code>tyui-menu</code>; the overflow trigger participates in roving order.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: inline-flex command row/column with wrapping or overflow policy remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: icons need labels via visible text, aria-label, or tooltip association.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: icons need labels via visible text, aria-label, or tooltip association.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/toolbar.md</code>): &quot;Group 3+ related commands. Label every toolbar. Use roving focus (single tab stop). Collapse extras into an overflow menu, don't wrap into many rows.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>1–2 actions → bare tyui-buttons</code>, <code>page nav → tyui-nav</code>, <code>form actions row → cluster layout</code>.</li>\n<li><strong>Layout ownership</strong>: toolbar owns arrangement, grouping, dividers, overflow, and roving; <strong>parent owns the toolbar's outer width/placement</strong>. layout.md: cluster + container query, not viewport breakpoints; <code>min-block-size</code>, not fixed height.</li>\n<li><strong>Token usage</strong>: gap/padding/divider via <code>--ty-toolbar-*</code>; child appearance via button tokens.</li>\n<li><strong>Anti-patterns to reject</strong>: toolbar with &lt;3 controls; unlabeled toolbar when multiple exist; focusable dividers; all-tabbable controls when roving is expected; overloaded buttons; wrapping rows instead of overflow.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;command-group&quot;,</p>\n<p>    &quot;composite&quot;: true,</p>\n<p>    &quot;rovingFocus&quot;: &quot;focusgroup-polyfilled&quot;,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;role&quot;: &quot;toolbar&quot;,</p>\n<p>      &quot;requiresLabel&quot;: &quot;when multiple&quot;,</p>\n<p>      &quot;disabledUsesFocusable&quot;: true</p>\n<p>    }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag <code>role</code> override; flag missing toolbar label with multiple toolbars; flag focusable separators; flag toolbars under 3 controls (warn).</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Single Tab stop</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Arrow/Home/End movement by orientation and direction</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled vs disabled-focusable commands</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td><code>checked-value-change</code> for grouped buttons</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Multiple toolbars require labels</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Separators not focusable</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Nested menu event isolation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Forced-colors focus ring distinct from hover/pressed/checked</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Single tab stop</td><td>Render toolbar with four buttons.</td><td>Press Tab into and out of toolbar.</td><td>Focus enters one active control and leaves toolbar on next Tab/Shift+Tab; controls are not all independent tab stops</td></tr><tr><td>APG: Horizontal navigation</td><td>Focus first command in horizontal toolbar.</td><td>Press ArrowRight, ArrowLeft, Home, End.</td><td>Focus moves among commands according to APG; separators are skipped</td></tr><tr><td>APG: Vertical navigation</td><td>Render <code>vertical</code>.</td><td>Press ArrowDown and ArrowUp.</td><td>Focus axis switches and <code>aria-orientation=&quot;vertical&quot;</code> is set</td></tr><tr><td>APG: Disabled discoverability</td><td>Render native disabled and disabled-focusable commands.</td><td>Arrow through toolbar and activate disabled command.</td><td>Disabled-focusable remains in roving order but is inert; native disabled can be skipped</td></tr><tr><td>Fluent UI: Under-three warning</td><td>Render toolbar with two buttons.</td><td>Run design validation.</td><td>Validation warns and suggests bare buttons or cluster layout</td></tr><tr><td>Fluent UI: Multiple-toolbar labels</td><td>Render two toolbars without labels.</td><td>Run accessibility validation.</td><td>Validation fails until each toolbar has a unique label</td></tr><tr><td>Fluent UI: Checked value coordination</td><td>Render controlled toggle/radio toolbar buttons.</td><td>Activate buttons by keyboard.</td><td><code>checked-value-change</code> emits <code>{ name, checkedItems }</code>; child ARIA pressed/checked state syncs</td></tr><tr><td>Fluent UI: Overflow command</td><td>Render toolbar with overflow menu.</td><td>Arrow to overflow trigger and open menu.</td><td>Trigger participates in roving order; menu owns its own focus while open and returns focus on close</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native buttons; Enter/Space native activation.</td></tr><tr><td>behavior.md</td><td>Roving tabindex (focusgroup); single tab stop; Home/End; visible ring.</td></tr><tr><td>behavior.md</td><td>One control = one action; dividers/groups not targets.</td></tr><tr><td>behavior.md</td><td>Composite root owns roving + grouped checked state; ignores foreign menu events.</td></tr><tr><td>behavior.md</td><td>User change → composed <code>checked-value-change</code>; programmatic silent.</td></tr><tr><td>behavior.md</td><td>Disabled commands use disabled-focusable parity.</td></tr><tr><td>behavior.md</td><td>Motion tokenized, reduced-motion safe.</td></tr></tbody></table>",
    )}
  </article>`,
};
