import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Dialog',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Dialog</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tags: <code>tyui-dialog</code> (+ <code>tyui-dialog-surface</code>, <code>tyui-dialog-title</code>, <code>tyui-dialog-body</code>, <code>tyui-dialog-actions</code>, <code>tyui-dialog-trigger</code>)</li>\n<li>Define: <code>@toyu-ui/define/dialog</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>native <code>&lt;dialog&gt;</code></strong> with <code>showModal()</code> (modal/alert) / <code>show()</code> (non-modal).</li>\n<li>Shadow DOM: minimal. The host renders a <code>&lt;dialog part=&quot;surface&quot;&gt;</code> plus slots for title/body/actions. Light-dismiss/backdrop/focus-trap/Esc come from the platform, not JS.</li>\n<li>Category: overlay (modal)</li>\n<li>Component family: popup/overlay</li>\n<li>Pattern type: modal dialog or alertdialog</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Present a window the user must engage with before continuing (modal), or a focused side task (non-modal), or an interrupting decision (alert).</p>\n<p>Do <strong>not</strong> nest dialogs (Fluent anti-pattern); do <strong>not</strong> use for non-blocking status (→ <code>tyui-message-bar</code>) or transient hints (→ <code>tyui-tooltip</code>).</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: blocking workflow, confirmation, or focused task requiring explicit dismissal.</li>\n<li>Do not use when: brief non-blocking hints, menus, or passive hover content.</li>\n<li>Prefer instead: Popover for non-modal disclosure, Tooltip for descriptions, MessageBar for status.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Backdrop/outside click closes only when explicitly dismissible; actions remain separate targets.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-dialog</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>modal-type</code></td><td><code>modal | non-modal | alert</code></td><td>yes</td><td><code>modal</code></td><td>drives <code>showModal</code> vs <code>show</code> + dismiss</td></tr><tr><td><code>open</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>controlled open state</td></tr><tr><td><code>default-open</code></td><td><code>boolean</code></td><td>no</td><td><code>false</code></td><td>uncontrolled initial</td></tr><tr><td><code>inert-trap-focus</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>use native dialog <code>inert</code> outside-trap</td></tr><tr><td><code>unmount-on-close</code></td><td><code>boolean</code></td><td>yes</td><td><code>true</code></td><td>remove surface when closed</td></tr></tbody></table>\n<h3>Events</h3>\n<ul>\n<li><code>open-change</code> — composed; <code>{ open, type }</code> where <code>type</code> ∈ <code>triggerClick \\| escapeKeyDown \\| backdropClick \\| closeButton</code>. Mirrors Fluent's <code>onOpenChange</code> (Esc carries the keyboard event). User-initiated only.</li>\n</ul>\n<h3>Slots</h3>\n<p><code>trigger</code>, <code>title</code>, <code>body</code>, <code>actions</code>, <code>backdrop</code> (custom backdrop, <code>aria-hidden</code>).</p>\n<h3>CSS parts</h3>\n<p><code>surface</code>, <code>backdrop</code>, <code>title</code>, <code>body</code>, <code>actions</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-dialog-background</code>, <code>--ty-dialog-radius</code>, <code>--ty-dialog-padding</code>, <code>--ty-dialog-max-inline-size</code>, <code>--ty-dialog-backdrop</code>, motion duration tokens.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: see Events above, including <code>open-change</code> — composed; <code>{ open, type }</code> where <code>type</code> ∈ <code>triggerClick \\| escapeKeyDown \\| backdropClick \\| closeButton</code>. Mirrors Fluent's <code>onOpenChange</code> (Esc carries the keyboard event). User-initiated only.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p><code>trigger</code>, <code>title</code>, <code>body</code>, <code>actions</code>, <code>backdrop</code> (custom backdrop, <code>aria-hidden</code>).</p>\n<h2>CSS Parts</h2>\n<p><code>surface</code>, <code>backdrop</code>, <code>title</code>, <code>body</code>, <code>actions</code>.</p>\n<h2>Events</h2>\n<ul>\n<li><code>open-change</code> — composed; <code>{ open, type }</code> where <code>type</code> ∈ <code>triggerClick \\| escapeKeyDown \\| backdropClick \\| closeButton</code>. Mirrors Fluent's <code>onOpenChange</code> (Esc carries the keyboard event). User-initiated only.</li>\n</ul>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: see Events above, including <code>open-change</code> — composed; <code>{ open, type }</code> where <code>type</code> ∈ <code>triggerClick \\| escapeKeyDown \\| backdropClick \\| closeButton</code>. Mirrors Fluent's <code>onOpenChange</code> (Esc carries the keyboard event). User-initiated only.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: title/body/actions slots; actions are separate controls. Triggers/actions may use native <code>&lt;button&gt;</code> or <code>tyui-button</code> when <code>tyui-button</code> exposes native-equivalent button semantics on the host; native <code>command</code>/<code>commandfor</code> and <code>form method=&quot;dialog&quot;</code> behavior require a real button path or a documented <code>tyui-button</code> forwarding API.</li>\n<li>Required parent: trigger/controller owns opening; dialog owns modal focus while open.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  <code>trigger</code>, <code>title</code>, <code>body</code>, <code>actions</code>, <code>backdrop</code> (custom backdrop, <code>aria-hidden</code>).</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-dialog&gt;&lt;tyui-dialog&gt;&lt;p&gt;Nested dialog&lt;/p&gt;&lt;/tyui-dialog&gt;&lt;/tyui-dialog&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: trigger/controller owns opening; dialog owns modal focus while open.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Opening moves focus according to initial-focus policy; modal Tab/Shift+Tab wrap; Escape closes when dismissible.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Native <code>&lt;dialog&gt;</code> gives the <strong>top layer</strong>, <code>::backdrop</code>, <strong>focus trap</strong>, <strong>Esc-to-close</strong>, background <code>inert</code> (with <code>inertTrapFocus</code>/<code>showModal</code>), and scroll-blocking — all <strong>for free</strong>. This is the flagship Oat win: a whole class of JS (focus-trap libs, portal, z-index juggling) disappears.</li>\n<li><code>modalType</code>: <code>modal</code>/<code>alert</code> → <code>showModal()</code>; <code>non-modal</code> → <code>show()</code>. <code>alert</code> additionally disables backdrop light-dismiss.</li>\n<li>Open/close wiring uses the <strong><code>command</code>/<code>commandfor</code> invoker</strong> (<code>command=&quot;show-modal&quot;</code> / <code>command=&quot;close&quot;</code>) when available, falling back to a tiny JS <code>showModal()/close()</code> call. Trigger and dialog must resolve ids in the same tree (see quirks).</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Opening moves focus according to initial-focus policy; modal Tab/Shift+Tab wrap; Escape closes when dismissible.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: trigger/controller owns opening; dialog owns modal focus while open.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Opening moves focus according to initial-focus policy; modal Tab/Shift+Tab wrap; Escape closes when dismissible.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Opening moves focus according to initial-focus policy; modal Tab/Shift+Tab wrap; Escape closes when dismissible.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Backdrop/outside click closes only when explicitly dismissible; actions remain separate targets.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: trigger activation, command/commandfor, or programmatic <code>open=true</code> using <code>showModal()</code> for modal/alert and <code>show()</code> for non-modal.</li>\n<li>Closes on: close action, <code>form method=&quot;dialog&quot;</code>, Escape when allowed, and backdrop click for dismissible modal only.</li>\n<li>DOM focus while open: modal/alert focus stays inside; non-modal allows focus to leave after documented Tab path.</li>\n<li>Next Tab behavior: modal wraps; non-modal proceeds to the next document focus target after the last dialog control.</li>\n<li>Arrow-key entry behavior: N/A except nested composites own their own arrow behavior.</li>\n<li>Outside click / pointerdown behavior: backdrop light-dismiss only when not alert and explicitly dismissible.</li>\n<li>Escape behavior: closes dismissible dialogs and emits <code>open-change</code> with escape reason.</li>\n<li>Focus restoration on close: return to invoking trigger unless workflow documents a more logical target.</li>\n<li>Behavior while enter / exit motion is running: logical close happens immediately; content is inert/hidden and focus has already returned.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Forms inside dialog use native form semantics; dialog may close on successful submit when configured.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Forms inside dialog use native form semantics; dialog may close on successful submit when configured.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Native cancel/close/backdrop/trigger listeners install on open/connect and are removed on close/disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: on open, focus moves into the dialog — to <code>autofocus</code>/first focusable (explicit user action opened it, so this is allowed). Native focus trap keeps Tab inside (modal). On close, focus <strong>returns to the trigger</strong> (native <code>&lt;dialog&gt;</code> does this). <code>non-modal</code> lets Tab leave after the last element (behavior.md: define next-keyboard-action).</li>\n<li><strong>Hit targets</strong>: trigger opens; each action button is its own target; the close (×) is a separate labelled button. No overloaded surface click except modal backdrop dismiss (documented).</li>\n<li><strong>Native behavior</strong>: Esc closes (modal/non-modal), backdrop click closes (modal, not alert), <code>&lt;form method=&quot;dialog&quot;&gt;</code> submit closes with the button's <code>value</code> — all native; reflect into <code>open-change</code>.</li>\n<li><strong>Composite widgets</strong>: dialog is a focus scope owner. Nested menus/comboboxes are separate composites; set <code>aria-modal=&quot;false&quot;</code> so their popups remain reachable.</li>\n<li><strong>State &amp; events</strong>: <code>open</code> reflected; user open/close emits <code>open-change</code>; programmatic <code>open</code> set updates state but should not double-emit a user event.</li>\n<li><strong>Disabled/readonly/loading</strong>: a pending async action keeps the dialog open with <code>aria-busy</code> on the body; don't disable the whole surface. No readonly.</li>\n<li><strong>Motion</strong>: surface scale + backdrop fade via tokens, <code>prefers-reduced-motion</code>-safe. During exit motion (delayed unmount) the dialog is <strong>logically closed</strong> — focus already returned, content <code>inert</code>/<code>aria-hidden</code> (behavior.md delayed-unmount rule).</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: centered top-layer surface with max block/inline size and internal scroll region.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p><code>trigger</code>, <code>title</code>, <code>body</code>, <code>actions</code>, <code>backdrop</code> (custom backdrop, <code>aria-hidden</code>).</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-dialog-background</code>, <code>--ty-dialog-radius</code>, <code>--ty-dialog-padding</code>, <code>--ty-dialog-max-inline-size</code>, <code>--ty-dialog-backdrop</code>, motion duration tokens.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>surface</code>, <code>backdrop</code>, <code>title</code>, <code>body</code>, <code>actions</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Native <code>&lt;dialog&gt;</code> → <code>role=&quot;dialog&quot;</code>; <code>modal-type=&quot;alert&quot;</code> sets <code>role=&quot;alertdialog&quot;</code>.</li>\n<li><code>aria-labelledby</code> → the title; if no title, require <code>aria-label</code>.</li>\n<li><code>aria-describedby</code> → main content for short confirmations; for complex content set it to <code>undefined</code> (Fluent) to avoid over-reading.</li>\n<li>When a <code>Menu</code>/<code>Combobox</code>/<code>Popover</code> lives inside, set <code>aria-modal=&quot;false&quot;</code> on the surface (Fluent/VoiceOver workaround).</li>\n<li><code>&lt;dialog&gt;</code> must contain at least one focusable element (Fluent: never empty/non-focusable).</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Patterns: [APG Dialog (Modal)](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) and [APG Alert Dialog](https://www.w3.org/WAI/ARIA/apg/patterns/alertdialog/) for <code>modal-type=&quot;alert&quot;</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - On open, focus moves to an element inside the dialog; initial focus may be a static <code>tabindex=&quot;-1&quot;</code> element when content structure needs to be read first.</p>\n<p>    - In modal dialog, <code>Tab</code> and <code>Shift+Tab</code> wrap within the dialog.</p>\n<p>    - <code>Escape</code> closes the dialog.</p>\n<p>    - On close, focus returns to the invoking element unless workflow requires another logical target.</p>\n<p>    - Dialog container is <code>role=&quot;dialog&quot;</code> with <code>aria-modal=&quot;true&quot;</code> only when outside content is truly inert and visually obscured.</p>\n<p>    - Alert dialog uses <code>role=&quot;alertdialog&quot;</code>, <code>aria-modal=&quot;true&quot;</code>, a label, and <code>aria-describedby</code> pointing to the alert message.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Dialog</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Structure includes title, content, actions inside a body.</p>\n<p>    - Modal dialogs are used sparingly for required/destructive choices.</p>\n<p>    - Do not use more than three action buttons.</p>\n<p>    - Do not open nested dialogs.</p>\n<p>    - Do not use a dialog with no focusable elements.</p>\n<p>    - Use <code>aria-describedby</code> for short confirmations, but set it undefined for complex form content.</p>\n<p>    - If Menu, Combobox, Dropdown, or Popover is inside the dialog, <code>aria-modal</code> should be false on the surface for VoiceOver reachability.</p>\n<p>    - <code>modalType</code> drives modal, non-modal, or alert behavior; alert is not dismissed by backdrop click.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><strong>Trigger ↔ surface across shadow boundaries</strong>: <code>command</code>/<code>commandfor</code> and focus return rely on same-tree id resolution and real button support. If the trigger is <code>tyui-button</code> or the <code>&lt;dialog&gt;</code> is in another root, wire opening via the component's JS trigger API instead of assuming native <code>commandfor</code>. Document the chosen mechanism.</li>\n<li><strong>No nested dialogs</strong> — enforce; suggest wizard/sequential/side-panel instead.</li>\n<li><code>aria-describedby</code> default points at the whole content; for forms set it to <code>undefined</code> to avoid the SR reading the entire form (Fluent).</li>\n<li>SSR: with <code>unmount-on-close=false</code> the surface stays mounted; native <code>&lt;dialog&gt;</code> avoids the React-portal SSR hydration issue Fluent warns about — a genuine advantage of going native.</li>\n<li><code>alert</code> must not light-dismiss (no backdrop close, Esc still allowed per platform but Fluent expects an explicit choice — document the policy).</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: centered top-layer surface with max block/inline size and internal scroll region remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: icons in alert dialogs are decorative unless they add unique warning meaning.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: icons in alert dialogs are decorative unless they add unique warning meaning.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/dialog.md</code>): &quot;Use modal sparingly for required/destructive decisions; non-modal for side tasks; alert for interruptions. ≤3 action buttons. Never nest dialogs.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>non-blocking status → tyui-message-bar</code>, <code>contextual content → tyui-popover</code>, <code>multi-step → wizard in one dialog</code>, <code>side task → drawer/panel</code>.</li>\n<li><strong>Layout ownership</strong>: dialog owns surface max-inline-size + internal title/body/actions layout; <strong>parent/viewport owns centering (top layer)</strong>. layout.md: <code>max-inline-size</code> token + intrinsic height, content scrolls inside.</li>\n<li><strong>Token usage</strong>: surface/backdrop/motion via <code>--ty-dialog-*</code>.</li>\n<li><strong>Anti-patterns to reject</strong>: nested dialogs; &gt;3 actions; empty/non-focusable dialog; modal for trivial info; missing label when no title; describedby over a complex form.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;modal-overlay&quot;,</p>\n<p>    &quot;nativeElement&quot;: &quot;dialog&quot;,</p>\n<p>    &quot;topLayer&quot;: true,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;focusTrap&quot;: &quot;native&quot;,</p>\n<p>      &quot;returnsFocusToTrigger&quot;: true,</p>\n<p>      &quot;alertUsesAlertdialog&quot;: true,</p>\n<p>      &quot;ariaModalFalseWhenNestedPopup&quot;: true</p>\n<p>    },</p>\n<p>    &quot;alternatives&quot;: { &quot;status&quot;: &quot;tyui-message-bar&quot;, &quot;contextual&quot;: &quot;tyui-popover&quot; }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag nested dialogs; flag &gt;3 actions; flag dialog with no focusable child; flag missing labelledby/aria-label.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td><code>showModal()</code> vs <code>show()</code> selection</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Esc/backdrop/action close reasons</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Alert ignores backdrop close</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Focus enters first/autofocus target and returns to trigger</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Non-modal next Tab path</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Form <code>method=&quot;dialog&quot;</code> return value</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Programmatic <code>open</code> is silent</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Nested popup reachability</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr><tr><td><code>tyui-button</code> trigger/action end-to-end</td><td>Render <code>tyui-dialog</code> with <code>tyui-button slot=&quot;trigger&quot;</code> and <code>tyui-button slot=&quot;actions&quot;</code>.</td><td>Open with Enter/Space/click; close with Escape and action button.</td><td>Dialog opens, traps focus, emits close reason, action buttons work, and focus restores to the <code>tyui-button</code> trigger.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Initial focus placement</td><td>Open a modal dialog with short content and one with structured content.</td><td>Activate each trigger.</td><td>Short dialog focuses the first/autofocus control; structured dialog may focus a static intro/title and omits broad <code>aria-describedby</code></td></tr><tr><td>APG: Modal tab containment</td><td>Open a modal dialog with at least two tabbable elements.</td><td>Press <code>Tab</code> and <code>Shift+Tab</code> repeatedly.</td><td>Focus wraps inside the dialog and never moves to inert page content</td></tr><tr><td>APG: Escape and focus return</td><td>Open from a button, press <code>Escape</code>.</td><td>Observe focus and events.</td><td>Dialog closes, <code>open-change</code> reports escape reason, and focus returns to the opener or documented fallback</td></tr><tr><td>APG: Alert dialog semantics</td><td>Open <code>modal-type=&quot;alert&quot;</code>.</td><td>Inspect accessibility tree.</td><td>Surface exposes <code>alertdialog</code>, <code>aria-modal</code>, label, and <code>aria-describedby</code> for the alert message</td></tr><tr><td>Fluent UI: Action count guard</td><td>Render four actions in <code>tyui-dialog-actions</code>.</td><td>Run design validation.</td><td>Validation fails and asks for three or fewer actions</td></tr><tr><td>Fluent UI: Nested dialog guard</td><td>Render a dialog trigger inside another open dialog.</td><td>Run validation.</td><td>Nested dialog is rejected with wizard/sequential/panel alternatives</td></tr><tr><td>Fluent UI: Complex content describedby</td><td>Render a form-heavy dialog body.</td><td>Inspect surface attributes.</td><td><code>aria-describedby</code> is omitted unless the description is short and linear</td></tr><tr><td>Fluent UI: Nested popup workaround</td><td>Render a menu/popover inside a modal dialog.</td><td>Open nested popup under VoiceOver test fixture.</td><td>Popup is reachable; surface applies the documented <code>aria-modal=false</code> workaround when required</td></tr><tr><td>Fluent UI: <code>tyui-button</code> invoker contract</td><td>Render <code>tyui-button</code> as dialog trigger and action.</td><td>Inspect command/JS trigger path, open, close, and restore focus.</td><td>The button exposes native-equivalent activation and required ARIA/focus surface; no native <code>commandfor</code> assumption is made unless a real button path exists</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → <code>&lt;dialog&gt;</code>, native trap/Esc/backdrop/inert/focus-return.</td></tr><tr><td>behavior.md</td><td>Focus enters on (user-triggered) open; returns to trigger on close; non-modal next-Tab defined.</td></tr><tr><td>behavior.md</td><td>Separate targets: trigger / actions / close.</td></tr><tr><td>behavior.md</td><td>Composite: <code>aria-modal=false</code> so nested popups stay reachable.</td></tr><tr><td>behavior.md</td><td>User open/close → composed <code>open-change</code>; programmatic silent.</td></tr><tr><td>behavior.md</td><td>Pending uses <code>aria-busy</code>, not full disable.</td></tr><tr><td>behavior.md</td><td>Motion tokenized; logically closed during exit; reduced-motion safe.</td></tr></tbody></table>")}
  </article>`,
};
