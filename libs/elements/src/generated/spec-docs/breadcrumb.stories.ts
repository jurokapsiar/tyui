import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Breadcrumb',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Breadcrumb</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tags: <code>tyui-breadcrumb</code>, <code>tyui-breadcrumb-item</code>, <code>tyui-breadcrumb-divider</code> (divider may be CSS-generated)</li>\n<li>Define: <code>@toyu-ui/define/breadcrumb</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <code>&lt;nav&gt;</code> → <code>&lt;ol&gt;</code> → <code>&lt;li&gt;</code>, with native <code>&lt;a href&gt;</code> (links) / <code>&lt;button&gt;</code> (in-app actions) inside each item.</li>\n<li>Shadow DOM: minimal on the root (renders <code>&lt;nav&gt;&lt;ol&gt;&lt;slot&gt;&lt;/slot&gt;&lt;/ol&gt;&lt;/nav&gt;</code>); items are light-DOM <code>&lt;li&gt;</code>s so the ordered-list semantics and <code>aria-current</code> stay intact. Dividers are decorative (<code>aria-hidden</code>), ideally CSS <code>::before</code> content rather than elements.</li>\n<li>Category: navigation (composite, roving via focusgroup polyfill in <code>arrow</code> mode)</li>\n<li>Component family: navigation</li>\n<li>Pattern type: breadcrumb landmark</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Show the user's location in a hierarchy and let them navigate up it.</p>\n<p>Do <strong>not</strong> use for unrelated tabs/steps (→ <code>tyui-tablist</code>/wizard) or for primary navigation menus (→ <code>tyui-nav</code>).</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: hierarchical page/location navigation with one current item.</li>\n<li>Do not use when: steps, tabs, menus, or arbitrary link groups.</li>\n<li>Prefer instead: Tabs for peer views, Menu for commands, Progress/steps for task progress.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Each crumb link is its own navigation target; separators are decorative.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-breadcrumb</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>size</code></td><td><code>small | medium | large</code></td><td>yes</td><td><code>medium</code></td><td>item + divider size</td></tr><tr><td><code>focus-mode</code></td><td><code>tab | arrow</code></td><td>yes</td><td><code>tab</code></td><td><code>arrow</code> → focusgroup polyfill</td></tr></tbody></table>\n<h3>tyui-breadcrumb-item</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>current</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>sets <code>aria-current=&quot;page&quot;</code>, removes link affordance</td></tr><tr><td><code>size</code></td><td>inherited</td><td>yes</td><td>from root</td><td></td></tr></tbody></table>\n<p>Item content is a native <code>&lt;a&gt;</code>/<code>&lt;button&gt;</code> (or <code>tyui-link</code>). <code>icon</code> via a slot.</p>\n<h3>Events</h3>\n<p>None from the breadcrumb itself; navigation is native (<code>&lt;a&gt;</code>). Selection is just following a link.</p>\n<h3>Slots</h3>\n<p><code>default</code> (items); per-item: <code>default</code> (label), <code>icon</code>.</p>\n<h3>CSS parts</h3>\n<p><code>list</code>, <code>item</code>, <code>divider</code>, <code>icon</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-breadcrumb-gap</code>, <code>--ty-breadcrumb-color</code>, <code>--ty-breadcrumb-current-color</code>, <code>--ty-breadcrumb-divider-color</code>, <code>--ty-breadcrumb-font-size</code>.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: None from the breadcrumb itself; navigation is native (<code>&lt;a&gt;</code>). Selection is just following a link.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p><code>default</code> (items); per-item: <code>default</code> (label), <code>icon</code>.</p>\n<h2>CSS Parts</h2>\n<p><code>list</code>, <code>item</code>, <code>divider</code>, <code>icon</code>.</p>\n<h2>Events</h2>\n<p>None from the breadcrumb itself; navigation is native (<code>&lt;a&gt;</code>). Selection is just following a link.</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: None from the breadcrumb itself; navigation is native (<code>&lt;a&gt;</code>). Selection is just following a link.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: ordered breadcrumb items containing native anchors except the current page.</li>\n<li>Required parent: none; component owns ordered-list navigation semantics.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  <code>default</code> (items); per-item: <code>default</code> (label), <code>icon</code>.</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-breadcrumb&gt;&lt;button&gt;Open menu&lt;/button&gt;&lt;button&gt;Current&lt;/button&gt;&lt;/tyui-breadcrumb&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: none; component owns ordered-list navigation semantics.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Native link keyboard behavior only; Tab moves link-to-link and Enter follows anchors.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Pure semantic HTML: <code>nav &gt; ol &gt; li &gt; a</code>. Screen-reader list semantics and link behavior are <strong>free</strong>.</li>\n<li><code>focusMode=&quot;tab&quot;</code> (default) needs <strong>no JS</strong> — each link is a native tab stop.</li>\n<li><code>focusMode=&quot;arrow&quot;</code> uses a roving-tabindex composite → implement with the <strong><code>focusgroup</code> polyfill</strong> (native <code>focusgroup</code> attribute when supported, JS fallback otherwise). The component does not hand-roll arrow logic.</li>\n<li>Dividers and sizes are CSS only.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Native link keyboard behavior only; Tab moves link-to-link and Enter follows anchors.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: none; component owns ordered-list navigation semantics.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Native link keyboard behavior only; Tab moves link-to-link and Enter follows anchors.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Native link keyboard behavior only; Tab moves link-to-link and Enter follows anchors.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Each crumb link is its own navigation target; separators are decorative.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — Breadcrumb is inline navigation, not a popup.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: native sequential focus through links.</li>\n<li>Arrow-key entry behavior: N/A; do not add menu-like arrow navigation.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: decorative transitions must not affect navigation semantics.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: No external listeners; optional ResizeObserver/slotchange for overflow must disconnect on removal.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: <code>tab</code> mode → each link is a native tab stop (focus on explicit Tab). <code>arrow</code> mode → roving tabindex: one tab stop, arrows move among items (focusgroup). Visible <code>:focus-visible</code> ring; distinct from hover/current.</li>\n<li><strong>Hit targets</strong>: each item exposes exactly one action (navigate). Current item is not a second action. Overflow &quot;…&quot; is its own separate target (a menu trigger), not overloaded onto an item.</li>\n<li><strong>Native behavior</strong>: native <code>&lt;a&gt;</code> activation, modified-click, context menu preserved; no scripted navigation.</li>\n<li><strong>Composite widgets</strong>: in <code>arrow</code> mode it's a roving composite; the root owns the tabindex coordination (behavior.md composite ownership). Children expose minimal state (<code>current</code>).</li>\n<li><strong>State &amp; events</strong>: <code>current</code> reflected via <code>aria-current</code>. No custom events; following a link is native.</li>\n<li><strong>Disabled/readonly/loading</strong>: a disabled crumb uses <code>disabled-focusable</code> parity (kept focusable with <code>aria-disabled</code> in command-bar-like contexts) per behavior.md; otherwise omit it.</li>\n<li><strong>Motion</strong>: none beyond tokenized hover/focus color transitions.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: inline/row navigation list with wrapping or explicit overflow policy.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p><code>default</code> (items); per-item: <code>default</code> (label), <code>icon</code>.</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-breadcrumb-gap</code>, <code>--ty-breadcrumb-color</code>, <code>--ty-breadcrumb-current-color</code>, <code>--ty-breadcrumb-divider-color</code>, <code>--ty-breadcrumb-font-size</code>.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>list</code>, <code>item</code>, <code>divider</code>, <code>icon</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Root: <code>&lt;nav&gt;</code> + <code>aria-label</code> (required, since there can be multiple navs).</li>\n<li>Structure: <code>&lt;ol&gt;</code>/<code>&lt;li&gt;</code> native list semantics.</li>\n<li>Current page: <code>aria-current=&quot;page&quot;</code> on the last item's link; render it as non-link text or a link-to-self.</li>\n<li>Dividers: <code>aria-hidden=&quot;true&quot;</code> / decorative.</li>\n<li>Overflow (&quot;…&quot;): a <code>tyui-menu</code> trigger button labelled e.g. &quot;More breadcrumbs&quot;.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: [APG Breadcrumb](https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/).</li>\n<p>  - Direct requirements:</p>\n<p>    - Breadcrumb is contained in a navigation landmark.</p>\n<p>    - The landmark has <code>aria-label</code> or <code>aria-labelledby</code>.</p>\n<p>    - Breadcrumb items are links to parent pages in hierarchy order.</p>\n<p>    - The current page link has <code>aria-current=&quot;page&quot;</code>; if current page is plain text, <code>aria-current</code> is optional.</p>\n<p>    - APG defines no breadcrumb-specific keyboard interaction; native link Tab/Enter behavior is preserved.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Breadcrumb</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - <code>focusMode=&quot;tab&quot;</code> cycles through inner elements with Tab and releases focus after the last item.</p>\n<p>    - <code>focusMode=&quot;arrow&quot;</code> uses Arrow keys to cycle through items.</p>\n<p>    - <code>size</code> controls item and divider size.</p>\n<p>    - <code>BreadcrumbItem</code> wraps link/button/non-interactive current content.</p>\n<p>    - Overflow uses a Menu trigger with a labelled &quot;more items&quot; button; hidden items become menu links/items.</p>\n<p>    - <code>disabledFocusable</code> is allowed only for stable order scenarios.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li>Last item duplicated as both link and <code>aria-current</code> confuses SRs — render current as plain text or self-link with <code>aria-current=&quot;page&quot;</code> only.</li>\n<li>Overflow collapsing must keep first + last visible and move the middle into a menu; the menu trigger needs its own label and follows <code>tyui-menu</code> behavior.</li>\n<li><code>arrow</code> focus mode is the only stateful part — it must degrade to <code>tab</code> if the focusgroup polyfill is unavailable (progressive enhancement).</li>\n<li>Dividers as real <code>&lt;li&gt;</code>s must be <code>aria-hidden</code>; better as CSS so they never enter the a11y tree or focus order.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: inline/row navigation list with wrapping or explicit overflow policy remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: separators are decorative text/icons hidden from assistive tech.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: separators are decorative text/icons hidden from assistive tech.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/breadcrumb.md</code>): &quot;Use for hierarchical location with up-navigation. Always set <code>aria-label</code>. Mark the final crumb <code>current</code>. Collapse long trails into an overflow menu, not a wrapped row.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>step progress → wizard</code>, <code>peer views → tyui-tablist</code>, <code>site nav → tyui-nav</code>.</li>\n<li><strong>Layout ownership</strong>: component owns item/divider arrangement + overflow; <strong>parent owns available width</strong>. layout.md: use intrinsic flex + overflow menu, not viewport breakpoints.</li>\n<li><strong>Token usage</strong>: colors/gaps via <code>--ty-breadcrumb-*</code>; current vs link distinguished by more than color (weight/<code>aria-current</code>).</li>\n<li><strong>Anti-patterns to reject</strong>: breadcrumb without <code>aria-label</code>; final crumb as an active link with no <code>aria-current</code>; focusable dividers; wrapping instead of overflow; arrow mode with no tab fallback.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;navigation&quot;,</p>\n<p>    &quot;composite&quot;: true,</p>\n<p>    &quot;rovingFocus&quot;: &quot;arrow mode (focusgroup-polyfilled)&quot;,</p>\n<p>    &quot;accessibility&quot;: { &quot;requiresAriaLabel&quot;: true, &quot;currentItem&quot;: &quot;aria-current=page&quot; }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag missing <code>aria-label</code>; flag last item without <code>aria-current</code>; flag focusable dividers; flag arrow mode without focusgroup fallback.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Native tab order in tab mode</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Arrow/Home/End wrapping in arrow mode</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td><code>aria-current=&quot;page&quot;</code> on exactly one current item</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Divider not focusable or announced</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Overflow target opens separately</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled-focusable item remains in roving order while suppressing activation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Generated design validation catches missing labels/current state</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Labelled navigation landmark</td><td>Render one breadcrumb and another page navigation landmark.</td><td>Inspect roles/names.</td><td>Breadcrumb exposes a labelled <code>nav</code>; missing label fails validation</td></tr><tr><td>APG: Ordered hierarchical links</td><td>Render Home, Products, Current.</td><td>Inspect DOM and accessibility tree.</td><td>Items are in <code>ol/li</code> order and links preserve native <code>href</code> behavior</td></tr><tr><td>APG: Current page state</td><td>Mark the final item <code>current</code>.</td><td>Inspect final item.</td><td>Exactly one current item has <code>aria-current=&quot;page&quot;</code> when it is a link; dividers are hidden</td></tr><tr><td>APG: Native keyboard in tab mode</td><td>Render <code>focus-mode=&quot;tab&quot;</code>.</td><td>Press <code>Tab</code> and <code>Enter</code> on each crumb.</td><td>Focus follows native link order and activation is native navigation; no custom arrow handling is required</td></tr><tr><td>Fluent UI: Tab focus mode release</td><td>Render three crumbs in <code>focus-mode=&quot;tab&quot;</code>.</td><td>Press <code>Tab</code> from before the breadcrumb through it.</td><td>Each interactive crumb receives focus, then focus leaves the breadcrumb after the last crumb</td></tr><tr><td>Fluent UI: Arrow focus mode</td><td>Render <code>focus-mode=&quot;arrow&quot;</code> with four enabled items.</td><td>Focus first item, press ArrowRight, ArrowLeft, Home, End.</td><td>Roving focus moves among crumb controls without making dividers focusable</td></tr><tr><td>Fluent UI: Overflow menu semantics</td><td>Render a collapsed breadcrumb with hidden middle items.</td><td>Activate the overflow trigger.</td><td>Trigger has an accessible count/name; menu opens with hidden items as real menu links</td></tr><tr><td>Fluent UI: Disabled-focusable parity</td><td>Render a disabled focusable crumb in arrow mode.</td><td>Arrow through items and press Enter on the disabled item.</td><td>Disabled crumb remains reachable if configured but activation is suppressed and announced disabled</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → <code>nav&gt;ol&gt;li&gt;a</code>, native nav.</td></tr><tr><td>behavior.md</td><td>One item = one action; overflow is a separate target.</td></tr><tr><td>behavior.md</td><td>Focus: tab stops (tab mode) / roving via focusgroup (arrow mode), visible ring.</td></tr><tr><td>behavior.md</td><td>Composite root owns roving coordination.</td></tr><tr><td>behavior.md</td><td><code>current</code> reflected via <code>aria-current</code>; no spurious events.</td></tr><tr><td>behavior.md</td><td>Disabled-focusable parity available.</td></tr><tr><td>behavior.md</td><td>Motion tokenized, reduced-motion safe.</td></tr></tbody></table>")}
  </article>`,
};
