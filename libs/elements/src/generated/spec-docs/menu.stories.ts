import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Menu',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Menu</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tags: <code>tyui-menu</code>, <code>tyui-menu-trigger</code>, <code>tyui-menu-popover</code>, <code>tyui-menu-list</code>, <code>tyui-menu-item</code>, <code>tyui-menu-item-checkbox</code>, <code>tyui-menu-item-radio</code>, <code>tyui-menu-item-link</code>, <code>tyui-menu-group</code>, <code>tyui-menu-group-header</code>, <code>tyui-menu-divider</code></li>\n<li>Define: <code>@toyu-ui/define/menu</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>Popover API</strong> (<code>popover</code>) for the surface + <strong>CSS Anchor Positioning</strong>; items are native <code>&lt;button&gt;</code>/<code>&lt;a&gt;</code> (link items) carrying menu roles; roving focus via the <strong><code>focusgroup</code> polyfill</strong>. There is <strong>no</strong> native menu _widget_, so this is the one component here that genuinely needs an ARIA composite — but the surface/dismiss/top-layer still come from the platform.</li>\n<li>Shadow DOM: minimal (surface + slots). Items are light-DOM native buttons/links with menu roles.</li>\n<li>Category: composite overlay (actions list)</li>\n<li>Component family: composite popup</li>\n<li>Pattern type: menu button plus menu/menubar</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Present a list of <strong>actions</strong> (and selectable checkbox/radio items) triggered by a button.</p>\n<p>Do <strong>not</strong> use for navigation between peer views (→ <code>tyui-tablist</code>), for form selection from data (→ <code>tyui-select</code>/combobox), or for non-action content (→ <code>tyui-popover</code>).</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: transient command list opened from a trigger.</li>\n<li>Do not use when: form data selection, persistent navigation lists, tabs, or arbitrary focusable content.</li>\n<li>Prefer instead: Select/Listbox for data selection, Toolbar for visible commands, Popover for rich content.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Each item is one command/toggle/link; no interactive descendants inside items.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-menu</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>open</code> / <code>default-open</code></td><td><code>boolean</code></td><td>yes/no</td><td><code>false</code></td><td>controlled/uncontrolled</td></tr><tr><td><code>open-on-hover</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>hover-open (menu exception, see behavior)</td></tr><tr><td><code>open-on-context</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>right-click; disables other triggers</td></tr><tr><td><code>persist-on-item-click</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>keep open after activation</td></tr><tr><td><code>has-icons</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>alignment column</td></tr><tr><td><code>has-checkmarks</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>alignment column</td></tr><tr><td><code>checked-values</code></td><td>property <code>Record&lt;string,string[]&gt;</code></td><td>—</td><td>—</td><td>checkbox/radio state</td></tr><tr><td><code>hover-delay</code></td><td>number</td><td>—</td><td>—</td><td></td></tr><tr><td><code>positioning</code></td><td>property</td><td>—</td><td>auto</td><td>CSS anchor</td></tr><tr><td><code>inline</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>render in DOM order</td></tr><tr><td><code>close-on-scroll</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td></td></tr></tbody></table>\n<h3>tyui-menu-item</h3>\n<p><code>disabled</code> (kept focusable — Fluent: disabled is focusable by default in menus), <code>has-submenu</code>, <code>persist-on-click</code>; slots <code>icon</code>, <code>content</code>, <code>secondary-content</code> (shortcut text), <code>submenu-indicator</code>, <code>checkmark</code>, <code>sub-text</code>.</p>\n<h3>Events</h3>\n<ul>\n<li><code>open-change</code> — composed; <code>{ open, reason }</code>.</li>\n<li><code>checked-value-change</code> — composed; <code>{ name, checkedItems }</code> for checkbox/radio items.</li>\n<li>per-item activation surfaces as a composed <code>activate</code> from the item (consumer hears it), aggregated by the menu. User-initiated only.</li>\n</ul>\n<h3>Slots</h3>\n<p>Menu: <code>trigger</code>, <code>popover</code>/<code>list</code>. Item slots as above.</p>\n<h3>CSS parts</h3>\n<p><code>popover</code>, <code>list</code>, <code>item</code>, <code>icon</code>, <code>checkmark</code>, <code>submenu-indicator</code>, <code>secondary-content</code>, <code>divider</code>, <code>group-header</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-menu-background</code>, <code>--ty-menu-radius</code>, <code>--ty-menu-item-padding</code>, <code>--ty-menu-item-hover</code>, <code>--ty-menu-item-foreground</code>, <code>--ty-menu-min-inline-size</code>, <code>--ty-menu-icon-size</code>.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: see Events above, including <code>open-change</code> — composed; <code>{ open, reason }</code>.</li>\n<li><code>checked-value-change</code> — composed; <code>{ name, checkedItems }</code> for checkbox/radio items.</li>\n<li>per-item activation surfaces as a composed <code>activate</code> from the item (consumer hears it), aggregated by the menu. User-initiated only.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p>Menu: <code>trigger</code>, <code>popover</code>/<code>list</code>. Item slots as above.</p>\n<h2>CSS Parts</h2>\n<p><code>popover</code>, <code>list</code>, <code>item</code>, <code>icon</code>, <code>checkmark</code>, <code>submenu-indicator</code>, <code>secondary-content</code>, <code>divider</code>, <code>group-header</code>.</p>\n<h2>Events</h2>\n<ul>\n<li><code>open-change</code> — composed; <code>{ open, reason }</code>.</li>\n<li><code>checked-value-change</code> — composed; <code>{ name, checkedItems }</code> for checkbox/radio items.</li>\n<li>per-item activation surfaces as a composed <code>activate</code> from the item (consumer hears it), aggregated by the menu. User-initiated only.</li>\n</ul>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: see Events above, including <code>open-change</code> — composed; <code>{ open, reason }</code>.</li>\n<li><code>checked-value-change</code> — composed; <code>{ name, checkedItems }</code> for checkbox/radio items.</li>\n<li>per-item activation surfaces as a composed <code>activate</code> from the item (consumer hears it), aggregated by the menu. User-initiated only.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: trigger first; popover contains one menu list; items/groups/dividers only. The trigger may be a native <code>&lt;button&gt;</code> or a <code>tyui-button</code> only when <code>tyui-button</code> exposes native-equivalent button semantics on the host and accepts/forwards <code>aria-haspopup</code>, <code>aria-expanded</code>, activation, focus, and disabled state.</li>\n<li>Required parent: none; submenus are separate menu instances owned by parent item handoff.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  Menu: <code>trigger</code>, <code>popover</code>/<code>list</code>. Item slots as above.</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-menu-item&gt;&lt;button&gt;Nested button&lt;/button&gt;&lt;/tyui-menu-item&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: none; submenus are separate menu instances owned by parent item handoff.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Enter/Space/Arrow opens; arrows/Home/End/typeahead move roving focus; Escape closes and restores focus; Tab leaves and closes.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Surface uses the <strong>Popover API</strong>: top-layer, light-dismiss, Esc, one-open-at-a-time — <strong>free</strong>. Positioning via <strong>CSS anchor</strong>; submenu offset via <code>position-area</code>.</li>\n<li>Items are real <code>&lt;button&gt;</code>/<code>&lt;a&gt;</code> → native activation, Enter/Space, link semantics for <code>MenuItemLink</code>.</li>\n<li>The menu <strong>composite</strong> layer (roles <code>menu</code>/<code>menuitem(checkbox/radio)</code>, arrow-key roving, typeahead, submenu open on ArrowRight/hover) is the irreducible ARIA work — implement roving with the <strong>focusgroup polyfill</strong> + a tiny keyboard controller. This is the deliberate exception behavior.md allows (&quot;custom ARIA widgets only when native elements cannot express the interaction&quot;).</li>\n<li>Trigger uses native <code>popovertarget</code> where the trigger is a real <code>&lt;button&gt;</code> and ids resolve in the same tree; <code>tyui-button</code> and other custom triggers use the component's JS open API/trigger adapter.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Enter/Space/Arrow opens; arrows/Home/End/typeahead move roving focus; Escape closes and restores focus; Tab leaves and closes.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: none; submenus are separate menu instances owned by parent item handoff.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Enter/Space/Arrow opens; arrows/Home/End/typeahead move roving focus; Escape closes and restores focus; Tab leaves and closes.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Enter/Space/Arrow opens; arrows/Home/End/typeahead move roving focus; Escape closes and restores focus; Tab leaves and closes.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Each item is one command/toggle/link; no interactive descendants inside items.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: trigger click, Enter, Space, optional ArrowDown/ArrowUp, context menu, or opt-in hover.</li>\n<li>Closes on: item activation unless persistent, Escape, Tab/Shift+Tab leaving the menu, outside click, parent submenu handoff, or close-on-scroll when enabled.</li>\n<li>DOM focus while open: keyboard open moves focus to first/last item; pointer open keeps trigger focus until keyboard navigation or moves to first item only if configured explicitly.</li>\n<li>Next Tab behavior: Tab and Shift+Tab close the menu and move to the next/previous page focus target; they do not rove among menu items.</li>\n<li>Arrow-key entry behavior: ArrowDown/ArrowUp may open from trigger; arrows rove inside menu; ArrowRight/ArrowLeft open/close submenus.</li>\n<li>Outside click / pointerdown behavior: light-dismiss closes the surface without activating items.</li>\n<li>Escape behavior: closes current menu/submenu and returns focus to parent item or trigger.</li>\n<li>Focus restoration on close: return to trigger for root menu; return to parent item for submenu close.</li>\n<li>Behavior while enter / exit motion is running: closed state immediately removes menu from roving focus and accessibility tree.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not a form field; checkbox/radio menu state emits events but does not submit without app wiring.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not a form field; checkbox/radio menu state emits events but does not submit without app wiring.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Popover, roving focus, typeahead, submenu, hover-delay, and context-menu listeners clean up on close/disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: keyboard open moves focus into the menu (first/last item by key); pointer open keeps focus on the trigger until keyboard navigation unless <code>focus-on-open=&quot;first&quot;</code> is explicitly configured. <strong>Roving tabindex</strong> inside (one tab stop); <code>ArrowUp/Down</code> move items, <code>Home</code>/<code>End</code>, typeahead to match labels, <code>ArrowRight</code>/<code>ArrowLeft</code> open/close submenus, Esc closes and returns focus to the trigger. This is the documented <strong>menu exception</strong> where directional arrows move focus (behavior.md Focus + Composite sections).</li>\n<li><strong>Hit targets</strong>: each item is one action/toggle with its own target. A submenu parent is a single item that opens a submenu (separate composite) — not overloaded with a second action. Don't put focusable/clickable elements _inside_ a menu item (Fluent).</li>\n<li><strong>Native behavior</strong>: items are native buttons/links (Enter/Space/native link). Surface dismissal is native Popover light-dismiss/Esc.</li>\n<li><strong>Composite widgets</strong>: the menu <strong>owns</strong> roving focus, open state, and checkbox/radio checked state; items dispatch narrow internal events the menu aggregates. Submenus are separate <code>tyui-menu</code>s; the parent ignores the submenu's internal arrow events except the documented open/close handoff (behavior.md: ignore events from composites it doesn't own).</li>\n<li><strong>State &amp; events</strong>: <code>open</code>/<code>aria-expanded</code> reflected; checkbox/radio reflect <code>aria-checked</code>; user actions emit composed events; programmatic state is silent. By default the menu closes on item activation unless <code>persist-on-item-click</code>.</li>\n<li><strong>Disabled/readonly/loading</strong>: disabled items stay <strong>focusable</strong> (<code>aria-disabled</code>) to keep arrow/typeahead order stable (Fluent + behavior.md disabled-focusable). No readonly. Async items use <code>aria-busy</code> on the item.</li>\n<li><strong>Motion</strong>: surface fade/scale via tokens, reduced-motion safe; submenu open is fast. Hover-open uses <code>hover-delay</code>; during exit the surface is logically closed (behavior.md delayed-unmount).</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: anchored top-layer popover with alignment columns and internal scroll/auto-size.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p>Menu: <code>trigger</code>, <code>popover</code>/<code>list</code>. Item slots as above.</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-menu-background</code>, <code>--ty-menu-radius</code>, <code>--ty-menu-item-padding</code>, <code>--ty-menu-item-hover</code>, <code>--ty-menu-item-foreground</code>, <code>--ty-menu-min-inline-size</code>, <code>--ty-menu-icon-size</code>.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>popover</code>, <code>list</code>, <code>item</code>, <code>icon</code>, <code>checkmark</code>, <code>submenu-indicator</code>, <code>secondary-content</code>, <code>divider</code>, <code>group-header</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Trigger: <code>aria-haspopup=&quot;menu&quot;</code> + <code>aria-expanded</code> + <code>aria-controls</code> (menu id).</li>\n<li>Surface list: <code>role=&quot;menu&quot;</code> (or <code>menubar</code> only for a persistent bar).</li>\n<li>Items: <code>role=&quot;menuitem&quot;</code> / <code>menuitemcheckbox</code> (<code>aria-checked</code>) / <code>menuitemradio</code> (within a <code>role=&quot;group&quot;</code>). Link items are <code>&lt;a role=&quot;menuitem&quot;&gt;</code>.</li>\n<li>Submenu parent item: <code>aria-haspopup=&quot;menu&quot;</code> + <code>aria-expanded</code>; submenu is a separate nested <code>tyui-menu</code>.</li>\n<li><code>has-icons</code>/<code>has-checkmarks</code> reserve alignment columns (CSS), not roles.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Patterns: [APG Menu Button](https://www.w3.org/WAI/ARIA/apg/patterns/menu-button/) and [APG Menu and Menubar](https://www.w3.org/WAI/ARIA/apg/patterns/menubar/).</li>\n<p>  - Direct requirements:</p>\n<p>    - Trigger is a button with <code>aria-haspopup=&quot;menu&quot;</code>, <code>aria-expanded</code>, and optional <code>aria-controls</code>.</p>\n<p>    - Enter and Space on the trigger open the menu and place focus on the first menu item; optional Down/Up Arrow open to first/last item.</p>\n<p>    - Menu container has <code>role=&quot;menu&quot;</code>; items are <code>menuitem</code>, <code>menuitemcheckbox</code>, or <code>menuitemradio</code>.</p>\n<p>    - Tab and Shift+Tab leave the menu and close it; they do not move among menu items.</p>\n<p>    - Arrow keys move focus inside the menu; Home/End move to first/last when supported; printable characters may typeahead.</p>\n<p>    - Escape closes the menu and returns focus to the invoking button or parent item.</p>\n<p>    - Disabled menu items are focusable but cannot be activated; separators are not focusable.</p>\n<p>    - Submenu parent items expose <code>aria-haspopup</code> and <code>aria-expanded</code>; checked items expose <code>aria-checked</code>.</p>\n<li><strong>Fluent UI reference</strong>: Source components: Fluent UI React v9 <code>Menu</code> and <code>MenuList</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - <code>MenuTrigger</code> is the first child of <code>Menu</code>.</p>\n<p>    - <code>MenuList</code> is the only child of <code>MenuPopover</code>.</p>\n<p>    - Nested menus are separate components and limited to two levels.</p>\n<p>    - <code>hasIcons</code> and <code>hasCheckmarks</code> reserve alignment columns when only some items have icons/selectable state.</p>\n<p>    - <code>MenuItemLink</code> is used for navigation.</p>\n<p>    - <code>positioning.autoSize</code> is used when the menu can be clipped at high zoom or small viewports.</p>\n<p>    - Do not render focusable/clickable elements inside menu items.</p>\n<p>    - Do not mix checkbox and radio items without <code>MenuGroup</code>.</p>\n<p>    - <code>persistOnItemClick</code>, <code>openOnContext</code>, <code>openOnHover</code>, <code>closeOnScroll</code>, and <code>hoverDelay</code> are explicit behavior props.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><strong>Hover-open is the menu exception</strong>: behavior.md generally forbids hover from moving focus, but menus may open submenus on hover. Even so, hover-open must not _steal_ DOM focus until a key/click; define the next-keyboard-action. Keep <code>open-on-hover</code> opt-in.</li>\n<li><strong>No native menu element</strong> — the roving/typeahead/submenu logic is unavoidable JS; the focusgroup polyfill covers roving, but typeahead + submenu handoff remain a small controller. This is the only component where we accept a real ARIA composite.</li>\n<li><strong>≤2 nesting levels</strong> (Fluent); deeper menus are an anti-pattern.</li>\n<li><strong>id resolution across shadow DOM</strong>: <code>popovertarget</code>/<code>aria-controls</code> between a light-DOM trigger and a shadow surface won't resolve — own both in one root or use the JS open API (shared overlay convention).</li>\n<li><strong><code>MenuItemLink</code></strong> must remain a real <code>&lt;a href&gt;</code> so middle-click/open-in-new-tab work, while still carrying <code>role=&quot;menuitem&quot;</code>.</li>\n<li>Don't mix checkbox and radio items without a <code>MenuGroup</code>/<code>role=&quot;group&quot;</code> (Fluent) — keep semantics clean.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: anchored top-layer popover with alignment columns and internal scroll/auto-size remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: item icons/checkmarks have reserved columns and are decorative unless item text is absent.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: item icons/checkmarks have reserved columns and are decorative unless item text is absent.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/menu.md</code>): &quot;Use for action lists from a trigger button. Use <code>tyui-menu-item-link</code> for navigation items, checkbox/radio items for toggles (wrap in groups). ≤2 nesting levels. Don't put focusable elements inside items.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>peer views → tyui-tablist</code>, <code>data selection → tyui-select/combobox</code>, <code>non-action content → tyui-popover</code>, <code>2–3 always-visible commands → tyui-toolbar</code>.</li>\n<li><strong>Layout ownership</strong>: menu owns item layout, alignment columns, and surface min-inline-size; <strong>placement owned by anchor positioning</strong>; overflow scrolls inside (<code>autoSize</code> parity). layout.md: anchor + container scroll, not viewport math.</li>\n<li><strong>Token usage</strong>: colors/padding via <code>--ty-menu-*</code>; checked state shown by checkmark + <code>aria-checked</code>, never color alone.</li>\n<li><strong>Anti-patterns to reject</strong>: focusable/interactive elements inside a menu item; &gt;2 nesting; navigation menu items as <code>&lt;button&gt;</code>+JS (use link items); mixing checkbox/radio without a group; hover-open that steals focus; menu used for form data selection.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;action-menu&quot;,</p>\n<p>    &quot;composite&quot;: true,</p>\n<p>    &quot;nativeApi&quot;: &quot;popover + anchor-positioning&quot;,</p>\n<p>    &quot;rovingFocus&quot;: &quot;focusgroup-polyfilled&quot;,</p>\n<p>    &quot;topLayer&quot;: true,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;triggerHaspopupMenu&quot;: true,</p>\n<p>      &quot;menuExceptionArrowsMoveFocus&quot;: true,</p>\n<p>      &quot;disabledItemsFocusable&quot;: true,</p>\n<p>      &quot;linkItemsAreAnchors&quot;: true</p>\n<p>    },</p>\n<p>    &quot;alternatives&quot;: { &quot;tabs&quot;: &quot;tyui-tablist&quot;, &quot;dataSelect&quot;: &quot;tyui-select&quot; }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag interactive children inside menu items; flag <code>&lt;button&gt;</code>+JS navigation items; flag missing <code>aria-haspopup</code>/<code>aria-expanded</code> on trigger; flag &gt;2 nesting; flag checkbox/radio mix without group.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Trigger click/keyboard opens and focuses correctly</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Arrow/Home/End/typeahead navigation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled items focusable but inert</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Enter/Space activation closes unless persistent</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Checkbox/radio <code>checked-value-change</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Link item preserves anchor behavior</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Submenu ArrowRight/Left and hover delay</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Esc returns focus to opener</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr><tr><td><code>tyui-button</code> trigger end-to-end</td><td>Render <code>&lt;tyui-menu-trigger&gt;&lt;tyui-button&gt;More&lt;/tyui-button&gt;&lt;/tyui-menu-trigger&gt;</code> with several items.</td><td>Tab to the button, press Enter, Space, ArrowDown, Escape, and click.</td><td>Menu opens per APG, trigger <code>aria-haspopup/expanded</code> are exposed on the accessible button, roving focus enters items, and Escape restores focus to <code>tyui-button</code>.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Trigger opens per APG</td><td>Focus the menu trigger.</td><td>Press <code>Enter</code>, <code>Space</code>, <code>ArrowDown</code>, and <code>ArrowUp</code> in separate runs.</td><td>Menu opens, trigger <code>aria-expanded</code> becomes true, and focus lands on first or last item as specified</td></tr><tr><td>APG: Roving menu navigation</td><td>Open a menu with five items, one disabled item, and a separator.</td><td>Press arrows, Home, End, and a printable character.</td><td>Focus moves only among items, includes disabled item as focusable, skips separator, and typeahead matches labels</td></tr><tr><td>APG: Activation and close</td><td>Focus a normal item and press <code>Enter</code>; focus checkbox item and press <code>Space</code>.</td><td>Observe menu state and events.</td><td>Normal item activates and closes unless persistent; checkbox/radio updates <code>aria-checked</code> according to documented close policy</td></tr><tr><td>APG: Escape return</td><td>Open submenu from parent item.</td><td>Press <code>Escape</code> and <code>ArrowLeft</code>.</td><td>Submenu closes and focus returns to parent item or trigger according to APG handoff</td></tr><tr><td>Fluent UI: Composition shape</td><td>Render Menu with trigger not first or two lists in popover.</td><td>Run contract validation.</td><td>Validation fails and names the required MenuTrigger/MenuPopover/MenuList shape</td></tr><tr><td>Fluent UI: Link item preservation</td><td>Render a navigation menu item.</td><td>Inspect DOM and middle-click the item.</td><td>Item is a real <code>&lt;a href&gt;</code> with <code>role=&quot;menuitem&quot;</code>; native link behaviors are preserved</td></tr><tr><td>Fluent UI: Selectable item grouping</td><td>Render checkbox and radio menu items together without a group.</td><td>Run validation.</td><td>Validation fails until groups/names make state ownership explicit</td></tr><tr><td>Fluent UI: No interactive descendants</td><td>Put a button inside a <code>tyui-menu-item</code>.</td><td>Run validation and keyboard E2E.</td><td>Validation rejects it; keyboard focus never enters nested controls inside an item</td></tr><tr><td>Fluent UI: Clipping policy</td><td>Render a tall menu near viewport edge at high zoom.</td><td>Open the menu.</td><td>Menu uses documented auto-size/scroll behavior instead of overflowing off screen</td></tr><tr><td>Fluent UI: <code>tyui-button</code> trigger contract</td><td>Render <code>tyui-button</code> as <code>MenuTrigger</code>.</td><td>Open by click and keyboard, close by item activation and Escape.</td><td>Trigger adapter forwards activation/focus/ARIA without piercing private shadow DOM; disabled/loading <code>tyui-button</code> does not open the menu</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native where possible (Popover API surface, native button/link items); ARIA composite only for the menu pattern (justified exception).</td></tr><tr><td>behavior.md</td><td>Roving tabindex (focusgroup) + typeahead; Esc returns focus to trigger.</td></tr><tr><td>behavior.md</td><td>Menu exception: arrows move focus / open submenus, documented; hover-open opt-in and non-focus-stealing.</td></tr><tr><td>behavior.md</td><td>One item = one action; submenu parent not overloaded; no focusable children in items.</td></tr><tr><td>behavior.md</td><td>Composite owns roving + open + checked state; ignores submenu-owned events except handoff.</td></tr><tr><td>behavior.md</td><td>User actions → composed <code>activate</code>/<code>checked-value-change</code>/<code>open-change</code>; programmatic silent.</td></tr><tr><td>behavior.md</td><td>Disabled items focusable for stable order; loading via <code>aria-busy</code>.</td></tr><tr><td>behavior.md</td><td>Motion tokenized; logically closed during exit; reduced-motion safe.</td></tr></tbody></table>")}
  </article>`,
};
