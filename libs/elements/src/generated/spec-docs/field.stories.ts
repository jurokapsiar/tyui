import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Field',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Field</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-field</code></li>\n<li>Define: <code>@toyu-ui/define/field</code></li>\n<li>Status: draft</li>\n<li>Native substrate: composition of native <code>&lt;label&gt;</code> + the slotted control + native text nodes for hint/validation. Wiring is done with <strong>native <code>id</code>/<code>for</code>/<code>aria-describedby</code>/<code>aria-errormessage</code></strong>.</li>\n<li>Shadow DOM: <strong>no (light DOM)</strong> — Field's entire purpose is to associate a label/description with a control via id references. Those references <strong>cannot cross a shadow boundary</strong>, so Field must live in light DOM and arrange light-DOM children. This is the decisive case where shadow DOM is the wrong tool.</li>\n<li>Category: form layout / association</li>\n<li>Component family: form field wrapper</li>\n<li>Pattern type: native label/description/error wiring; alert only for announced validation</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Add a label, optional hint, and a validation message to a <strong>single</strong> form control.</p>\n<p>Do <strong>not</strong> put multiple controls in one Field (the label associates with one control). Do <strong>not</strong> use the Field label for Checkbox/Switch/Radio — those own their label; Field still supplies hint/validation.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: label, hint, validation message, and layout for one native form control.</li>\n<li>Do not use when: standalone text blocks, generic layout, or replacing the inner control semantics.</li>\n<li>Prefer instead: Label for standalone label, MessageBar for page-level status.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Clicking label focuses associated control; helper text is not interactive unless slotted as a separate link/control.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>orientation</code></td><td><code>vertical | horizontal</code></td><td>yes</td><td><code>vertical</code></td><td>label beside (33% col) vs above</td></tr><tr><td><code>size</code></td><td><code>small | medium | large</code></td><td>yes</td><td><code>medium</code></td><td>label size</td></tr><tr><td><code>required</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>adds <code>*</code> + <code>aria-required</code></td></tr><tr><td><code>validation-state</code></td><td><code>none | success | warning | error</code></td><td>yes</td><td><code>error</code> when message set, else <code>none</code></td><td>drives icon/color/alert</td></tr></tbody></table>\n<h3>Properties</h3>\n<p><code>label</code>, <code>hint</code>, <code>validationMessage</code>, <code>validationState</code> (also settable as slots). No methods beyond an internal <code>wire()</code> on slot change.</p>\n<h3>Events</h3>\n<p>None public (it's structural). It does not emit on validation change — the consumer owns validity (behavior.md: programmatic state emits nothing).</p>\n<h3>Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>label</code></td><td>label text (or use <code>label</code> attr)</td></tr><tr><td>default</td><td>the single control</td></tr><tr><td><code>hint</code></td><td>helper text</td></tr><tr><td><code>validation</code></td><td>validation message (icon auto by state)</td></tr></tbody></table>\n<h3>CSS parts</h3>\n<p><code>label</code>, <code>hint</code>, <code>validation</code>, <code>validation-icon</code>, <code>required-indicator</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-field-gap</code>, <code>--ty-field-label-size</code>, <code>--ty-field-label-col</code> (default <code>33%</code> horizontal), <code>--ty-field-validation-color-error/-warning/-success</code>.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: None public (it's structural). It does not emit on validation change — the consumer owns validity (behavior.md: programmatic state emits nothing).</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>label</code></td><td>label text (or use <code>label</code> attr)</td></tr><tr><td>default</td><td>the single control</td></tr><tr><td><code>hint</code></td><td>helper text</td></tr><tr><td><code>validation</code></td><td>validation message (icon auto by state)</td></tr></tbody></table>\n<h2>CSS Parts</h2>\n<p><code>label</code>, <code>hint</code>, <code>validation</code>, <code>validation-icon</code>, <code>required-indicator</code>.</p>\n<h2>Events</h2>\n<p>None public (it's structural). It does not emit on validation change — the consumer owns validity (behavior.md: programmatic state emits nothing).</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: None public (it's structural). It does not emit on validation change — the consumer owns validity (behavior.md: programmatic state emits nothing).</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: one label, one control slot, optional hint/error/validation message.</li>\n<li>Required parent: form or layout container; field owns relationships for its control.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n</ul>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>label</code></td><td>label text (or use <code>label</code> attr)</td></tr><tr><td>default</td><td>the single control</td></tr><tr><td><code>hint</code></td><td>helper text</td></tr><tr><td><code>validation</code></td><td>validation message (icon auto by state)</td></tr></tbody></table>\n<ul>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-field&gt;&lt;input&gt;&lt;input&gt;&lt;/tyui-field&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: form or layout container; field owns relationships for its control.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Keyboard belongs to the slotted native control; field does not intercept editing keys.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>No widget here — Field is structure + ARIA wiring. The lightest correct implementation is light-DOM composition, letting native <code>&lt;label for&gt;</code> and <code>aria-describedby</code> do the association with zero JS state.</li>\n<li>The only logic: generate ids, set <code>aria-describedby</code>/<code>aria-errormessage</code>/<code>aria-invalid</code>/<code>aria-required</code> on the slotted control, and toggle the validation row's <code>role</code>.</li>\n<li><code>orientation</code>, <code>size</code> = CSS grid/flex. Fluent fixes the horizontal label column at 33% — replicate with a layout token.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Keyboard belongs to the slotted native control; field does not intercept editing keys.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: form or layout container; field owns relationships for its control.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Keyboard belongs to the slotted native control; field does not intercept editing keys.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Keyboard belongs to the slotted native control; field does not intercept editing keys.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Clicking label focuses associated control; helper text is not interactive unless slotted as a separate link/control.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — Field is inline form structure.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: native tab order reaches the associated control, not helper/error text.</li>\n<li>Arrow-key entry behavior: belongs to the slotted control.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: belongs to the slotted control.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: validation/help motion must not move focus or alter form state.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Field itself is not submitted; it propagates name/required/disabled/invalid relationships to the control where applicable.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Field itself is not submitted; it propagates name/required/disabled/invalid relationships to the control where applicable.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Slotchange/ID relationship work updates label, description, and validation wiring and disconnects on removal.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: Field is not focusable and never moves focus. Clicking the label focuses the control (native <code>for</code>). Per behavior.md, focus only on explicit action.</li>\n<li><strong>Hit targets</strong>: Field wraps exactly one control = one primary target. Do not stuff extra interactive elements as the labelled child (a second control breaks the association).</li>\n<li><strong>Native behavior</strong>: relies on native label association and native control validation (<code>:invalid</code>, <code>validity</code>) — adds ARIA description/error wiring on top, no scripted focus side effects.</li>\n<li><strong>Composite widgets</strong>: Field is a _labelling container_, not a roving composite. For Checkbox/Radio/Switch it must <strong>not</strong> add its own label (behavior.md &quot;ignore events from nested composites it does not own&quot;); it only supplies hint/validation.</li>\n<li><strong>State &amp; events</strong>: validation-state is reflected; the alert row appears via <code>role=&quot;alert&quot;</code> so SR users hear error/warning. No events emitted.</li>\n<li><strong>Disabled/readonly/loading</strong>: when the control is disabled, the <strong>label stays non-disabled/readable</strong> (Fluent rule). Field forwards no disabledness; the control owns it.</li>\n<li><strong>Motion</strong>: validation message appearance may use a tokenized, reduced-motion-safe transition; motion must not delay the <code>alert</code> announcement.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: grid/flex label-control-message stack; orientation controls label placement.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>label</code></td><td>label text (or use <code>label</code> attr)</td></tr><tr><td>default</td><td>the single control</td></tr><tr><td><code>hint</code></td><td>helper text</td></tr><tr><td><code>validation</code></td><td>validation message (icon auto by state)</td></tr></tbody></table>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-field-gap</code>, <code>--ty-field-label-size</code>, <code>--ty-field-label-col</code> (default <code>33%</code> horizontal), <code>--ty-field-validation-color-error/-warning/-success</code>.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>label</code>, <code>hint</code>, <code>validation</code>, <code>validation-icon</code>, <code>required-indicator</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Label ↔ control: native <code>for</code>/<code>id</code> (control must be light-DOM or expose an id the field can target).</li>\n<li>Hint: <code>aria-describedby</code>.</li>\n<li>Validation message: <code>aria-errormessage</code> + <code>role=&quot;alert&quot;</code> for <code>error</code>/<code>warning</code> (announced); <code>success</code>/<code>none</code> use <code>aria-describedby</code>, no alert.</li>\n<li><code>required</code> → <code>aria-required=&quot;true&quot;</code> on the control + visual <code>*</code> on the label.</li>\n<li><code>error</code> → <code>aria-invalid=&quot;true&quot;</code> on the control.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: N/A for Field itself. APG has no Field pattern. Conditional validation messages map to [APG Alert](https://www.w3.org/WAI/ARIA/apg/patterns/alert/) only for error/warning announcements.</li>\n<p>  - Direct requirements:</p>\n<p>    - Field does not create a widget role or keyboard model.</p>\n<p>    - Label/control association uses native label and ARIA relationships in the same DOM scope.</p>\n<p>    - Error/warning validation messages use <code>role=&quot;alert&quot;</code> only when they are important updates and must not move keyboard focus.</p>\n<p>    - Validation alerts must be present or updated in a way that screen readers announce reliably; they must not auto-dismiss quickly.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Field</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Use Field to add label and validation message to form controls, and to label unlabeled controls like ProgressBar.</p>\n<p>    - Avoid including both <code>validationMessage</code> and <code>hint</code>.</p>\n<p>    - Do not add multiple controls as a child of one Field.</p>\n<p>    - Do not use Field's label with Checkbox; checkbox owns its label.</p>\n<p>    - Required appends an indicator and sets <code>aria-required</code> on the child.</p>\n<p>    - Horizontal orientation places the label beside the input while hint/validation remain below the field component, with a 33 percent label column.</p>\n<p>    - Disabled inner controls should not make the label disabled or unreadable.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><strong>Shadow boundary is fatal here</strong>: if the control is inside another component's shadow root, <code>for</code>/<code>aria-describedby</code> ids won't resolve. Contract: the labelled control must be light-DOM, or must expose a way for the field to set its accessible name/description (documented hook). This is why Field itself is light-DOM.</li>\n<li>Avoid both <code>hint</code> and <code>validationMessage</code> simultaneously (Fluent guidance) — when both exist, <code>aria-describedby</code> should list hint then error, and the spec should warn.</li>\n<li>Default <code>validation-state</code> becomes <code>error</code> automatically when a message is set — make that explicit so a neutral message isn't announced as an error.</li>\n<li>Checkbox-in-Field: label must come from the checkbox; Field's <code>label</code> slot is suppressed for those controls.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: grid/flex label-control-message stack; orientation controls label placement remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: icons in labels/messages need text equivalents if meaningful.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: icons in labels/messages need text equivalents if meaningful.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/field.md</code>): &quot;Wrap every standalone form control in <code>tyui-field</code> to supply label/hint/validation. One control per field. Use it (without its label) to add validation to a <code>tyui-progress-bar</code> or to a checkbox.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>checkbox/radio/switch label → control's own label</code>, <code>icon-only info → tyui-tooltip/InfoLabel</code>.</li>\n<li><strong>Layout ownership</strong>: Field owns label/hint/validation arrangement and (horizontal) the 33% label column; <strong>parent owns field width and stacking</strong>. layout.md: stacked horizontal fields align via the shared label-column token.</li>\n<li><strong>Token usage</strong>: spacing/validation colors via <code>--ty-field-*</code>; validation color must be paired with icon + text (never color-only).</li>\n<li><strong>Anti-patterns to reject</strong>: multiple controls in one Field; Field label on checkbox/switch/radio; both hint and validationMessage; disabled label over an enabled control; control nested in shadow DOM unreachable by <code>for</code>.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;form-field&quot;,</p>\n<p>    &quot;focusable&quot;: false,</p>\n<p>    &quot;wrapsControls&quot;: 1,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;associationMechanism&quot;: &quot;for/aria-describedby/aria-errormessage&quot;,</p>\n<p>      &quot;requiresLightDomControl&quot;: true</p>\n<p>    }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag Field with ≠1 labelled control; flag unresolved <code>for</code>/<code>aria-describedby</code>; flag color-only validation; flag Field label used on checkbox/radio/switch.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td><code>for</code> focuses the control</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Hint and validation ID token merging preserves consumer ARIA</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Error sets <code>aria-invalid</code>/<code>aria-errormessage</code> and <code>role=&quot;alert&quot;</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Success/warning semantics</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Control replacement cleanup</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Checkbox/radio label anti-pattern validation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Horizontal label column token and 400% zoom wrapping</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: No widget role</td><td>Render Field around one input.</td><td>Inspect accessibility tree.</td><td>Field does not expose a composite/widget role; the input owns focus and editing</td></tr><tr><td>APG: Native label association</td><td>Render label, hint, and input with generated IDs.</td><td>Click the label and inspect input attributes.</td><td>Click focuses the control; <code>aria-describedby</code> includes hint and preserves author tokens</td></tr><tr><td>APG: Error alert semantics</td><td>Set <code>validation-state=&quot;error&quot;</code> with a new validation message.</td><td>Update the message text.</td><td>Message has <code>role=&quot;alert&quot;</code>, input has <code>aria-invalid</code>, and focus remains on the input</td></tr><tr><td>APG: Non-error message is not alert</td><td>Set <code>validation-state=&quot;success&quot;</code> or <code>none</code>.</td><td>Inspect validation row.</td><td>Message is described text, not an alert, unless state is warning/error</td></tr><tr><td>Fluent UI: One-control rule</td><td>Render Field with two controls.</td><td>Run contract validation.</td><td>Validation fails and asks for separate fields or a future group component</td></tr><tr><td>Fluent UI: Hint plus validation warning</td><td>Render both hint and validation message.</td><td>Run validation.</td><td>Spec warns or fails according to severity; ARIA order is deterministic if both are allowed by author override</td></tr><tr><td>Fluent UI: Checkbox label exception</td><td>Render Field with label plus Checkbox child.</td><td>Run validation.</td><td>Field label is rejected/suppressed; checkbox's own label remains source of name</td></tr><tr><td>Fluent UI: Horizontal layout</td><td>Render horizontal Field at narrow width and 400 percent zoom.</td><td>Inspect layout.</td><td>Label column uses the documented token and content wraps without hiding hint/validation</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → native <code>for</code>/<code>aria-describedby</code>/<code>aria-errormessage</code>.</td></tr><tr><td>behavior.md</td><td>One target = one action (single control per field).</td></tr><tr><td>behavior.md</td><td>Focus only on explicit action (label click focuses control).</td></tr><tr><td>behavior.md</td><td>Composite rule: defers labelling to checkbox/radio/switch.</td></tr><tr><td>behavior.md</td><td>Error/warning announced via <code>role=alert</code>; no spurious events.</td></tr><tr><td>behavior.md</td><td>Disabled control keeps readable label.</td></tr><tr><td>behavior.md</td><td>Motion reduced-motion safe, never blocks announcement.</td></tr></tbody></table>")}
  </article>`,
};
