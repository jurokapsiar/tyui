import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Tooltip',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Tooltip</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-tooltip</code></li>\n<li>Define: <code>@toyu-ui/define/tooltip</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>HTML Popover API</strong> (<code>popover=&quot;manual&quot;</code>) surface positioned with <strong>CSS Anchor Positioning</strong>; the trigger is the wrapped interactive control. The relationship to the trigger is wired with native <code>aria-describedby</code>/<code>aria-labelledby</code>.</li>\n<li>Shadow DOM: minimal. Host renders a <code>&lt;div popover=&quot;manual&quot; part=&quot;content&quot; role=&quot;tooltip&quot;&gt;</code>; the trigger is the slotted child.</li>\n<li>Category: overlay (passive, non-interactive hint)</li>\n<li>Component family: popup description</li>\n<li>Pattern type: tooltip</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Show a short text hint about an <strong>interactive</strong> control on hover/focus.</p>\n<p>Do <strong>not</strong> put interactive content in a tooltip (→ <code>tyui-popover</code> with an expand button). Do <strong>not</strong> attach a tooltip to a static, non-focusable element (icons → <code>tyui-info-label</code>). Do <strong>not</strong> use it as the full-text alternative for truncated text.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: brief supplemental description for an existing trigger.</li>\n<li>Do not use when: interactive content, required form help, rich popovers, or hover-only controls.</li>\n<li>Prefer instead: Popover for interactive/rich content, Field hint for persistent help.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Hover may show/hide but must not move focus or activate anything.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-tooltip</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>content</code></td><td><code>string</code> (or <code>content</code> slot)</td><td>—</td><td>—</td><td><strong>required</strong> hint text</td></tr><tr><td><code>relationship</code></td><td><code>label | description | inaccessible</code></td><td>yes</td><td>—</td><td><strong>required</strong>; sets aria wiring</td></tr><tr><td><code>appearance</code></td><td><code>normal | inverted</code></td><td>yes</td><td><code>normal</code></td><td>styling</td></tr><tr><td><code>with-arrow</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>anchor-positioned arrow</td></tr><tr><td><code>positioning</code></td><td>property</td><td>—</td><td><code>above</code></td><td>→ CSS anchor area</td></tr><tr><td><code>show-delay</code></td><td>number</td><td>—</td><td><code>250</code></td><td>ms</td></tr><tr><td><code>hide-delay</code></td><td>number</td><td>—</td><td><code>250</code></td><td>ms</td></tr><tr><td><code>visible</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>controlled visibility</td></tr></tbody></table>\n<h3>Events</h3>\n<ul>\n<li><code>visible-change</code> — composed; <code>{ visible }</code> (+ optional keyboard event ref). Fires on hover/focus show &amp; hide. User/timing-initiated.</li>\n</ul>\n<h3>Slots</h3>\n<p><code>default</code> (trigger), <code>content</code> (rich tooltip text alternative to the attribute).</p>\n<h3>CSS parts</h3>\n<p><code>content</code>, <code>arrow</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-tooltip-background</code>, <code>--ty-tooltip-foreground</code>, <code>--ty-tooltip-radius</code>, <code>--ty-tooltip-padding</code>, <code>--ty-tooltip-max-inline-size</code>, <code>--ty-tooltip-arrow-size</code>. <code>inverted</code> remaps colors.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: - <code>visible-change</code> — composed; <code>{ visible }</code> (+ optional keyboard event ref). Fires on hover/focus show &amp; hide. User/timing-initiated.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p><code>default</code> (trigger), <code>content</code> (rich tooltip text alternative to the attribute).</p>\n<h2>CSS Parts</h2>\n<p><code>content</code>, <code>arrow</code>.</p>\n<h2>Events</h2>\n<ul>\n<li><code>visible-change</code> — composed; <code>{ visible }</code> (+ optional keyboard event ref). Fires on hover/focus show &amp; hide. User/timing-initiated.</li>\n</ul>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: - <code>visible-change</code> — composed; <code>{ visible }</code> (+ optional keyboard event ref). Fires on hover/focus show &amp; hide. User/timing-initiated.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: plain text/phrasing content only; no focusable descendants in the tooltip content. The owning trigger may be a native <code>&lt;button&gt;</code> or a <code>tyui-button</code> only when <code>tyui-button</code> exposes native-equivalent focus, accessible-name, disabled, and Enter/Space behavior on the host or provides an API for the tooltip to wire <code>aria-describedby</code>/<code>aria-labelledby</code> to the real focusable control.</li>\n<li>Required parent: owning trigger control.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  <code>default</code> (trigger), <code>content</code> (rich tooltip text alternative to the attribute).</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-tooltip&gt;&lt;button&gt;Click me&lt;/button&gt;&lt;/tyui-tooltip&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: owning trigger control.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Trigger focus shows tooltip after delay; Escape dismisses; tooltip itself never receives focus.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>The surface is a <code>popover=&quot;manual&quot;</code> element in the <strong>top layer</strong> with <strong>anchor positioning</strong> — no portal, no z-index, no JS positioner in the common case.</li>\n<li>The only JS is the <strong>show/hide timing</strong> (hover/focus in with <code>show-delay</code>, out with <code>hide-delay</code>) and dismissing on Esc — a tiny controller, because tooltips are inherently timing-driven and the platform has no declarative hover-tooltip primitive.</li>\n<li>Accessibility wiring (<code>aria-describedby</code> vs <code>aria-labelledby</code>) is set on the trigger via <code>relationship</code> — native attributes, no scripted AT hacks.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Trigger focus shows tooltip after delay; Escape dismisses; tooltip itself never receives focus.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: owning trigger control.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Trigger focus shows tooltip after delay; Escape dismisses; tooltip itself never receives focus.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Trigger focus shows tooltip after delay; Escape dismisses; tooltip itself never receives focus.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Hover may show/hide but must not move focus or activate anything.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: trigger focus or hover after delay; may also open programmatically for testing.</li>\n<li>Closes on: trigger blur, pointer leave after delay, Escape, or trigger removal.</li>\n<li>DOM focus while open: focus remains on the trigger; tooltip never receives focus.</li>\n<li>Next Tab behavior: Tab moves away from trigger and closes tooltip.</li>\n<li>Arrow-key entry behavior: N/A.</li>\n<li>Outside click / pointerdown behavior: pointer outside trigger/tooltip hover region closes when hover-open.</li>\n<li>Escape behavior: closes tooltip without moving focus from trigger.</li>\n<li>Focus restoration on close: N/A because focus never leaves trigger.</li>\n<li>Behavior while enter / exit motion is running: tooltip is inaccessible/undiscoverable once logically closed.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated; for required help prefer Field hint/description.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated; for required help prefer Field hint/description.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Trigger focus/blur/mouse/pointer, aria-describedby, and show/hide timers clean up on disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: the tooltip <strong>never takes focus</strong> (behavior.md: passive disclosure must not steal focus). It shows on trigger hover <strong>or</strong> trigger focus, and the surface itself is not a tab stop. Esc hides it while keeping focus on the trigger.</li>\n<li><strong>Hit targets</strong>: the tooltip adds no action; it only annotates the trigger's single action. Hover shows the hint (allowed preview), never selects/dismisses/navigates.</li>\n<li><strong>Native behavior</strong>: trigger is a native interactive control; tooltip layers description/label via native ARIA attributes.</li>\n<li><strong>State &amp; events</strong>: <code>visible</code> reflected; show/hide emits <code>visible-change</code>. Programmatic <code>visible</code> set should reflect without spurious duplicate events.</li>\n<li><strong>Disabled/readonly/loading</strong>: a disabled trigger generally won't receive hover/focus; for an icon button that must still explain itself when disabled, use <code>disabled-focusable</code> so focus (and thus the tooltip) still works. No readonly/loading.</li>\n<li><strong>Motion</strong>: fade only, fast, reduced-motion safe. Timing (<code>show-delay</code>/<code>hide-delay</code>) is behavior, not decoration — must remain even under reduced motion. During hide it is logically gone immediately (not interactive).</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: anchored lightweight surface with collision handling; does not affect document flow.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p><code>default</code> (trigger), <code>content</code> (rich tooltip text alternative to the attribute).</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-tooltip-background</code>, <code>--ty-tooltip-foreground</code>, <code>--ty-tooltip-radius</code>, <code>--ty-tooltip-padding</code>, <code>--ty-tooltip-max-inline-size</code>, <code>--ty-tooltip-arrow-size</code>. <code>inverted</code> remaps colors.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>content</code>, <code>arrow</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Surface: <code>role=&quot;tooltip&quot;</code>.</li>\n<li><code>relationship</code> (REQUIRED): <code>label</code> → trigger gets <code>aria-labelledby</code>/<code>aria-label</code> (use for icon-only buttons); <code>description</code> → trigger gets <code>aria-describedby</code>; <code>inaccessible</code> → no wiring (content must be available elsewhere).</li>\n<li>Trigger must be a genuinely focusable interactive control (button/link/field) — never a bare icon/<code>tabindex</code> hack (Fluent).</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: [APG Tooltip](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/). Note: APG marks this pattern as work in progress and without task force consensus; use only the stable requirements that are explicit in the page.</li>\n<p>  - Direct requirements:</p>\n<p>    - Tooltip appears when the trigger receives keyboard focus or mouse hover, typically after a small delay.</p>\n<p>    - Tooltip dismisses on Escape, mouse out, or trigger blur depending on how it was invoked.</p>\n<p>    - Focus stays on the triggering element while the tooltip is displayed.</p>\n<p>    - Tooltip does not receive focus; a hover surface with focusable elements is a non-modal dialog/popover instead.</p>\n<p>    - Tooltip container has <code>role=&quot;tooltip&quot;</code>.</p>\n<p>    - Trigger references the tooltip with <code>aria-describedby</code> for description behavior.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Tooltip</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Tooltips wrap interactive controls such as buttons, links, form fields, and gridcells.</p>\n<p>    - Static icons use InfoLabel; do not add <code>tabindex</code> to static content just to host a tooltip.</p>\n<p>    - Tooltip is not for interactive content; use expand/collapse button plus Popover if interaction is needed.</p>\n<p>    - Tooltip is not a full-text alternative for truncated content.</p>\n<p>    - <code>relationship</code> is required: <code>label</code>, <code>description</code>, or <code>inaccessible</code>.</p>\n<p>    - <code>label</code> sets trigger label for icon-only controls; <code>description</code> supplements a visible label; <code>inaccessible</code> sets no ARIA and is allowed only when text is available elsewhere.</p>\n<p>    - <code>showDelay</code> and <code>hideDelay</code> default to 250 ms; <code>visible</code>/<code>onVisibleChange</code> can control visibility.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><strong>Tooltip ≠ accessible name fallback for truncation</strong> — explicitly disallowed (Fluent). For truncated text use a real expand/title mechanism.</li>\n<li><strong>Static icons</strong>: do not attach a tooltip to a non-interactive icon or add <code>tabindex</code> to make it work → use <code>tyui-info-label</code> (a labelled button + popover). Encode this redirect.</li>\n<li><strong><code>relationship</code> is required and consequential</strong>: <code>label</code> overrides the trigger's accessible name (good for icon-only), <code>description</code> supplements it; choosing wrong harms SR users. Validate it is always set.</li>\n<li><strong>Top-layer + shadow</strong>: <code>aria-describedby</code>/<code>labelledby</code> must reference an id reachable from the trigger; if trigger is light-DOM and surface is in shadow, expose the id via the host or render the surface light-DOM. Document (shared overlay convention).</li>\n<li>Only one tooltip visible at a time (manual popover semantics help here).</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: anchored lightweight surface with collision handling; does not affect document flow remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: no interactive media; icons in trigger must still provide accessible name.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: no interactive media; icons in trigger must still provide accessible name.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/tooltip.md</code>): &quot;Attach only to focusable interactive controls. Use <code>relationship=label</code> for icon-only buttons, <code>description</code> for supplementary hints. Keep text short and non-interactive. For static icons use <code>tyui-info-label</code>.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>interactive content → tyui-popover</code>, <code>static icon info → tyui-info-label</code>, <code>truncated text → title/expand, not tooltip</code>.</li>\n<li><strong>Layout ownership</strong>: tooltip owns padding/arrow/max-inline-size; <strong>placement owned by anchor positioning</strong> relative to the trigger.</li>\n<li><strong>Token usage</strong>: colors via <code>--ty-tooltip-*</code>; <code>inverted</code> for higher contrast.</li>\n<li><strong>Anti-patterns to reject</strong>: tooltip on a static/non-focusable element; <code>tabindex</code> added to an icon just to host a tooltip; interactive content inside; missing <code>relationship</code>; tooltip as truncation fallback.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;hint&quot;,</p>\n<p>    &quot;focusable&quot;: false,</p>\n<p>    &quot;nativeApi&quot;: &quot;popover(manual) + anchor-positioning&quot;,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;relationshipRequired&quot;: true,</p>\n<p>      &quot;triggerMustBeInteractive&quot;: true,</p>\n<p>      &quot;neverStealsFocus&quot;: true</p>\n<p>    },</p>\n<p>    &quot;alternatives&quot;: { &quot;interactive&quot;: &quot;tyui-popover&quot;, &quot;iconInfo&quot;: &quot;tyui-info-label&quot; }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag tooltip without <code>relationship</code>; flag tooltip on non-interactive/<code>tabindex</code>-hacked trigger; flag interactive content inside; flag tooltip used for truncation.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td><code>relationship=label|description|inaccessible</code> ARIA wiring</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Existing trigger ARIA value preservation/merge</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Hover/focus show delays and hide delay with fake timers</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Esc hides without moving focus</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Tooltip never tabbable</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled-focusable trigger path</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Interactive content validation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Reduced-motion still preserves timing</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr><tr><td><code>tyui-button</code> trigger end-to-end</td><td>Render <code>tyui-tooltip content=&quot;Saves&quot; relationship=&quot;description&quot;&gt;&lt;tyui-button&gt;Save&lt;/tyui-button&gt;&lt;/tyui-tooltip&gt;</code>.</td><td>Tab to the button, wait show delay, hover, press Escape, then blur.</td><td>Tooltip relationship attaches to the accessible button, visible state changes without moving focus, Escape hides it, and disabled-focusable behavior is explicit.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Focus invocation</td><td>Focus an icon button with <code>relationship=&quot;label&quot;</code>.</td><td>Wait <code>show-delay</code>.</td><td>Tooltip becomes visible while DOM focus remains on the button</td></tr><tr><td>APG: Escape dismissal</td><td>Show tooltip from keyboard focus.</td><td>Press <code>Escape</code>.</td><td>Tooltip hides; focus remains on the trigger</td></tr><tr><td>APG: Hover invocation</td><td>Hover trigger, then move pointer to tooltip and out.</td><td>Observe timers.</td><td>Tooltip stays open while pointer is over trigger or tooltip and hides after configured delay</td></tr><tr><td>APG: Non-focusable surface</td><td>Render tooltip with a link in content.</td><td>Run validation and press Tab.</td><td>Validation rejects interactive content; tooltip surface is never tabbable</td></tr><tr><td>APG: ARIA relationship</td><td>Render <code>relationship=&quot;description&quot;</code>.</td><td>Inspect trigger attributes.</td><td>Trigger references tooltip content with <code>aria-describedby</code>/equivalent without clobbering existing descriptions</td></tr><tr><td>Fluent UI: Relationship required</td><td>Render Tooltip without <code>relationship</code>.</td><td>Run validation.</td><td>Validation fails and requires label, description, or inaccessible with rationale</td></tr><tr><td>Fluent UI: Static icon redirect</td><td>Render tooltip around a non-focusable icon or a <code>tabindex</code>-hacked span.</td><td>Run validation.</td><td>Validation rejects it and suggests InfoLabel</td></tr><tr><td>Fluent UI: Label relationship</td><td>Render icon-only button with <code>relationship=&quot;label&quot;</code>.</td><td>Inspect accessible name.</td><td>Button name comes from tooltip content without requiring tooltip focus</td></tr><tr><td>Fluent UI: Description relationship</td><td>Render visible-label button with <code>relationship=&quot;description&quot;</code>.</td><td>Inspect accessible description.</td><td>Tooltip supplements the existing name and preserves prior description tokens</td></tr><tr><td>Fluent UI: <code>tyui-button</code> relationship contract</td><td>Render <code>tyui-button</code> tooltip trigger with existing <code>aria-describedby</code>.</td><td>Show/hide by focus and hover.</td><td>Tooltip appends/preserves description tokens on the accessible button surface and never requires private shadow DOM access</td></tr><tr><td>Fluent UI: Truncation misuse</td><td>Generate tooltip solely to reveal clipped text.</td><td>Run design validation.</td><td>Validation rejects it and suggests real expand/title/content strategy</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Passive disclosure never steals focus; surface not a tab stop.</td></tr><tr><td>behavior.md</td><td>Shows on hover/focus; Esc hides, focus stays on trigger.</td></tr><tr><td>behavior.md</td><td>Annotates one trigger; hover never selects/dismisses.</td></tr><tr><td>behavior.md</td><td>Native interactive trigger + native aria-describedby/labelledby wiring.</td></tr><tr><td>behavior.md</td><td>Show/hide → composed <code>visible-change</code>; programmatic set non-duplicating.</td></tr><tr><td>behavior.md</td><td>disabled-focusable keeps hint reachable when needed.</td></tr><tr><td>behavior.md</td><td>Motion minimal; timing preserved under reduced motion.</td></tr></tbody></table>")}
  </article>`,
};
