import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Label',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Label</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-label</code></li>\n<li>Define: <code>@tyui/define/label</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>native <code>&lt;label&gt;</code></strong> (host renders/upgrades to or wraps a <code>&lt;label&gt;</code>)</li>\n<li>Shadow DOM: <strong>no</strong> — a label's whole job is the native <code>for</code>/implicit-wrapping association, which a shadow boundary breaks. Render light DOM so <code>&lt;label for&gt;</code> reaches the control in the same tree.</li>\n<li>Category: form (supporting)</li>\n<li>Component family: form text</li>\n<li>Pattern type: native label</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Provide an accessible name/title for a single form control.</p>\n<p>Do <strong>not</strong> use a standalone Label for Checkbox/Switch/Radio — those own their inline label; use the control's own label and let <code>tyui-field</code> supply only validation/hint. Do not use Label as generic bold text.</p>\n<p>Alternatives: <code>tyui-field</code> (label + hint + validation wrapper), control-owned labels.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: caption one form control and expose required/disabled/size styling.</li>\n<li>Do not use when: generic text, headings, links, or clickable commands.</li>\n<li>Prefer instead: Text for generic copy, Field for complete form field layout.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Click/tap delegates to associated control; required marker is not a separate target.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>for</code></td><td><code>string</code></td><td>yes</td><td>—</td><td>forwarded to native <code>&lt;label for&gt;</code></td></tr><tr><td><code>required</code></td><td><code>boolean | string</code></td><td>yes (bool)</td><td><code>false</code></td><td><code>true</code> → <code>*</code>; string → custom indicator</td></tr><tr><td><code>size</code></td><td><code>small | medium | large</code></td><td>yes</td><td><code>medium</code></td><td></td></tr><tr><td><code>weight</code></td><td><code>regular | semibold</code></td><td>yes</td><td><code>regular</code></td><td></td></tr><tr><td><code>disabled</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td><strong>visual only</strong></td></tr></tbody></table>\n<h3>Properties</h3>\n<p>Mirror attributes; <code>htmlFor</code> alias for <code>for</code>. No methods.</p>\n<h3>Events</h3>\n<p>None.</p>\n<h3>Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>label text</td></tr></tbody></table>\n<h3>CSS parts</h3>\n<p><code>required</code> (the indicator) — exposed so products can restyle the asterisk.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-label-color</code>, <code>--ty-label-font-size</code>, <code>--ty-label-font-weight</code>, <code>--ty-label-required-color</code>. Default to semantic tokens.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: None.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>label text</td></tr></tbody></table>\n<h2>CSS Parts</h2>\n<p><code>required</code> (the indicator) — exposed so products can restyle the asterisk.</p>\n<h2>Events</h2>\n<p>None.</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: None.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: text and optional required marker only; no nested interactive content.</li>\n<li>Required parent: associated form control or Field.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n</ul>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>label text</td></tr></tbody></table>\n<ul>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-label onclick=&quot;save()&quot;&gt;Save&lt;/tyui-label&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: associated form control or Field.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Native label click/focus behavior only; label is not tabbable.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Render an actual <code>&lt;label&gt;</code> element. <code>for</code>/<code>htmlFor</code> and click-to-focus/activate are <strong>native</strong> — no JS.</li>\n<li><code>required</code> asterisk and <code>size</code>/<code>weight</code> are pure CSS + a tiny presentational <code>&lt;span aria-hidden&gt;</code> for the indicator.</li>\n<li>No shadow DOM = no label-crossing-boundary problem. This is deliberately the cheapest correct implementation.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Native label click/focus behavior only; label is not tabbable.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: associated form control or Field.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Native label click/focus behavior only; label is not tabbable.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Native label click/focus behavior only; label is not tabbable.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Click/tap delegates to associated control; required marker is not a separate target.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — Label is inline form text.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: native tab order reaches the associated control, not the label.</li>\n<li>Arrow-key entry behavior: N/A.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: required/disabled styling motion must not alter association semantics.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Label associates with the control via for/control relationship; it submits no value.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Label associates with the control via for/control relationship; it submits no value.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Control association and generated ID bookkeeping update on slot/control changes and disconnect cleanly.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: a label is not a focus stop. Clicking it focuses/activates the associated control — <strong>native</strong> behavior, preserved (behavior.md &quot;use native HTML semantics before custom roles&quot;).</li>\n<li><strong>Hit targets</strong>: the label forwards activation to exactly one control; it adds no second action.</li>\n<li><strong>Native behavior</strong>: native <code>&lt;label&gt;</code> editing/association/click semantics kept intact; no scripted focus side effects.</li>\n<li><strong>State &amp; events</strong>: no events. <code>disabled</code> is a reflected visual attribute; it must <strong>not</strong> disable the control (Fluent best practice: disabled control keeps a readable, non-disabled label).</li>\n<li><strong>Disabled/readonly/loading</strong>: label has no interactive state; documented that disabledness belongs to the control.</li>\n<li><strong>Motion</strong>: none.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: inline/block label text with optional required marker.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>label text</td></tr></tbody></table>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-label-color</code>, <code>--ty-label-font-size</code>, <code>--ty-label-font-weight</code>, <code>--ty-label-required-color</code>. Default to semantic tokens.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>required</code> (the indicator) — exposed so products can restyle the asterisk.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Implicit <code>label</code> semantics from native <code>&lt;label&gt;</code>.</li>\n<li>Required indicator is <code>aria-hidden</code>; requiredness is conveyed on the <strong>control</strong> (<code>aria-required</code>/<code>required</code>), set by <code>tyui-field</code>, not by the asterisk.</li>\n<li><code>disabled</code> styling does not change semantics — disabled state lives on the control.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: N/A. APG has no Label pattern; label behavior is native HTML label behavior and must not be replaced by an ARIA widget.</li>\n<p>  - Direct requirements:</p>\n<p>    - Label itself is not a focus stop.</p>\n<p>    - Label click forwards focus/activation to exactly one associated labelable control.</p>\n<p>    - <code>required</code> indicator is presentational; required state lives on the control.</p>\n<p>    - Label relationships do not cross shadow boundaries unless a containing component documents ARIA mirroring.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Label</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Label renders as native <code>label</code>.</p>\n<p>    - <code>disabled</code> is visual and should be used sparingly because it does not meet the required contrast ratio.</p>\n<p>    - <code>required</code> can be boolean or custom content.</p>\n<p>    - <code>size</code> supports small, medium, large.</p>\n<p>    - <code>weight</code> supports regular and semibold.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li>Disabled label fails contrast by design — spec says use sparingly and only when the control is clearly non-interactive (mirrors Fluent guidance).</li>\n<li>If the associated control lives inside another component's shadow DOM, native <code>for</code> cannot cross into it. Contract: either the control is light-DOM, or the wrapping component (<code>tyui-field</code>) sets the name via <code>aria-labelledby</code>/<code>aria-label</code> plumbing it documents.</li>\n<li><code>required</code> is presentation only; never let the asterisk be the sole signal — <code>tyui-field</code> must also set <code>aria-required</code> on the control.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: inline/block label text with optional required marker remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: required marker has accessible text or is paired with aria-required on control.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: required marker has accessible text or is paired with aria-required on control.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/label.md</code>): &quot;Prefer <code>tyui-field</code> which composes the label; use bare <code>tyui-label</code> only for a custom layout. Never label a checkbox/switch/radio with it.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>full form row → tyui-field</code>, <code>checkbox/radio/switch → control's own label</code>.</li>\n<li><strong>Layout ownership</strong>: in horizontal orientation the _parent/field_ owns the label column width (Fluent fixes 33%); the label owns only its text wrapping. Encode <code>parentOwnsStretching: true</code>.</li>\n<li><strong>Token usage</strong>: theme via <code>--ty-label-*</code>; reject literal colors/sizes.</li>\n<li><strong>Anti-patterns to reject</strong>: standalone label on checkbox/switch/radio; using Label as a heading; marking the label disabled while its control is enabled; asterisk-only requiredness.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;control-name&quot;,</p>\n<p>    &quot;focusable&quot;: false,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;associatesWith&quot;: &quot;single control&quot;,</p>\n<p>      &quot;requiredIndicatorIsPresentational&quot;: true</p>\n<p>    }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: every <code>tyui-label[for]</code> must resolve to exactly one control id in the generated tree; flag dangling <code>for</code>; flag <code>required</code> without matching <code>aria-required</code> on the control.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Click label focuses matching light-DOM control</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Dangling <code>for</code> validation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Required indicator is hidden from AT but paired with control <code>aria-required</code> when Field coordinates</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled label is visual only</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>No public events</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Forced-colors required indicator visible beyond color alone</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Native click association</td><td>Render <code>tyui-label for=&quot;email&quot;</code> and <code>&lt;input id=&quot;email&quot;&gt;</code>.</td><td>Click the label text.</td><td>Focus moves to the input using native label behavior</td></tr><tr><td>APG: No focusable label</td><td>Render a label with text only.</td><td>Press <code>Tab</code>.</td><td>Label is skipped and has no widget role</td></tr><tr><td>APG: Single-control association</td><td>Render one label pointing to a missing or duplicate ID.</td><td>Run validation.</td><td>Dangling or ambiguous <code>for</code> fails</td></tr><tr><td>APG: Required indicator is presentational</td><td>Render required label coordinated by Field.</td><td>Inspect accessibility tree.</td><td>Indicator is hidden from AT; the control has <code>required</code>/<code>aria-required</code></td></tr><tr><td>Fluent UI: Disabled is visual only</td><td>Render a disabled label for an enabled control.</td><td>Inspect state and contrast.</td><td>Validation warns/fails according to policy; control is not silently disabled by the label</td></tr><tr><td>Fluent UI: Custom required indicator</td><td>Render <code>required=&quot;***&quot;</code>.</td><td>Inspect markup.</td><td>Custom indicator displays but remains presentational; control required state is separately set</td></tr><tr><td>Fluent UI: Size and weight tokens</td><td>Render all sizes and weights.</td><td>Inspect generated CSS.</td><td>Styling uses <code>--ty-label-*</code> tokens and does not alter semantics</td></tr><tr><td>Fluent UI: Not generic text</td><td>Generate a heading-like label without an associated control.</td><td>Run design validation.</td><td>Validation rejects Label as generic bold text and suggests heading/text component</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → real <code>&lt;label&gt;</code>, native association/click.</td></tr><tr><td>behavior.md</td><td>One target = one action → forwards to a single control only.</td></tr><tr><td>behavior.md</td><td>Focus only on explicit action → click focuses control (native).</td></tr><tr><td>behavior.md</td><td>No undocumented events → emits none.</td></tr><tr><td>behavior.md</td><td>Disabled is visual, control owns interactive state.</td></tr><tr><td>behavior.md</td><td>No motion.</td></tr></tbody></table>",
    )}
  </article>`,
};
