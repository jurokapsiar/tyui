import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Card',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Card</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tags: <code>tyui-card</code> (+ <code>tyui-card-header</code>, <code>tyui-card-footer</code>, <code>tyui-card-preview</code> as light structural slots/elements)</li>\n<li>Define: <code>@tyui/define/card</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <code>&lt;article&gt;</code> (or <code>&lt;div role=&quot;group&quot;&gt;</code>); selectable cards use a <strong>native <code>&lt;input type=&quot;checkbox&quot;&gt;</code></strong> in the floating-action slot; a primary-action card uses a native <code>&lt;a&gt;</code>/<code>&lt;button&gt;</code> overlay.</li>\n<li>Shadow DOM: minimal (root renders the appearance container + <code>&lt;slot&gt;</code>s for preview/header/body/footer). Content stays light-DOM for headings/links.</li>\n<li>Category: container (optionally selectable / interactive — composite focus modes)</li>\n<li>Component family: composition surface</li>\n<li>Pattern type: non-interactive group by default; conditional button/checkbox only when opted in</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Group information and actions about a single concept (a document, a contact).</p>\n<p>Do <strong>not</strong> turn an entire card into one giant button when it contains its own links/buttons — that violates hit-target separation. Use a clear primary action or a selection checkbox instead.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: group related content and optional separate actions in a bounded surface.</li>\n<li>Do not use when: overload the whole card with navigation, selection, expand, and secondary actions at once.</li>\n<li>Prefer instead: Button/Link for a single action, Checkbox/Listbox for selection, Dialog/Popover for disclosure.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: One primary target per mode; secondary actions must be separate focusable controls.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-card</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>appearance</code></td><td><code>filled | filled-alternative | outline | subtle</code></td><td>yes</td><td><code>filled</code></td><td>token remap</td></tr><tr><td><code>orientation</code></td><td><code>vertical | horizontal</code></td><td>yes</td><td><code>vertical</code></td><td></td></tr><tr><td><code>size</code></td><td><code>small | medium | large</code></td><td>yes</td><td><code>medium</code></td><td>radius + inner gap</td></tr><tr><td><code>focus-mode</code></td><td><code>off | no-tab | tab-exit | tab-only</code></td><td>yes</td><td><code>off</code></td><td>composite focus (focusgroup + trap)</td></tr><tr><td><code>selected</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>controlled selection (mirrors checkbox)</td></tr><tr><td><code>default-selected</code></td><td><code>boolean</code></td><td>no</td><td><code>false</code></td><td>uncontrolled initial</td></tr><tr><td><code>disabled</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>disables card selection (not children)</td></tr></tbody></table>\n<h3>Events</h3>\n<ul>\n<li><code>selection-change</code> — composed; emitted on user selection toggle, payload <code>{ selected }</code>. Programmatic <code>selected</code> set emits nothing.</li>\n</ul>\n<h3>Slots</h3>\n<p><code>floating-action</code>, <code>preview</code>, <code>header</code>, default (body), <code>footer</code>. Header sub-slots: <code>image</code>, <code>header</code> (title), <code>description</code>, <code>action</code>.</p>\n<h3>CSS parts</h3>\n<p><code>root</code>, <code>preview</code>, <code>header</code>, <code>footer</code>, <code>checkbox</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-card-background</code>, <code>--ty-card-border-color</code>, <code>--ty-card-radius</code>, <code>--ty-card-padding</code>, <code>--ty-card-gap</code>, <code>--ty-card-shadow</code>. Appearances remap.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: - <code>selection-change</code> — composed; emitted on user selection toggle, payload <code>{ selected }</code>. Programmatic <code>selected</code> set emits nothing.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p><code>floating-action</code>, <code>preview</code>, <code>header</code>, default (body), <code>footer</code>. Header sub-slots: <code>image</code>, <code>header</code> (title), <code>description</code>, <code>action</code>.</p>\n<h2>CSS Parts</h2>\n<p><code>root</code>, <code>preview</code>, <code>header</code>, <code>footer</code>, <code>checkbox</code>.</p>\n<h2>Events</h2>\n<ul>\n<li><code>selection-change</code> — composed; emitted on user selection toggle, payload <code>{ selected }</code>. Programmatic <code>selected</code> set emits nothing.</li>\n</ul>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: - <code>selection-change</code> — composed; emitted on user selection toggle, payload <code>{ selected }</code>. Programmatic <code>selected</code> set emits nothing.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: header/media/body/footer/actions slots; interactive children allowed only as separate targets.</li>\n<li>Required parent: none; a list/grid parent may own card selection.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  <code>floating-action</code>, <code>preview</code>, <code>header</code>, default (body), <code>footer</code>. Header sub-slots: <code>image</code>, <code>header</code> (title), <code>description</code>, <code>action</code>.</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-card onclick=&quot;open()&quot;&gt;&lt;button&gt;Delete&lt;/button&gt;&lt;a href=&quot;/details&quot;&gt;Details&lt;/a&gt;&lt;/tyui-card&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: none; a list/grid parent may own card selection.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Default card has no keyboard model; actionable/selectable modes use native button/checkbox behavior.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Default card = a styled <code>&lt;article&gt;</code> container. <strong>No JS</strong> for the common, non-interactive case.</li>\n<li>Selection = a real <code>&lt;input type=checkbox&gt;</code> (native checked state, label, keyboard) placed in the <code>floating-action</code>/<code>checkbox</code> slot; the card reflects <code>selected</code> from it. No scripted selection model for single cards.</li>\n<li>Interactive focus modes (Fluent <code>no-tab</code>/<code>tab-exit</code>/<code>tab-only</code>) are a roving/focus-trap composite → use the <strong>focusgroup polyfill</strong> + a small focus-trap helper; treat as opt-in, default <code>focus-mode=&quot;off&quot;</code>.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Default card has no keyboard model; actionable/selectable modes use native button/checkbox behavior.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: none; a list/grid parent may own card selection.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Default card has no keyboard model; actionable/selectable modes use native button/checkbox behavior.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Default card has no keyboard model; actionable/selectable modes use native button/checkbox behavior.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: One primary target per mode; secondary actions must be separate focusable controls.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A for default cards. If a card action opens a popup, that popup component owns the overlay contract.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: follows native order among separate controls inside the card.</li>\n<li>Arrow-key entry behavior: N/A unless an owning list/grid pattern defines it.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: card visual transitions never change selection/action semantics.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Selectable mode may submit via an owning form-control pattern only when explicitly implemented.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Selectable mode may submit via an owning form-control pattern only when explicitly implemented.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: No external listeners; slotchange validation for illegal interactive descendants must disconnect on removal.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: default card is not focusable. <code>focus-mode</code> variants create a focus scope: <code>no-tab</code> traps with Enter, releases on Escape; <code>tab-exit</code> releases on Tab past the last child; <code>tab-only</code> rovers through children. Implemented via focusgroup + trap helper. Focus enters only on explicit Enter/Tab. Visible ring distinct from <code>selected</code>.</li>\n<li><strong>Hit targets</strong>: <strong>the key rule</strong> — a card with interior links/buttons must NOT also be one big button. Choose one model: (a) non-interactive container, (b) a single primary-action overlay link/button (with inner actions as separate targets that stop propagation), or (c) selection via the checkbox. behavior.md: do not combine navigation + selection + child actions on one target.</li>\n<li><strong>Native behavior</strong>: selection is a native checkbox; actions are native buttons/links; the card adds grouping/focus scope only.</li>\n<li><strong>Composite widgets</strong>: in focus modes the card owns the focus scope/roving; child actions remain independent targets. It ignores events from nested composites (e.g. a menu in the header <code>action</code> slot).</li>\n<li><strong>State &amp; events</strong>: <code>selected</code> reflected for styling; user toggle emits <code>selection-change</code>. Hover is a visual state only (no selection on hover).</li>\n<li><strong>Disabled/readonly/loading</strong>: <code>disabled</code> disables card-level selection but <strong>not</strong> child controls (Fluent). No readonly. Loading content uses skeletons inside, not a card-level busy.</li>\n<li><strong>Motion</strong>: hover/selected elevation transitions tokenized, reduced-motion safe.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: block/card surface; component owns padding/radius/elevation, parent owns grid/list placement.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p><code>floating-action</code>, <code>preview</code>, <code>header</code>, default (body), <code>footer</code>. Header sub-slots: <code>image</code>, <code>header</code> (title), <code>description</code>, <code>action</code>.</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-card-background</code>, <code>--ty-card-border-color</code>, <code>--ty-card-radius</code>, <code>--ty-card-padding</code>, <code>--ty-card-gap</code>, <code>--ty-card-shadow</code>. Appearances remap.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>root</code>, <code>preview</code>, <code>header</code>, <code>footer</code>, <code>checkbox</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Default: <code>role=&quot;group&quot;</code> (Fluent default). For a titled card, wrap the title in a real heading (<code>&lt;h2&gt;</code>–<code>&lt;h4&gt;</code>) chosen by context.</li>\n<li>Focusable card (<code>focus-mode</code> ≠ off): provide <code>aria-label</code>/<code>aria-labelledby</code> + <code>aria-describedby</code> covering the key inner text (Fluent requirement), since the group becomes a focus target.</li>\n<li>Selectable: the checkbox carries <code>checked</code>; the card reflects <code>selected</code> (host attribute) for styling, not as the a11y source of truth.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: N/A for the default container. APG has no Card pattern. Conditional mappings apply only when Card opts into a single interaction model: [APG Button](https://www.w3.org/WAI/ARIA/apg/patterns/button/) for a single primary action and [APG Checkbox](https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/) for selectable cards.</li>\n<p>  - Direct requirements:</p>\n<p>    - A non-interactive card is a labelled group/container and has no APG widget keyboard model.</p>\n<p>    - A primary-action card preserves native button/link activation and never also exposes independent inner controls on the same target.</p>\n<p>    - A selectable card uses a checkbox with an accessible label and <code>checked</code> state as the source of truth.</p>\n<p>    - Card implementations must not combine navigation, selection, and child actions into one overloaded target.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Card</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Default card role is <code>group</code>.</p>\n<p>    - Focusable cards need meaningful <code>aria-label</code> or <code>aria-labelledby</code> plus <code>aria-describedby</code> covering relevant internal text.</p>\n<p>    - Larger cards with one title use a heading whose level is chosen by page context.</p>\n<p>    - <code>focusMode</code> supports <code>off</code>, <code>no-tab</code>, <code>tab-exit</code>, and <code>tab-only</code>.</p>\n<p>    - <code>disabled</code> disables card-level interaction and selection but does not automatically disable slotted child controls.</p>\n<p>    - <code>selected</code>, <code>defaultSelected</code>, and <code>onSelectionChange</code> apply only to selectable cards.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li>&quot;Clickable card with inner buttons&quot; is the classic accessibility trap. Document the three allowed patterns and forbid wrapping the whole card in an anchor when it has interactive children.</li>\n<li>Selection truth lives on the checkbox; <code>selected</code> host attribute is derived — keep them in sync but don't double-fire events.</li>\n<li>Title must be a real heading for document outline; the level is context-dependent (can't hardcode).</li>\n<li>Focus modes are powerful but heavy; default off and only enable when the whole card is a single navigable unit with no competing inner targets (which then conflicts with inner buttons — pick one).</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: block/card surface; component owns padding/radius/elevation, parent owns grid/list placement remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: media slot supports image/icon content with author-provided alternatives.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: media slot supports image/icon content with author-provided alternatives.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/card.md</code>): &quot;Use as a content container. If the card is a single navigable unit, use one primary action; if it has multiple inner actions, keep the card non-interactive (or selectable via checkbox). Never wrap a multi-action card in one button/anchor.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>single nav target → primary-action card / tyui-link</code>, <code>pick from many → selectable cards (checkbox)</code>, <code>list rows → tyui-table</code>.</li>\n<li><strong>Layout ownership</strong>: card owns inner padding/gap and preview/header/footer arrangement; <strong>parent owns grid placement and width</strong> (layout.md intrinsic grid <code>auto-fit minmax</code>). Card height is <code>fit-content</code>, never fixed.</li>\n<li><strong>Token usage</strong>: appearance/elevation via <code>--ty-card-*</code>; <code>filled-alternative</code> for light surfaces (contrast).</li>\n<li><strong>Anti-patterns to reject</strong>: whole-card anchor/button wrapping interactive children; selection on hover; fixed card height; title not a heading; <code>disabled</code> cascading to children.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;container&quot;,</p>\n<p>    &quot;focusable&quot;: &quot;only with focus-mode&quot;,</p>\n<p>    &quot;interactionModels&quot;: [&quot;non-interactive&quot;, &quot;single-primary-action&quot;, &quot;selectable-checkbox&quot;],</p>\n<p>    &quot;accessibility&quot;: { &quot;role&quot;: &quot;group&quot;, &quot;focusableNeedsLabel&quot;: true, &quot;titleIsHeading&quot;: true }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag a card wrapped in a single button/anchor that also contains buttons/links; flag hover-selection; flag focusable card without label; flag fixed heights.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Invalid overloaded-card examples</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Title heading/accessible name</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Selectable card checkbox toggles by pointer and keyboard and emits composed <code>selection-change</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Programmatic <code>selected</code> is silent</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Focus-mode keyboard entry/exit</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Fixed-height and hover-selection rejected by design validation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Forced-colors focus/selection visible without color alone</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Default card is not a widget</td><td>Render a card with text and a heading only.</td><td>Press <code>Tab</code> through the page.</td><td>Card host is skipped unless explicitly focusable; no button, checkbox, or grid role is invented</td></tr><tr><td>APG: Primary action follows Button/Link semantics</td><td>Render a card with exactly one primary overlay button or link.</td><td>Press <code>Enter</code> and <code>Space</code> when focused.</td><td>Button action uses Enter/Space; link action preserves native link behavior; no inner competing controls are present</td></tr><tr><td>APG: Selectable card follows Checkbox semantics</td><td>Render a selectable card with a checkbox target.</td><td>Focus checkbox and press <code>Space</code>.</td><td>Checkbox toggles, exposes checked state, and card reflects <code>selected</code> for styling only</td></tr><tr><td>APG: Overloaded card is invalid</td><td>Render a whole-card click handler plus inner buttons and a checkbox.</td><td>Run design validation.</td><td>Validation rejects the card and requires one interaction model</td></tr><tr><td>Fluent UI: Focusable card labelling</td><td>Render <code>focus-mode=&quot;tab-only&quot;</code> without a label.</td><td>Run accessibility validation.</td><td>Validation fails until the focusable card has name/description that summarize relevant content</td></tr><tr><td>Fluent UI: Focus mode entry/exit</td><td>Render cards for <code>no-tab</code>, <code>tab-exit</code>, and <code>tab-only</code>.</td><td>Use trusted <code>Tab</code>, <code>Enter</code>, <code>Escape</code>, and <code>Shift+Tab</code>.</td><td>Focus behavior matches the selected mode and observable focus never gets trapped unexpectedly</td></tr><tr><td>Fluent UI: Disabled card scope</td><td>Render disabled interactive and selectable cards with slotted child buttons.</td><td>Click host, checkbox, and child buttons.</td><td>Host/selection interaction is suppressed; child controls are not assumed disabled unless explicitly disabled</td></tr><tr><td>Fluent UI: Heading requirement</td><td>Render a large card with one title.</td><td>Inspect markup.</td><td>Title is a real heading at an author-chosen valid level, not just styled text</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>One target = one action → enforced via 3 allowed interaction models, no overloaded card.</td></tr><tr><td>behavior.md</td><td>Native checkbox for selection; native buttons/links for actions.</td></tr><tr><td>behavior.md</td><td>Focus enters only on explicit Enter/Tab; focus modes via focusgroup + trap.</td></tr><tr><td>behavior.md</td><td>Composite root owns focus scope; ignores foreign menu events.</td></tr><tr><td>behavior.md</td><td>User selection → composed <code>selection-change</code>; programmatic silent; hover is visual only.</td></tr><tr><td>behavior.md</td><td><code>disabled</code> doesn't cascade to children.</td></tr><tr><td>behavior.md</td><td>Motion tokenized, reduced-motion safe.</td></tr></tbody></table>",
    )}
  </article>`,
};
