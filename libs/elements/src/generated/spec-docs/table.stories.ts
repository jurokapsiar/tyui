import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Table',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Table</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tags: <code>tyui-table</code>, <code>tyui-table-header</code>, <code>tyui-table-header-cell</code>, <code>tyui-table-body</code>, <code>tyui-table-row</code>, <code>tyui-table-cell</code>, <code>tyui-table-selection-cell</code>, <code>tyui-table-cell-layout</code></li>\n<li>Define: <code>@toyu-ui/define/table</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>native <code>&lt;table&gt; &lt;thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;th&gt; &lt;td&gt;</code></strong> (Fluent's &quot;primitive, low-level&quot; table). Sort control = native <code>&lt;button&gt;</code> inside <code>&lt;th&gt;</code>; selection = native <code>&lt;input type=checkbox/radio&gt;</code>.</li>\n<li>Shadow DOM: minimal/none. Prefer <strong>light DOM</strong> so native table semantics and the <code>grid</code> keyboard pattern work without re-mapping roles. Styling via tokens + parts on the host.</li>\n<li>Category: data display (becomes an interactive <code>grid</code> only when keyboard nav/selection is added)</li>\n<li>Component family: data display/composite when interactive</li>\n<li>Pattern type: table by default; grid when interactive</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Display 2D data with full control over markup; the low-level primitive (use a future <code>tyui-data-grid</code> for batteries-included sorting/selection/virtualization).</p>\n<p>Do <strong>not</strong> use for single-column lists (→ <code>tyui-list</code>) or layout.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: tabular data with rows/columns and optional explicit selection/sort behavior.</li>\n<li>Do not use when: layout grids, cards, menus, or spreadsheet editing without grid contract.</li>\n<li>Prefer instead: CSS grid/layout for layout, Listbox/Grid for interactive selection-heavy data.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Row selection and row actions are separate targets; sorting header is one button target.</li>\n</ul>\n<h2>API</h2>\n<h3>tyui-table</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>size</code></td><td><code>extra-small | small | medium</code></td><td>yes</td><td><code>medium</code></td><td>row density</td></tr><tr><td><code>sortable</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>enables header sort affordance</td></tr><tr><td><code>no-native-elements</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>div+flex fallback (re-adds roles) — discouraged</td></tr></tbody></table>\n<h3>tyui-table-header-cell</h3>\n<table><thead><tr><th><code>sortable</code> (bool), <code>sort-direction</code> (<code>ascending|descending</code>) → <code>aria-sort</code>; slots <code>sort-icon</code>, <code>aside</code>, <code>button</code>.</th></tr></thead><tbody></tbody></table>\n<h3>tyui-table-selection-cell</h3>\n<table><thead><tr><th><code>type</code> (<code>checkbox|radio</code>), <code>checked</code> (<code>boolean|&quot;mixed&quot;</code>), <code>subtle</code>, <code>invisible</code>. Renders a native checkbox/radio.</th></tr></thead><tbody></tbody></table>\n<h3>tyui-table-row</h3>\n<table><thead><tr><th><code>appearance</code> (<code>none|neutral|brand</code>) — selection styling.</th></tr></thead><tbody></tbody></table>\n<h3>Events</h3>\n<ul>\n<li><code>sort-change</code> — composed; <code>{ columnId, direction }</code> on user sort.</li>\n<li><code>selection-change</code> — composed; <code>{ selectedItems, allSelected }</code> on user selection.</li>\n<p>  Both are user-initiated; programmatic state set is silent.</p>\n</ul>\n<h3>Slots</h3>\n<p>Structural (header/body/row/cell). <code>tyui-table-cell-layout</code>: <code>media</code>, <code>main</code>, <code>description</code>, <code>content</code>.</p>\n<h3>CSS parts</h3>\n<p><code>table</code>, <code>header</code>, <code>header-cell</code>, <code>sort</code>, <code>row</code>, <code>cell</code>, <code>selection-cell</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-table-row-min-block-size</code>, <code>--ty-table-cell-padding-inline</code>, <code>--ty-table-border-color</code>, <code>--ty-table-row-hover</code>, <code>--ty-table-row-selected</code>, <code>--ty-table-min-inline-size</code>.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: - <code>sort-change</code> — composed; <code>{ columnId, direction }</code> on user sort.</li>\n<li><code>selection-change</code> — composed; <code>{ selectedItems, allSelected }</code> on user selection.</li>\n<p>  Both are user-initiated; programmatic state set is silent.</p>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p>Structural (header/body/row/cell). <code>tyui-table-cell-layout</code>: <code>media</code>, <code>main</code>, <code>description</code>, <code>content</code>.</p>\n<h2>CSS Parts</h2>\n<p><code>table</code>, <code>header</code>, <code>header-cell</code>, <code>sort</code>, <code>row</code>, <code>cell</code>, <code>selection-cell</code>.</p>\n<h2>Events</h2>\n<ul>\n<li><code>sort-change</code> — composed; <code>{ columnId, direction }</code> on user sort.</li>\n<li><code>selection-change</code> — composed; <code>{ selectedItems, allSelected }</code> on user selection.</li>\n<p>  Both are user-initiated; programmatic state set is silent.</p>\n</ul>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: - <code>sort-change</code> — composed; <code>{ columnId, direction }</code> on user sort.</li>\n<li><code>selection-change</code> — composed; <code>{ selectedItems, allSelected }</code> on user selection.</li>\n<p>  Both are user-initiated; programmatic state set is silent.</p>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: table sections/rows/cells/header/selection cells only.</li>\n<li>Required parent: data region or form when selection submitted by app.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  Structural (header/body/row/cell). <code>tyui-table-cell-layout</code>: <code>media</code>, <code>main</code>, <code>description</code>, <code>content</code>.</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-table role=&quot;grid&quot;&gt;&lt;button&gt;&lt;a href=&quot;/&quot;&gt;Nested actions&lt;/a&gt;&lt;/button&gt;&lt;/tyui-table&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: data region or form when selection submitted by app.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Static table uses page Tab only; interactive grid mode defines cell/row roving focus and selection keys.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Use real <code>&lt;table&gt;</code> markup: semantics, header association, and screen-reader row/column reading are <strong>free</strong>. This is Oat's exact thesis applied to data.</li>\n<li><code>noNativeElements</code> (div+flex) mode exists in Fluent for layout flexibility, but the <strong>default and recommended path is native elements</strong>; only fall back to <code>role</code>-annotated divs when <code>display:table</code> truly can't do the layout, and then you must re-add every role.</li>\n<li>Sorting: a native <code>&lt;button&gt;</code> in the header toggles <code>aria-sort</code> on the <code>&lt;th&gt;</code>. Keyboard grid navigation (when interactive) = <strong>focusgroup polyfill</strong> following the <code>role=&quot;grid&quot;</code> pattern.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Static table uses page Tab only; interactive grid mode defines cell/row roving focus and selection keys.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: data region or form when selection submitted by app.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Static table uses page Tab only; interactive grid mode defines cell/row roving focus and selection keys.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Static table uses page Tab only; interactive grid mode defines cell/row roving focus and selection keys.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Row selection and row actions are separate targets; sorting header is one button target.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A unless an explicit row/cell action opens another component.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A for static table; grid mode manages focus inside cells/rows.</li>\n<li>Next Tab behavior: static table follows page order; grid mode uses one documented grid tab stop.</li>\n<li>Arrow-key entry behavior: N/A for static table; grid mode owns APG grid arrow navigation.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A unless grid edit/selection mode documents it.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: sorting/selection motion never changes row/cell semantics.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not inherently form-associated; selection value emits to app, not submitted unless wrapped.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not inherently form-associated; selection value emits to app, not submitted unless wrapped.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Sort, selection, resize, scroll, and optional virtualization observers clean up on disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: static table is not focusable. Interactive (grid) mode: <strong>single tab stop</strong>, arrow keys move the active cell, Home/End/PageUp/Down per grid pattern (focusgroup). Visible <code>:focus-visible</code> on the active cell/row, distinct from <code>selected</code>/hover.</li>\n<li><strong>Hit targets</strong>: separate targets for separate jobs — sort button, row-selection checkbox, and per-cell actions (<code>TableCellActions</code>) are each their own focusable target. Never make the whole row a single button that also contains a checkbox + actions. Row click-to-select is allowed only as an enhancement layered on the checkbox's state, not as an overloaded target.</li>\n<li><strong>Native behavior</strong>: native table semantics + native checkbox/radio + native sort button; no role override in native mode.</li>\n<li><strong>Composite widgets</strong>: the table owns grid roving focus and the shared selection/sort state; cells/rows dispatch narrow events the table aggregates (behavior.md composite ownership). Cell actions that are themselves menus are foreign composites the table ignores.</li>\n<li><strong>State &amp; events</strong>: <code>aria-sort</code>/selection reflected via ARIA + attributes; user sort/selection emit composed events; programmatic updates silent.</li>\n<li><strong>Disabled/readonly/loading</strong>: disabled row/checkbox uses native disabled or <code>disabled-focusable</code> (keep in grid order). Loading uses <code>aria-busy</code> on the table region + skeleton rows (behavior.md loading rule). Readonly = display-only (no selection cells).</li>\n<li><strong>Motion</strong>: row hover/selection transitions tokenized; reduced-motion safe. No layout-shifting animation during sort.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: native table layout by default; scroll container owns overflow; grid mode defines sticky/virtualization policy.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p>Structural (header/body/row/cell). <code>tyui-table-cell-layout</code>: <code>media</code>, <code>main</code>, <code>description</code>, <code>content</code>.</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-table-row-min-block-size</code>, <code>--ty-table-cell-padding-inline</code>, <code>--ty-table-border-color</code>, <code>--ty-table-row-hover</code>, <code>--ty-table-row-selected</code>, <code>--ty-table-min-inline-size</code>.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>table</code>, <code>header</code>, <code>header-cell</code>, <code>sort</code>, <code>row</code>, <code>cell</code>, <code>selection-cell</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Static table: native <code>table</code>/<code>row</code>/<code>columnheader</code>/<code>cell</code> roles — <strong>do not override</strong> (Fluent: &quot;Don't override the role&quot;).</li>\n<li><strong>Once any keyboard navigation/selection exists</strong>, the structure must follow the <strong><code>role=&quot;grid&quot;</code> pattern</strong> (W3C) — cells become <code>gridcell</code>, arrow-key navigation, single tab stop.</li>\n<li>Name the table: <code>aria-labelledby</code> (preceding heading) or <code>aria-label</code>.</li>\n<li>Sortable header: <code>&lt;th aria-sort=&quot;ascending|descending|none&quot;&gt;</code> + a <code>&lt;button&gt;</code> for the action; narrate via the button.</li>\n<li>Selection: <code>&lt;input type=checkbox&gt;</code> per row + a header &quot;select all&quot; (<code>aria-checked=&quot;mixed&quot;</code> for partial). Selected row reflects via attribute/<code>aria-selected</code> in grid mode.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Patterns: [APG Table](https://www.w3.org/WAI/ARIA/apg/patterns/table/) for static tabular data and [APG Grid](https://www.w3.org/WAI/ARIA/apg/patterns/grid/) when keyboard navigation or selection makes the table interactive.</li>\n<p>  - Direct requirements:</p>\n<p>    - Static table is not an interactive widget; cells are not focusable/selectable by the table itself.</p>\n<p>    - Use native HTML <code>table</code> whenever possible.</p>\n<p>    - If the table becomes a grid, it is a composite widget with one tab stop and author-managed focus movement inside.</p>\n<p>    - Data-grid arrows move focus by cell; Home/End move within row; Ctrl+Home/Ctrl+End move to first/last cell when supported.</p>\n<p>    - If selection is supported, common shortcuts include Shift+Space for row selection and Ctrl+A for select all when implemented.</p>\n<p>    - Grid cells should focus either the cell itself for text/graphics or the single contained widget when that widget does not use arrow keys.</p>\n<p>    - Interactive elements inside a static table remain normal page tab stops unless grid mode is explicitly implemented.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Table</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Table is low-level; prefer DataGrid when common features and first-class accessibility support are enough.</p>\n<p>    - Always include a header row.</p>\n<p>    - Use <code>aria-labelledby</code> when visible label text exists, or <code>aria-label</code> otherwise.</p>\n<p>    - Set a <code>min-width</code> so the table remains usable at high zoom or on small screens.</p>\n<p>    - Do not override Table control roles.</p>\n<p>    - Once any keyboard navigation exists, the component must follow the ARIA <code>role=&quot;grid&quot;</code> pattern.</p>\n<p>    - <code>noNativeElements</code> switches to div/flex and must re-add all roles manually.</p>\n<p>    - Cell actions that appear on hover must also appear/reveal on focus and remain keyboard accessible.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><strong>The <code>grid</code> upgrade is the trap</strong>: the moment you add keyboard nav or selection, ARIA requires the full <code>role=&quot;grid&quot;</code> pattern — partial implementations are worse than none. Spec must gate &quot;interactive table ⇒ grid pattern + focusgroup.&quot;</li>\n<li><code>noNativeElements</code> switches from <code>display:table</code> to flex and <strong>drops all native roles</strong> — every role/relationship must be re-added manually; recommend against unless layout demands it.</li>\n<li>Set a <code>min-width</code> (Fluent) so the table stays usable at high zoom / small screens; horizontal scroll container owned by the parent.</li>\n<li>&quot;Select all&quot; tri-state needs <code>aria-checked=&quot;mixed&quot;</code>; keep it in sync with row checkboxes.</li>\n<li>Cell actions appear on row hover/focus — they must remain reachable by keyboard (focus within row reveals them), not hover-only (behavior.md: hover must not be the only way to reach an action).</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: native table layout by default; scroll container owns overflow; grid mode defines sticky/virtualization policy remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: cell images/icons follow their own alt/decorative rules.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: cell images/icons follow their own alt/decorative rules.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/table.md</code>): &quot;Use the low-level <code>tyui-table</code> only when you need custom features; otherwise prefer <code>tyui-data-grid</code>. Always include a header row and a table name. Adding keyboard nav/selection means committing to the grid pattern.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>common features → tyui-data-grid</code>, <code>single column → tyui-list</code>, <code>key/value → description list</code>.</li>\n<li><strong>Layout ownership</strong>: table owns row density/cell padding/sort affordance; <strong>parent owns width, min-width, and horizontal scroll</strong>. layout.md: <code>min-inline-size</code> + container scroll, logical properties.</li>\n<li><strong>Token usage</strong>: density/hover/selected via <code>--ty-table-*</code>; selected row indicated by more than color (checkbox + <code>aria-selected</code>).</li>\n<li><strong>Anti-patterns to reject</strong>: role override on native table; whole-row single button containing checkbox/actions; hover-only cell actions; interactive table without grid pattern; <code>noNativeElements</code> without re-added roles; missing table name/header.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;data-table&quot;,</p>\n<p>    &quot;composite&quot;: &quot;grid when interactive&quot;,</p>\n<p>    &quot;rovingFocus&quot;: &quot;focusgroup-polyfilled&quot;,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;nativeRolesPreserved&quot;: true,</p>\n<p>      &quot;interactiveRequiresGridPattern&quot;: true,</p>\n<p>      &quot;needsAccessibleName&quot;: true</p>\n<p>    }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag missing <code>aria-label</code>/<code>aria-labelledby</code>; flag role overrides; flag interactive table lacking grid roles/focus; flag hover-only actions; flag overloaded row targets.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Native static table has correct roles and no extra tab stop</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Sortable header button updates <code>aria-sort</code> and emits composed <code>sort-change</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Checkbox/radio selection emits <code>selection-change</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Grid mode arrow navigation and Home/End</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled rows/cells behavior</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Virtualization/row replacement keeps selection by key</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Hover-only actions rejected</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Accessible name validation</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Static table semantics</td><td>Render a labelled native table with header/body rows.</td><td>Press <code>Tab</code> through the table.</td><td>Table itself and plain cells are not tab stops; native table roles/header associations are preserved</td></tr><tr><td>APG: Grid upgrade completeness</td><td>Enable keyboard navigation/selection.</td><td>Press Tab, Arrow keys, Home, End, Ctrl+Home, Ctrl+End.</td><td>Exactly one grid entry tab stop exists and focus moves per APG grid rules</td></tr><tr><td>APG: Cell focus target</td><td>Render grid cells with text, with a single button, and with multiple widgets.</td><td>Arrow into each cell.</td><td>Text cells focus the cell; single-widget cells may focus the widget; multiple-widget cells require explicit edit/interaction mode</td></tr><tr><td>APG: Selection shortcut</td><td>Render selectable grid rows.</td><td>Press Shift+Space on a focused row/cell.</td><td>Row selection updates checkbox/aria-selected and emits composed selection event</td></tr><tr><td>Fluent UI: Header and name</td><td>Render table without header or accessible name.</td><td>Run validation.</td><td>Missing header row/name fails</td></tr><tr><td>Fluent UI: Role override guard</td><td>Render native table cells with custom roles.</td><td>Run validation.</td><td>Role overrides fail unless <code>no-native-elements</code>/grid mode requires explicit roles</td></tr><tr><td>Fluent UI: High zoom min width</td><td>Render table at 400 percent zoom.</td><td>Inspect overflow behavior.</td><td>Table has documented min inline size and parent scroll ownership; content remains reachable</td></tr><tr><td>Fluent UI: Cell actions keyboard reachability</td><td>Render row actions visible on hover.</td><td>Focus through row and cell actions.</td><td>Actions become visible on focus within, are reachable, and are not hover-only</td></tr><tr><td>Fluent UI: DataGrid alternative</td><td>Ask generator for common sortable/selectable grid.</td><td>Run selection guidance.</td><td>Generator recommends <code>tyui-data-grid</code> unless low-level custom Table is justified</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → real <code>&lt;table&gt;</code> markup, native checkbox/radio/sort button.</td></tr><tr><td>behavior.md</td><td>Separate targets for sort / select / cell actions; no overloaded row.</td></tr><tr><td>behavior.md</td><td>Static = not focusable; interactive = grid pattern single tab stop via focusgroup.</td></tr><tr><td>behavior.md</td><td>Composite owns roving + selection/sort state; ignores foreign cell menus.</td></tr><tr><td>behavior.md</td><td>User sort/selection → composed events; programmatic silent.</td></tr><tr><td>behavior.md</td><td>Loading via <code>aria-busy</code> + skeletons; disabled-focusable for stable order.</td></tr><tr><td>behavior.md</td><td>Cell actions keyboard-reachable (not hover-only).</td></tr><tr><td>behavior.md</td><td>Motion tokenized, reduced-motion safe.</td></tr></tbody></table>")}
  </article>`,
};
