import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Badge',
  tags: ['spec-docs', "Show unimplemented components"],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font: 14px/1.55 system-ui, sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML("<h1>Badge</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-badge</code></li>\n<li>Define: <code>@toyu-ui/define/badge</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <code>&lt;span&gt;</code> (host itself; no inner control)</li>\n<li>Shadow DOM: <strong>no</strong> — Badge is pure decoration. A shadow root + <code>&lt;slot&gt;</code> buys nothing and only adds a boundary that label text must cross. Style the host directly (Oat-style classless/contextual CSS via <code>:host</code>).</li>\n<li>Category: presentational (non-interactive)</li>\n<li>Component family: display/status decoration</li>\n<li>Pattern type: presentational inline decoration</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Use to decorate another element with a short count, status, or label (&quot;6&quot;, &quot;New&quot;, &quot;•&quot;).</p>\n<p>Do <strong>not</strong> use when the information must be independently focusable or read on its own — badges are not in the tab order and are announced only as inline text of their host control. Convey the meaning on the parent (<code>aria-label</code>) instead.</p>\n<p>Alternatives: counter/status text inside a <code>Field</code>, <code>tyui-message-bar</code> for surface-level status.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: short counts, status words, or icon dots attached to an already-labelled control.</li>\n<li>Do not use when: standalone focusable information, live status, or actionable counters.</li>\n<li>Prefer instead: inline status text, Field helper text, or MessageBar.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: No pointer action on the badge; parent owns any click target.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>appearance</code></td><td><code>filled | ghost | outline | tint</code></td><td>yes</td><td><code>filled</code></td><td>remaps tokens only</td></tr><tr><td><code>color</code></td><td><code>brand | danger | important | informative | severe | subtle | success | warning</code></td><td>yes</td><td><code>brand</code></td><td>must not be sole meaning carrier</td></tr><tr><td><code>shape</code></td><td><code>circular | rounded | square</code></td><td>yes</td><td><code>circular</code></td><td></td></tr><tr><td><code>size</code></td><td><code>tiny | extra-small | small | medium | large | extra-large</code></td><td>yes</td><td><code>medium</code></td><td></td></tr><tr><td><code>icon-position</code></td><td><code>before | after</code></td><td>yes</td><td><code>before</code></td><td>CSS order flip</td></tr></tbody></table>\n<h3>Properties</h3>\n<p>Mirror the attributes (string/enums). No imperative methods.</p>\n<h3>Events</h3>\n<p>None. Badge emits nothing (behavior.md: no events without documented intent).</p>\n<h3>Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>count/label text</td></tr><tr><td><code>icon</code></td><td>optional leading/trailing icon</td></tr></tbody></table>\n<h3>CSS parts</h3>\n<p>None needed (no shadow DOM). Style via the element type + <code>[slot=icon]</code>.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-badge-background</code>, <code>--ty-badge-foreground</code>, <code>--ty-badge-border-color</code>, <code>--ty-badge-radius</code>, <code>--ty-badge-size</code>, <code>--ty-badge-padding-inline</code>. Each defaults to TYUI semantic tokens such as <code>var(--ty-color-surface)</code> and <code>var(--ty-color-text)</code>; variants remap these, never duplicate rule blocks (styles.md &quot;variants should remap tokens&quot;).</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: None. Badge emits nothing (behavior.md: no events without documented intent).</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>count/label text</td></tr><tr><td><code>icon</code></td><td>optional leading/trailing icon</td></tr></tbody></table>\n<h2>CSS Parts</h2>\n<p>None needed (no shadow DOM). Style via the element type + <code>[slot=icon]</code>.</p>\n<h2>Events</h2>\n<p>None. Badge emits nothing (behavior.md: no events without documented intent).</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: None. Badge emits nothing (behavior.md: no events without documented intent).</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: short text plus optional icon only; no interactive descendants.</li>\n<li>Required parent: optional labelled control that owns the action and accessible name.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n</ul>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>count/label text</td></tr><tr><td><code>icon</code></td><td>optional leading/trailing icon</td></tr></tbody></table>\n<ul>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-badge tabindex=&quot;0&quot; onclick=&quot;openInbox()&quot;&gt;6&lt;/tyui-badge&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: optional labelled control that owns the action and accessible name.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>No component keyboard handling; Tab skips the badge and lands on the owning control.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>The host <strong>is</strong> the badge. <code>display: inline-flex</code>, intrinsic size from content.</li>\n<li>No JS behavior at all in the common case — it is a styled box. This is the leanest possible component and the model the rest of the library should aspire to.</li>\n<li>Icon + text are slotted as plain light-DOM children; <code>iconPosition</code> is a host attribute that flips <code>flex-direction</code>/order in CSS, not JS.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: No component keyboard handling; Tab skips the badge and lands on the owning control.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: optional labelled control that owns the action and accessible name.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: No component keyboard handling; Tab skips the badge and lands on the owning control.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>No component keyboard handling; Tab skips the badge and lands on the owning control.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: No pointer action on the badge; parent owns any click target.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — Badge is never a popup or overlay.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: Tab skips the badge and reaches the owning control or next page target.</li>\n<li>Arrow-key entry behavior: N/A.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: motion is decorative only and never creates interactive overlay state.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated; disabled/readonly/loading do not apply to the badge itself.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated; disabled/readonly/loading do not apply to the badge itself.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: No external listeners; optional slotchange validation only if semantics are derived from slotted content.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: never focusable; never receives or moves focus. No <code>tabindex</code>.</li>\n<li><strong>Hit targets</strong>: Badge carries no action. If a count must be actionable (e.g. &quot;6 unread → open inbox&quot;), the _parent control_ owns that single action; the badge stays decorative. Never wrap a badge around a second interactive target.</li>\n<li><strong>Native behavior</strong>: it is literally a styled native <code>&lt;span&gt;</code> host — nothing to reimplement.</li>\n<li><strong>State &amp; events</strong>: no public state, no events. Color/shape are reflected host attributes (visual state via attributes per behavior.md).</li>\n<li><strong>Disabled/readonly/loading</strong>: N/A. A disabled host control styles its own badge via inherited tokens; the badge has no disabled mode.</li>\n<li><strong>Motion</strong>: none by default. A count change may use a tokenized, <code>prefers-reduced-motion</code>-gated transition; motion must never imply interactivity.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: inline-flex intrinsic pill/dot; parent owns placement such as avatar or button overlay.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td>count/label text</td></tr><tr><td><code>icon</code></td><td>optional leading/trailing icon</td></tr></tbody></table>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-badge-background</code>, <code>--ty-badge-foreground</code>, <code>--ty-badge-border-color</code>, <code>--ty-badge-radius</code>, <code>--ty-badge-size</code>, <code>--ty-badge-padding-inline</code>. Each defaults to TYUI semantic tokens such as <code>var(--ty-color-surface)</code> and <code>var(--ty-color-text)</code>; variants remap these, never duplicate rule blocks (styles.md &quot;variants should remap tokens&quot;).</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p>None needed (no shadow DOM). Style via the element type + <code>[slot=icon]</code>.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<table><thead><tr><th>Case</th><th>Markup</th></tr></thead><tbody><tr><td>Text badge (default)</td><td>no role; treated as inline text of host control</td></tr><tr><td>Icon-only / color-only</td><td><code>role=&quot;img&quot;</code> + <code>aria-label=&quot;…&quot;</code> (REQUIRED — meaning must not be color-only)</td></tr><tr><td>Decorative duplicate of visible info</td><td><code>aria-hidden=&quot;true&quot;</code></td></tr></tbody></table>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: N/A. APG has no Badge pattern; do not map Badge to Alert because APG Alert is for brief, important messages that are announced without moving focus. Badge remains inline/presentational unless the author explicitly exposes icon-only or color-only meaning as an image.</li>\n<p>  - Direct requirements:</p>\n<p>    - Default Badge has no widget role, no <code>tabindex</code>, and no activation behavior.</p>\n<p>    - Icon-only or color-only meaning is exposed through the parent accessible name/description or <code>role=&quot;img&quot;</code> plus <code>aria-label</code> on the badge.</p>\n<p>    - Important status that must be announced uses <code>tyui-message-bar</code>, not Badge.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Badge</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Badges should not receive focus; badge information is surfaced as part of the associated control.</p>\n<p>    - Custom icons need alternative text unless purely presentational.</p>\n<p>    - Color must not be the sole carrier of meaning.</p>\n<p>    - Badge text is short: small counts, labels, or status only.</p>\n<p>    - Supported API mirrors <code>appearance</code>, <code>color</code>, <code>shape</code>, <code>size</code>, and <code>iconPosition</code>.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li>Color-only badges (the &quot;unread dot&quot;) are an accessibility trap — the spec must force <code>role=&quot;img&quot;</code> + <code>aria-label</code>, or require the meaning on the parent's accessible name.</li>\n<li>Long text is unsupported by contract; the spec caps content and lets it truncate rather than wrap into a pill blob.</li>\n<li>Because there is no shadow DOM, page CSS can reach the badge — acceptable for decoration, but document that the public styling API is the token set, not arbitrary selectors.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: inline-flex intrinsic pill/dot; parent owns placement such as avatar or button overlay remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: optional icon is decorative unless it carries the entire visible meaning, then provide a text equivalent.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: optional icon is decorative unless it carries the entire visible meaning, then provide a text equivalent.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<p>How the agentic pipeline (<code>DESIGN.md</code> + <code>custom-elements.json</code> + <code>ai/components/badge.md</code> + <code>llms.txt</code>) should treat Badge:</p>\n<ul>\n<li><strong>Selection guidance</strong>: &quot;Use <code>tyui-badge</code> only as a child decoration of another labelled control. It is never a standalone interactive element.&quot; Add to <code>ai/components/badge.md</code> Intent + Invalid-examples.</li>\n<li><strong>Alternatives map</strong> (<code>x-design-system.alternatives</code>): <code>actionable → host control + aria-label</code>, <code>surface-status → tyui-message-bar</code>, <code>progress → tyui-progress-bar</code>.</li>\n<li><strong>Layout ownership</strong>: component owns intrinsic size + padding; parent owns placement (often absolute/<code>floatingAction</code> slot of a card or overlaid on an avatar). DESIGN.md layout policy should state badges do not stretch (<code>parentOwnsStretching: false</code>, intrinsic only).</li>\n<li><strong>Token usage</strong>: agent must theme via <code>--ty-badge-*</code> / semantic color tokens, never literal colors — feed this into the lint rule &quot;reject literal color on badge.&quot;</li>\n<li><strong>Anti-patterns to reject</strong>: focusable badge (<code>tabindex</code>), <code>onClick</code> on badge, color as sole status signal, long text.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;decoration&quot;,</p>\n<p>    &quot;focusable&quot;: false,</p>\n<p>    &quot;accessibility&quot;: {</p>\n<p>      &quot;requiresAccessibleName&quot;: &quot;when icon-only or color-only&quot;,</p>\n<p>      &quot;allowsInteractiveChildren&quot;: false</p>\n<p>    }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag any generated <code>tyui-badge</code> with event handlers, <code>tabindex</code>, or color-only meaning without <code>aria-label</code>/parent label.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Registration and default attributes</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Every appearance/shape/size reflects to host and uses <code>--ty-badge-*</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Badge is never tabbable</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Color-only or icon-only status requires an accessible name</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Forced-colors preserves text/border contrast</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Generated design validation rejects interactive/color-only misuse</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Badge is not an APG widget</td><td>Render <code>&lt;tyui-badge&gt;6&lt;/tyui-badge&gt;</code> inside a focusable Inbox button.</td><td>Press <code>Tab</code> through the page.</td><td>Focus lands on the button, never the badge; badge has no <code>role=&quot;button&quot;</code>, <code>role=&quot;alert&quot;</code>, or <code>tabindex</code></td></tr><tr><td>APG: Color-only status has a text equivalent</td><td>Render an unread-dot badge with no visible text.</td><td>Inspect generated markup and accessible name of the parent.</td><td>Either parent name/description includes the status or the badge has <code>role=&quot;img&quot;</code> and a non-empty <code>aria-label</code>; otherwise validation fails</td></tr><tr><td>APG: Announced status is routed away from Badge</td><td>Ask the generator for an important dynamic status badge.</td><td>Validate the generated component choice.</td><td>Generator selects <code>tyui-message-bar</code> or inline status text instead of adding alert semantics to Badge</td></tr><tr><td>Fluent UI: Fluent no-focus rule</td><td>Render all sizes and appearances with and without icons.</td><td>Sequentially press <code>Tab</code>.</td><td>No badge enters the tab order and no badge emits activation events</td></tr><tr><td>Fluent UI: Meaningful icon is named</td><td>Render a badge with an icon and no visible text.</td><td>Run accessibility validation.</td><td>Validation requires icon alt text or a parent label that includes the icon meaning</td></tr><tr><td>Fluent UI: Color is not the only signal</td><td>Render <code>color=&quot;success&quot;</code> and <code>color=&quot;warning&quot;</code> badges.</td><td>Inspect visible and accessibility output.</td><td>Each status is paired with text/hidden text/parent label; color-only output fails</td></tr><tr><td>Fluent UI: Short text constraint</td><td>Render a badge with a long sentence.</td><td>Run contract validation.</td><td>Generator flags the content as misuse and suggests inline text or MessageBar</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Focus moves only on explicit action → badge never takes focus.</td></tr><tr><td>behavior.md</td><td>One target = one action → badge holds no action; parent owns it.</td></tr><tr><td>behavior.md</td><td>Native semantics first → styled native span host, no scripted interaction.</td></tr><tr><td>behavior.md</td><td>Composite rules → N/A (leaf).</td></tr><tr><td>behavior.md</td><td>No events without documented intent → emits none.</td></tr><tr><td>behavior.md</td><td>Disabled/readonly/loading → N/A, documented.</td></tr><tr><td>behavior.md</td><td>Motion decorates only, reduced-motion safe.</td></tr></tbody></table>")}
  </article>`,
};
