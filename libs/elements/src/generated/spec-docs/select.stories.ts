import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Select',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Select</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-select</code></li>\n<li>Define: <code>@tyui/define/select</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>native <code>&lt;select&gt;</code></strong> (Fluent v9's Select is itself a thin wrapper around <code>&lt;select&gt;</code>).</li>\n<li>Shadow DOM: minimal. Shadow root holds a styled wrapper + <code>&lt;slot&gt;</code> for the <code>&lt;select&gt;</code> <strong>or</strong> a forwarded <code>&lt;select part=&quot;select&quot;&gt;</code> plus a decorative chevron <code>&lt;span part=&quot;icon&quot;&gt;</code>. The <code>&lt;option&gt;</code> list, popup, keyboard, typeahead, and form submission stay 100% native.</li>\n<li>Category: form control</li>\n<li>Component family: form control</li>\n<li>Pattern type: native select with listbox analogue</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Choose one option from a short, known list where a plain native dropdown is sufficient.</p>\n<p>Do <strong>not</strong> use for multi-select, free text, async search, or rich option content → that is a future <code>tyui-combobox</code>/<code>tyui-dropdown</code> (listbox + <code>aria-activedescendant</code>). Select is intentionally the cheap, fully-native option.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: single native choice from a bounded option list.</li>\n<li>Do not use when: rich async filtering, multi-select chips, or menu commands.</li>\n<li>Prefer instead: Combobox/Listbox for rich selection, Menu for commands.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: Click/tap opens native picker; option choice changes value.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>appearance</code></td><td><code>outline | underline | filled-darker | filled-lighter</code></td><td>yes</td><td><code>outline</code></td><td>CSS only</td></tr><tr><td><code>size</code></td><td><code>small | medium | large</code></td><td>yes</td><td><code>medium</code></td><td>matches Input</td></tr><tr><td><code>value</code></td><td><code>string</code></td><td>→select</td><td>—</td><td>controlled value</td></tr><tr><td><code>name</code></td><td><code>string</code></td><td>→select</td><td>—</td><td>form field name</td></tr><tr><td><code>disabled</code></td><td><code>boolean</code></td><td>→select</td><td><code>false</code></td><td>native</td></tr><tr><td><code>required</code></td><td><code>boolean</code></td><td>→select</td><td><code>false</code></td><td>native</td></tr></tbody></table>\n<h3>Properties</h3>\n<p><code>value</code>, <code>selectedIndex</code>, <code>options</code>, <code>form</code>, <code>validity</code>, <code>validationMessage</code> — forward to the inner <code>&lt;select&gt;</code>. Implement form participation via <strong><code>ElementInternals</code></strong> (<code>formAssociated = true</code>) so <code>tyui-select</code> submits like a native field.</p>\n<h3>Events</h3>\n<p>Re-dispatch native <strong><code>change</code></strong> and <strong><code>input</code></strong> as composed events crossing the shadow boundary (payload <code>{ value }</code>). These are user-input events → public (behavior.md &quot;user input emits public events&quot;). Programmatic <code>value</code> set emits nothing.</p>\n<h3>Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td><code>&lt;option&gt;</code> / <code>&lt;optgroup&gt;</code> elements</td></tr><tr><td><code>icon</code></td><td>optional custom chevron</td></tr></tbody></table>\n<h3>CSS parts</h3>\n<p><code>select</code> (control), <code>icon</code> (chevron).</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-select-background</code>, <code>--ty-select-border-color</code>, <code>--ty-select-radius</code>, <code>--ty-select-padding-inline</code>, <code>--ty-select-min-block-size</code>, <code>--ty-select-icon-color</code>. Appearances remap these.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: Re-dispatch native <strong><code>change</code></strong> and <strong><code>input</code></strong> as composed events crossing the shadow boundary (payload <code>{ value }</code>). These are user-input events → public (behavior.md &quot;user input emits public events&quot;). Programmatic <code>value</code> set emits nothing.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td><code>&lt;option&gt;</code> / <code>&lt;optgroup&gt;</code> elements</td></tr><tr><td><code>icon</code></td><td>optional custom chevron</td></tr></tbody></table>\n<h2>CSS Parts</h2>\n<p><code>select</code> (control), <code>icon</code> (chevron).</p>\n<h2>Events</h2>\n<p>Re-dispatch native <strong><code>change</code></strong> and <strong><code>input</code></strong> as composed events crossing the shadow boundary (payload <code>{ value }</code>). These are user-input events → public (behavior.md &quot;user input emits public events&quot;). Programmatic <code>value</code> set emits nothing.</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: Re-dispatch native <strong><code>change</code></strong> and <strong><code>input</code></strong> as composed events crossing the shadow boundary (payload <code>{ value }</code>). These are user-input events → public (behavior.md &quot;user input emits public events&quot;). Programmatic <code>value</code> set emits nothing.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: native option/optgroup-compatible items only.</li>\n<li>Required parent: Field/form.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n</ul>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td><code>&lt;option&gt;</code> / <code>&lt;optgroup&gt;</code> elements</td></tr><tr><td><code>icon</code></td><td>optional custom chevron</td></tr></tbody></table>\n<ul>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-select&gt;&lt;button&gt;Not an option&lt;/button&gt;&lt;/tyui-select&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: Field/form.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>Native select keyboard behavior; component must not reimplement arrow/typeahead differently.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>Use the real <code>&lt;select&gt;</code>: the OS-native popup, full keyboard model (typeahead, Home/End, arrows), touch behavior, and form participation are <strong>free and unbeatable</strong> — exactly Oat's &quot;let the platform be the component.&quot;</li>\n<li>We add only: border/appearance CSS and a decorative chevron. We do <strong>not</strong> rebuild the listbox.</li>\n<li><code>appearance</code>, <code>size</code> = CSS. <code>onChange</code> = native <code>change</code>.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: Native select keyboard behavior; component must not reimplement arrow/typeahead differently.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: Field/form.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: Native select keyboard behavior; component must not reimplement arrow/typeahead differently.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>Native select keyboard behavior; component must not reimplement arrow/typeahead differently.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: Click/tap opens native picker; option choice changes value.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: native select activation by pointer, keyboard, or platform picker.</li>\n<li>Closes on: native picker selection, cancel, blur, or platform-specific close behavior.</li>\n<li>DOM focus while open: focus remains on the native select/picker according to platform behavior.</li>\n<li>Next Tab behavior: native select commits/leaves according to platform defaults.</li>\n<li>Arrow-key entry behavior: native select owns arrow/typeahead behavior.</li>\n<li>Outside click / pointerdown behavior: native picker/platform behavior.</li>\n<li>Escape behavior: native picker/platform behavior.</li>\n<li>Focus restoration on close: remains on or returns to the native select per platform.</li>\n<li>Behavior while enter / exit motion is running: no custom popup motion is applied.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Form-associated native select semantics: name/value, required, disabled, validation, reset.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Form-associated native select semantics: name/value, required, disabled, validation, reset.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Native select/form/slotchange listeners update options and validation wiring and clean up on disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: native <code>&lt;select&gt;</code> is one tab stop; focus moves only on click/Tab. Visible <code>:focus-visible</code> ring on <code>part(select)</code>. No active-descendant gymnastics — the native control owns it.</li>\n<li><strong>Hit targets</strong>: one control, one job (pick a value). Chevron is decorative, not a separate target.</li>\n<li><strong>Native behavior</strong>: keep native popup, typeahead, keyboard, IME, validation, and form submit. This is the textbook behavior.md &quot;checkbox/radio/select preserve native semantics&quot; requirement.</li>\n<li><strong>Composite widgets</strong>: N/A — <code>&lt;select&gt;</code> is a single native control, not a JS composite. (Multi/async = different component.)</li>\n<li><strong>State &amp; events</strong>: emits composed <code>change</code>/<code>input</code> on user selection; reflects <code>value</code> as a property (not an attribute, to avoid attribute/DOM thrash); <code>disabled</code>/<code>required</code>/<code>invalid</code> reflected for styling.</li>\n<li><strong>Disabled/readonly/loading</strong>: <code>disabled</code> → native disabled (out of tab order). <code>&lt;select&gt;</code> has no native readonly → if needed, emulate by disabling + a hidden submitted value, and <strong>document</strong> it. No loading state.</li>\n<li><strong>Motion</strong>: none (native popup animation is OS-owned).</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: inline/block control with intrinsic min width and field-compatible sizing.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<table><thead><tr><th>Name</th><th>Description</th></tr></thead><tbody><tr><td>default</td><td><code>&lt;option&gt;</code> / <code>&lt;optgroup&gt;</code> elements</td></tr><tr><td><code>icon</code></td><td>optional custom chevron</td></tr></tbody></table>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-select-background</code>, <code>--ty-select-border-color</code>, <code>--ty-select-radius</code>, <code>--ty-select-padding-inline</code>, <code>--ty-select-min-block-size</code>, <code>--ty-select-icon-color</code>. Appearances remap these.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>select</code> (control), <code>icon</code> (chevron).</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Native <code>&lt;select&gt;</code> exposes <code>combobox</code>/<code>listbox</code> semantics natively — <strong>do not override role</strong>.</li>\n<li>Labeling comes from <code>tyui-field</code>/<code>&lt;label for&gt;</code>; the component forwards <code>id</code>/<code>aria-labelledby</code>/<code>aria-describedby</code> to the inner <code>&lt;select&gt;</code>.</li>\n<li><code>aria-invalid</code> mirrored from field validation.</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: [APG Listbox](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) as the closest selection pattern, with the native <code>&lt;select&gt;</code> providing the actual platform behavior.</li>\n<p>  - Direct requirements:</p>\n<p>    - Select presents a list of options and allows one option to be chosen.</p>\n<p>    - Options have flat string names; semantic structures inside options are not exposed as rich semantics.</p>\n<p>    - Listbox-style options must not contain interactive elements; use Grid or Combobox/Listbox alternatives for rich interactive content.</p>\n<p>    - Avoid very long option names and repeated prefixes that make screen-reader navigation inefficient.</p>\n<p>    - Native <code>&lt;select&gt;</code> keyboard, typeahead, popup, and form behavior must not be overridden.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Select</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - Select is a wrapper around native <code>&lt;select&gt;</code>.</p>\n<p>    - Prefer underline or outline appearances when surrounding contrast is less than 3:1.</p>\n<p>    - Filled appearances require sufficient contrast against the immediate surrounding surface.</p>\n<p>    - <code>onChange</code> fires when the user changes the native select value.</p>\n<p>    - Disabled is native.</p>\n<p>    - Controlled value, default value, and size map to native select behavior and styling.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><code>&lt;select&gt;</code> has <strong>no readonly</strong> — spec must state this and give the disabled+hidden-input pattern if a readonly select is requested.</li>\n<li>Cross-browser <code>&lt;option&gt;</code> styling is limited; advanced option rendering is explicitly out of scope (→ combobox). Don't promise themed options.</li>\n<li>Form participation requires <code>ElementInternals</code>; without it the custom element won't submit. Make it a hard requirement.</li>\n<li>Mirroring author <code>&lt;option&gt;</code>s into an internal select needs a <code>slotchange</code> sync; keep it minimal and avoid rebuilding on every change.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: inline/block control with intrinsic min width and field-compatible sizing remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: decorative chevron only; option icons are not supported in native mode.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: decorative chevron only; option icons are not supported in native mode.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/select.md</code>): &quot;Use <code>tyui-select</code> for single choice from a short static list. For search, multi-select, or rich options use <code>tyui-combobox</code>/<code>tyui-dropdown</code>.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>multi/async/search → tyui-combobox</code>, <code>2–5 visible choices → tyui-radio-group</code>, <code>binary → tyui-switch</code>.</li>\n<li><strong>Layout ownership</strong>: component owns intrinsic min size + padding; parent/field owns width (often full-width in a form). layout.md: <code>min-block-size</code>, never fixed height.</li>\n<li><strong>Token usage</strong>: appearance via <code>--ty-select-*</code>; contrast requirement (≥3:1 border vs surface) becomes a DESIGN.md accessibility note for filled variants.</li>\n<li><strong>Anti-patterns to reject</strong>: overriding the native role; using Select where free text/multi is needed; readonly Select without the documented workaround; missing label/field association.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;single-select&quot;,</p>\n<p>    &quot;focusable&quot;: true,</p>\n<p>    &quot;formAssociated&quot;: true,</p>\n<p>    &quot;alternatives&quot;: { &quot;search&quot;: &quot;tyui-combobox&quot;, &quot;multiselect&quot;: &quot;tyui-combobox&quot; },</p>\n<p>    &quot;accessibility&quot;: { &quot;nativeRole&quot;: &quot;preserved&quot;, &quot;needsAssociatedLabel&quot;: true }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag generated <code>tyui-select</code> without an associated label/field; flag <code>role</code> override; flag attempts at <code>multiple</code>/free-text.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Form submission and <code>FormData</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Value/default selected option</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td><code>input</code>/<code>change</code> composed event payloads</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Programmatic <code>value</code> silent</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Required validity and <code>reportValidity()</code> anchor</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Disabled removal from Tab</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Native keyboard/typeahead not canceled</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Option projection rejects rich content</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Native selection behavior</td><td>Render three static options in a labelled Select.</td><td>Use trusted keyboard to open, arrow/typeahead, and change value.</td><td>Native select updates value and emits composed user <code>input</code>/<code>change</code>; no custom role override exists</td></tr><tr><td>APG: Rich option guard</td><td>Render an option containing a button, link, or heading.</td><td>Run design validation.</td><td>Validation rejects rich/interactive option content and suggests Combobox/Grid alternative</td></tr><tr><td>APG: Option-name quality</td><td>Render options with long repeated prefixes.</td><td>Run content validation.</td><td>Validation warns about repeated prefix/long names and suggests restructuring</td></tr><tr><td>APG: Label requirement</td><td>Render Select without Field/label/aria-label.</td><td>Run accessibility validation.</td><td>Validation fails until the native select has an associated name</td></tr><tr><td>Fluent UI: Contrast guard</td><td>Render filled-lighter and filled-darker selects on low-contrast surfaces.</td><td>Run visual/contrast validation.</td><td>Border/surface contrast is at least 3:1 or validation recommends outline/underline</td></tr><tr><td>Fluent UI: Controlled value</td><td>Render controlled Select and external &quot;Select Blue&quot; button.</td><td>Click external button, then use native select.</td><td>Programmatic value update is silent; user change emits composed event with new value</td></tr><tr><td>Fluent UI: Disabled native behavior</td><td>Render disabled Select.</td><td>Press Tab and attempt pointer activation.</td><td>Native select is skipped/inert and retains disabled styling</td></tr><tr><td>Fluent UI: Size styling</td><td>Render small, medium, large.</td><td>Inspect computed styles.</td><td>Size changes use tokens and preserve native select semantics</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → real <code>&lt;select&gt;</code>, native keyboard/popup/validation.</td></tr><tr><td>behavior.md</td><td>One target = one action.</td></tr><tr><td>behavior.md</td><td>Focus native; visible ring.</td></tr><tr><td>behavior.md</td><td>User input → composed <code>change</code>/<code>input</code>; programmatic set silent.</td></tr><tr><td>behavior.md</td><td>Disabled native; readonly limitation documented.</td></tr><tr><td>behavior.md</td><td>Composite rules N/A (single native control).</td></tr><tr><td>behavior.md</td><td>No custom motion.</td></tr></tbody></table>",
    )}
  </article>`,
};
