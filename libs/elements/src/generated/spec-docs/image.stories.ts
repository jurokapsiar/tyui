import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { unsafeHTML } from 'lit/directives/unsafe-html.js';

const meta: Meta = {
  title: 'Unimplemented components/Image',
  tags: ['spec-docs', 'Show unimplemented components'],
  parameters: {
    options: { showPanel: false },
  },
};

export default meta;

type Story = StoryObj;

const styles = html`
  <style>
    .ty-spec-doc {
      box-sizing: border-box;
      max-width: 920px;
      padding: 32px;
      color: CanvasText;
      font:
        14px/1.55 system-ui,
        sans-serif;
    }

    .ty-spec-doc h1,
    .ty-spec-doc h2,
    .ty-spec-doc h3 {
      line-height: 1.2;
    }

    .ty-spec-doc h1 {
      margin: 0 0 20px;
      font-size: 28px;
    }

    .ty-spec-doc h2 {
      margin: 28px 0 12px;
      font-size: 20px;
    }

    .ty-spec-doc h3 {
      margin: 22px 0 10px;
      font-size: 16px;
    }

    .ty-spec-doc table {
      width: 100%;
      border-collapse: collapse;
      margin: 14px 0 22px;
      font-size: 13px;
    }

    .ty-spec-doc th,
    .ty-spec-doc td {
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      padding: 8px 10px;
      text-align: start;
      vertical-align: top;
    }

    .ty-spec-doc th {
      background: color-mix(in srgb, CanvasText 6%, Canvas);
      font-weight: 650;
    }

    .ty-spec-doc code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      font-size: 0.92em;
    }

    .ty-spec-doc pre {
      overflow: auto;
      padding: 14px;
      border: 1px solid color-mix(in srgb, CanvasText 18%, Canvas);
      border-radius: 6px;
      background: color-mix(in srgb, CanvasText 5%, Canvas);
    }
  </style>
`;

export const Spec: Story = {
  render: () => html`<article class="ty-spec-doc">
    ${styles}
    ${unsafeHTML(
      "<h1>Image</h1>\n<h2>Identity</h2>\n<ul>\n<li>Tag: <code>tyui-image</code></li>\n<li>Define: <code>@tyui/define/image</code></li>\n<li>Status: draft</li>\n<li>Native substrate: <strong>native <code>&lt;img&gt;</code></strong></li>\n<li>Shadow DOM: optional/minimal. A thin shadow root holding one <code>&lt;img part=&quot;img&quot;&gt;</code> is acceptable so <code>shape</code>/<code>fit</code>/<code>bordered</code> styling is encapsulated, but <code>alt</code>, <code>src</code>, <code>loading</code>, native fallback and the broken-image glyph must be forwarded to the real <code>&lt;img&gt;</code>. If encapsulation is not needed, render the <code>&lt;img&gt;</code> in light DOM (lighter, Oat-preferred).</li>\n<li>Category: presentational</li>\n<li>Component family: media</li>\n<li>Pattern type: native image</li>\n<li>Fluent / reference analogue: see API and Accessibility.</li>\n</ul>\n<h2>Intent</h2>\n<p>Display a photo/illustration with consistent shape/fit/border/shadow treatment.</p>\n<p>Do <strong>not</strong> use for icons (use <code>tyui-icon</code>) or for decorative CSS backgrounds.</p>\n<h2>Selection Guidance</h2>\n<ul>\n<li>Use when: render meaningful or decorative images with native loading/fallback behavior.</li>\n<li>Do not use when: icons without labels, interactive image buttons, or responsive art direction beyond contract.</li>\n<li>Prefer instead: Icon for symbolic glyphs, Card media slot for composed content.</li>\n<li>Product-level variant preferences: choose the least-interactive variant that preserves native semantics and behavior.md target ownership.</li>\n<li>One semantic target / one action rule: No component pointer action; wrapping control owns any action.</li>\n</ul>\n<h2>API</h2>\n<h3>Attributes</h3>\n<table><thead><tr><th>Name</th><th>Type</th><th>Reflects</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>src</code></td><td><code>string</code></td><td>→img</td><td>—</td><td>forwarded</td></tr><tr><td><code>alt</code></td><td><code>string</code></td><td>→img</td><td>—</td><td>forwarded; required unless decorative</td></tr><tr><td><code>block</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>fill container width</td></tr><tr><td><code>bordered</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>rectangular border</td></tr><tr><td><code>shadow</code></td><td><code>boolean</code></td><td>yes</td><td><code>false</code></td><td>elevation</td></tr><tr><td><code>fit</code></td><td><code>none | center | contain | cover | default</code></td><td>yes</td><td><code>default</code></td><td>→ <code>object-fit</code>/positioning</td></tr><tr><td><code>shape</code></td><td><code>square | rounded | circular</code></td><td>yes</td><td><code>square</code></td><td>→ border-radius</td></tr><tr><td><code>loading</code></td><td><code>lazy | eager</code></td><td>→img</td><td><code>lazy</code></td><td>native</td></tr></tbody></table>\n<h3>Properties</h3>\n<p>Mirror attributes; expose <code>naturalWidth</code>/<code>complete</code> passthrough getters if needed. No methods.</p>\n<h3>Events</h3>\n<p>Re-expose native <code>load</code>/<code>error</code> (already bubble? no — <code>error</code> does not bubble). Forward as composed <code>load</code>/<code>error</code> only if a product needs them; otherwise rely on listening to the inner img. Default: <strong>no custom events</strong>.</p>\n<h3>Slots</h3>\n<p>None (content is the image itself). Avoid a default slot — an <code>&lt;img&gt;</code> has no children.</p>\n<h3>CSS parts</h3>\n<p><code>img</code> — the native image element.</p>\n<h3>CSS custom properties</h3>\n<p><code>--ty-image-radius</code>, <code>--ty-image-border-color</code>, <code>--ty-image-border-width</code>, <code>--ty-image-shadow</code>. Default to semantic tokens; <code>shape</code>/<code>bordered</code> remap these.</p>\n<h3>Event Semantics</h3>\n<ul>\n<li>User-initiated events: Re-expose native <code>load</code>/<code>error</code> (already bubble? no — <code>error</code> does not bubble). Forward as composed <code>load</code>/<code>error</code> only if a product needs them; otherwise rely on listening to the inner img. Default: <strong>no custom events</strong>.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Slots</h2>\n<p>None (content is the image itself). Avoid a default slot — an <code>&lt;img&gt;</code> has no children.</p>\n<h2>CSS Parts</h2>\n<p><code>img</code> — the native image element.</p>\n<h2>Events</h2>\n<p>Re-expose native <code>load</code>/<code>error</code> (already bubble? no — <code>error</code> does not bubble). Forward as composed <code>load</code>/<code>error</code> only if a product needs them; otherwise rely on listening to the inner img. Default: <strong>no custom events</strong>.</p>\n<h2>Event Semantics</h2>\n<ul>\n<li>User-initiated events: Re-expose native <code>load</code>/<code>error</code> (already bubble? no — <code>error</code> does not bubble). Forward as composed <code>load</code>/<code>error</code> only if a product needs them; otherwise rely on listening to the inner img. Default: <strong>no custom events</strong>.</li>\n<li>Programmatic state changes that must not emit user events: reflected attribute/property updates are silent unless the Events table explicitly says otherwise.</li>\n<li>Native events that are re-dispatched: preserve native semantics; do not wrap native activation/change events unless needed for composed shadow-boundary API.</li>\n<li>Internal coordination events: component-specific, non-public, and ignored by parents that do not own the composite.</li>\n<li>Cancellation behavior: only cancel default browser behavior when required by the documented keyboard/popup/composite pattern.</li>\n</ul>\n<h2>Composition Contract</h2>\n<ul>\n<li>Allowed children: fallback/placeholder content only when image fails or is loading.</li>\n<li>Required parent: none.</li>\n<li>Required child components: N/A unless listed in the API slots below.</li>\n<li>Optional child components: see Slots and Examples.</li>\n<li>Allowed slots:</li>\n<p>  None (content is the image itself). Avoid a default slot — an <code>&lt;img&gt;</code> has no children.</p>\n<li>Disallowed nested interactive content: any interactive descendant not explicitly named as an allowed separate target.</li>\n<li>Composition anti-patterns: &lt;tyui-image src=&quot;delete.png&quot; onclick=&quot;deleteItem()&quot;&gt;&lt;/tyui-image&gt;</li>\n</ul>\n<h2>Behavior</h2>\n<h3>State Model</h3>\n<ul>\n<li>Controlled state: public attributes/properties listed in API; app-owned where the component exposes state reflection.</li>\n<li>Uncontrolled / default state: defaults listed in API are used when attributes are absent.</li>\n<li>Derived internal state: private only; reflected through documented attributes, ARIA, pseudo-classes, or CSS variables when public.</li>\n<li>Parent-owned state: none.</li>\n<li>Child-owned state: children own only native local state unless this spec assigns ownership to a parent composite.</li>\n<li>Programmatic update behavior: update rendering/ARIA without emitting user-facing change events.</li>\n<li>User update behavior: emit only the public events documented in API.</li>\n<li>State reset behavior: reset to API defaults or native form reset behavior where applicable.</li>\n</ul>\n<h3>State Transition Matrix</h3>\n<table><thead><tr><th>Current State</th><th>User / Programmatic Action</th><th>Next State</th><th>Event</th><th>Focus Result</th><th>Notes</th></tr></thead><tbody><tr><td>Default</td><td>Programmatic attribute/property update</td><td>Reflected documented state</td><td>None unless documented</td><td>Focus unchanged</td><td>State/events rule from behavior.md.</td></tr><tr><td>Default / interactive state</td><td>Valid user activation or input</td><td>Component-specific state from API</td><td>Public composed event when documented</td><td>No keyboard behavior; image is not focusable unless wrapped by an external control.</td><td>Preserve native behavior first.</td></tr><tr><td>Disabled / inert where supported</td><td>User pointer or keyboard input</td><td>State unchanged</td><td>None</td><td>Focus follows disabled contract</td><td>Disabled-focusable exceptions must be explicit.</td></tr></tbody></table>\n<h3>Native Behavior First</h3>\n<ul>\n<li>The browser does everything: loading, decoding, lazy-loading, broken-image fallback, <code>srcset</code>/<code>sizes</code>. We add only CSS for <code>shape</code> (border-radius), <code>fit</code> (<code>object-fit</code>), <code>bordered</code>, <code>shadow</code>, <code>block</code>.</li>\n<li>Zero JS interaction. <code>fit</code> maps directly to <code>object-fit</code>; <code>block</code> toggles <code>display:block; inline-size:100%</code>.</li>\n<li>Fluent's &quot;fallback = native <code>&lt;img&gt;</code> fallback&quot; is exactly the native behavior we keep — do not script an error placeholder unless a product opts in.</li>\n</ul>\n<ul>\n<li>Native element used: see Identity and DOM sections.</li>\n<li>Native behavior preserved: No keyboard behavior; image is not focusable unless wrapped by an external control.</li>\n<li>Custom behavior added: only behavior commitments below and reference-pattern requirements in Accessibility.</li>\n<li>Why custom behavior is necessary: design-system parity, composite coordination, popup lifecycle, or accessibility wiring native HTML cannot express alone.</li>\n</ul>\n<h3>Focus Model</h3>\n<ul>\n<li>Focus owner: none.</li>\n<li><code>delegatesFocus</code>: no unless the DOM section explicitly opts into shadow focus delegation.</li>\n<li>Tabbable elements: No keyboard behavior; image is not focusable unless wrapped by an external control.</li>\n<li>Roving tabindex: only when the APG/Fluent reference requires it.</li>\n<li>Active descendant: only for combobox/listbox-like controls; otherwise N/A.</li>\n<li>Focus restoration: required for popups/overlays and documented in Popup / Overlay Contract.</li>\n<li>Focus trap: only modal dialog or explicitly trapped popover mode.</li>\n<li>Focus-visible treatment: keyboard focus must be visible and distinct from hover/selected/active/pressed states.</li>\n<li>Pointer focus treatment: pointer interaction must not move DOM focus unless native activation or the documented pattern requires it.</li>\n</ul>\n<h3>Keyboard Contract</h3>\n<table><thead><tr><th>Key</th><th>Context</th><th>Action</th><th>Prevent Default</th><th>Event</th><th>Notes</th></tr></thead><tbody><tr><td>Tab / Shift+Tab</td><td>Sequential navigation</td><td>No keyboard behavior; image is not focusable unless wrapped by an external control.</td><td>No, except trapped modal focus cycles</td><td>None</td><td>Defines tab-order participation.</td></tr><tr><td>Enter / Space</td><td>Activation-capable target</td><td>Use native activation or documented APG behavior</td><td>Only when replacing native behavior</td><td>Public event if documented</td><td>One target = one action.</td></tr><tr><td>Escape</td><td>Popup/overlay/composite where applicable</td><td>Close or dismiss only when documented</td><td>Yes when handled</td><td>open/dismiss event if documented</td><td>No-op for non-popup components.</td></tr><tr><td>Arrow keys</td><td>Composite widgets only</td><td>Follow selected APG pattern</td><td>Yes when roving/composite handles it</td><td>None or documented navigation event</td><td>Non-composites preserve native behavior.</td></tr></tbody></table>\n<h3>Pointer And Hit Target Contract</h3>\n<ul>\n<li>Primary hit target: No component pointer action; wrapping control owns any action.</li>\n<li>Secondary hit targets: must be separate focusable controls, slots, or mode-specific entries.</li>\n<li>Hover behavior: may preview or style only; must not select, dismiss, navigate, toggle, or steal focus unless this spec documents a menu-like exception.</li>\n<li>Pressed / active behavior: mirrors native active state or documented selected/checked/expanded state.</li>\n<li>Minimum target size: follow layout tokens and do not shrink interactive controls below the design-system minimum target.</li>\n<li>Touch / pen considerations: same target ownership as mouse; no hover-only required behavior.</li>\n<li>Overloaded-target risks: reject generated output matching the anti-example in Selection/Composition.</li>\n</ul>\n<h3>Popup / Overlay Contract</h3>\n<ul>\n<li>Opens on: N/A — Image is media, not an overlay.</li>\n<li>Closes on: N/A.</li>\n<li>DOM focus while open: N/A.</li>\n<li>Next Tab behavior: image is skipped unless wrapped by an external control.</li>\n<li>Arrow-key entry behavior: N/A.</li>\n<li>Outside click / pointerdown behavior: N/A.</li>\n<li>Escape behavior: N/A.</li>\n<li>Focus restoration on close: N/A.</li>\n<li>Behavior while enter / exit motion is running: loading/fallback visual changes do not alter focus or semantics.</li>\n</ul>\n<h3>Form Contract</h3>\n<ul>\n<li>Form-associated: Not form-associated; native loading attribute is not busy state.</li>\n<li>Submitted value: native form controls only; otherwise N/A.</li>\n<li><code>FormData</code> behavior: native form controls only; otherwise N/A.</li>\n<li>Validity states: native form controls or documented validation wrappers only.</li>\n<li><code>checkValidity()</code> / <code>reportValidity()</code>: preserved for native/form-associated controls only.</li>\n<li>Name propagation: native/form-associated controls only.</li>\n<li>Required / readonly / disabled behavior: Not form-associated; native loading attribute is not busy state.</li>\n</ul>\n<h3>Lifecycle And Cleanup</h3>\n<ul>\n<li>External event listeners: Native load/error listeners update state and are removed on disconnect.</li>\n<li>Observers: disconnect on component removal and when no longer needed.</li>\n<li>Timers: clear on close/disconnect; no orphan delayed hover, dismiss, or animation timers.</li>\n<li>Generated IDs: stable for ARIA relationships and not regenerated on every render.</li>\n<li>Slotchange work: validate composition and update ARIA/relationship wiring without emitting user events.</li>\n<li>Cleanup requirements: all listeners, observers, timers, and top-layer state removed on disconnect.</li>\n</ul>\n<h3>Current Behavior Commitments</h3>\n<ul>\n<li><strong>Focus</strong>: not focusable (an image is not a control). If an image is a link/button, the _wrapping_ <code>tyui-link</code>/<code>tyui-button</code> owns focus and action — never put <code>tabindex</code> on the image.</li>\n<li><strong>Hit targets</strong>: image carries no action; one action belongs to a wrapping control.</li>\n<li><strong>Native behavior</strong>: native <code>&lt;img&gt;</code> loading/fallback/<code>alt</code> kept fully intact — the canonical behavior.md &quot;preserve native semantics&quot; case.</li>\n<li><strong>State &amp; events</strong>: no public state; load/error are native. No reflected interactive state.</li>\n<li><strong>Disabled/readonly/loading</strong>: N/A. (&quot;loading&quot; here is the native lazy attribute, not a busy state.)</li>\n<li><strong>Motion</strong>: none, except an optional tokenized fade-in on <code>load</code>, gated by <code>prefers-reduced-motion</code>.</li>\n</ul>\n<h2>Layout Contract</h2>\n<ul>\n<li>Display: inline/block replaced element with aspect-ratio and object-fit/object-position contract.</li>\n<li>Intrinsic size: documented by CSS tokens and native substrate.</li>\n<li>Shrink policy: shrink only within readable/usable limits; do not collapse interactive targets below minimum.</li>\n<li>Wrap policy: component-specific content may wrap only where layout section or examples show it.</li>\n<li>Minimum target token: use the design-system interactive target token for focusable controls.</li>\n<li>Minimum visual target: preserve hit target even when visual affordance is smaller.</li>\n<li>Flexible slots: content/body/default slots unless API marks them fixed.</li>\n<li>Fixed slots: icons, checkmarks, separators, triggers, and action regions when documented.</li>\n<li>Parent owns: placement in page, grid/list position, and external margins.</li>\n<li>Component owns: internal alignment, gap, padding, state surfaces, and ARIA-owned relationship layout.</li>\n<li>Container-query thresholds: use only when documented in DESIGN metadata.</li>\n<li>Scroll / overflow policy: internal scroll only for documented popup/table/dialog surfaces.</li>\n<li>Top-layer / popover policy: use native Popover/top-layer only for popup components.</li>\n</ul>\n<h3>Regions / Slots</h3>\n<p>None (content is the image itself). Avoid a default slot — an <code>&lt;img&gt;</code> has no children.</p>\n<h2>Styling Contract</h2>\n<h3>Public Tokens</h3>\n<p><code>--ty-image-radius</code>, <code>--ty-image-border-color</code>, <code>--ty-image-border-width</code>, <code>--ty-image-shadow</code>. Default to semantic tokens; <code>shape</code>/<code>bordered</code> remap these.</p>\n<h3>Private Implementation Variables</h3>\n<p>Private variables use the <code>--_ty-*</code> prefix and are not consumer API. Do not document generated/private variables as stable hooks.</p>\n<h3>CSS Parts</h3>\n<p><code>img</code> — the native image element.</p>\n<h3>Styling State Surface</h3>\n<table><thead><tr><th>State</th><th>Surface</th><th>Public</th><th>Notes</th></tr></thead><tbody><tr><td>Default / variant</td><td>Reflected attributes and public CSS custom properties</td><td>Yes</td><td>Variants remap tokens, not duplicated rule blocks.</td></tr><tr><td>Focus / hover / active</td><td>Native pseudo-classes or host attributes</td><td>Yes</td><td>Focus must remain distinct from hover/selected/pressed.</td></tr><tr><td>Internal derived state</td><td><code>data-*</code> or private variables</td><td>No</td><td>Private unless explicitly listed in API.</td></tr></tbody></table>\n<ul>\n<li>Forced-colors behavior: preserve visible boundaries and text/icon contrast.</li>\n<li>Reduced-motion behavior: honor <code>prefers-reduced-motion</code>; motion never decides state.</li>\n<li>App-variant hooks: public tokens and documented parts only.</li>\n</ul>\n<h2>Accessibility</h2>\n<ul>\n<li>Meaningful image: native <code>alt=&quot;…&quot;</code> (required for non-decorative).</li>\n<li>Decorative image: <code>alt=&quot;&quot;</code> (preferred) or <code>role=&quot;presentation&quot;</code>/<code>aria-hidden=&quot;true&quot;</code>.</li>\n<li>No &quot;image of&quot;/&quot;picture of&quot; prefixes (Fluent guidance baked into docs).</li>\n</ul>\n<h3>Reference Requirements</h3>\n<ul>\n<li><strong>APG reference</strong>: Pattern: N/A. APG has no Image pattern; do not infer a widget role. Image behavior is native <code>&lt;img&gt;</code> semantics plus author-provided text alternatives.</li>\n<p>  - Direct requirements:</p>\n<p>    - Meaningful images expose an accessible name through <code>alt</code>.</p>\n<p>    - Decorative images opt out with <code>alt=&quot;&quot;</code>, <code>role=&quot;presentation&quot;</code>, or <code>aria-hidden=&quot;true&quot;</code> according to the chosen native rendering.</p>\n<p>    - Image is not focusable and carries no action; wrapping Link/Button owns any action.</p>\n<p>    - Native load/error/fallback behavior is preserved.</p>\n<li><strong>Fluent UI reference</strong>: Source component: Fluent UI React v9 <code>Image</code>.</li>\n<p>  - Direct requirements:</p>\n<p>    - <code>alt</code> is descriptive, accurate, concise, and context-aware.</p>\n<p>    - Do not include redundant prefixes like &quot;image of&quot; or &quot;picture of&quot;.</p>\n<p>    - Do not repeat information already present on the page.</p>\n<p>    - Decorative images are excluded from assistive tools.</p>\n<p>    - <code>block</code>, <code>bordered</code>, <code>fit</code>, <code>shadow</code>, and <code>shape</code> are visual/layout props only.</p>\n<p>    - Native <code>&lt;img&gt;</code> browser fallback is the failure behavior.</p>\n</ul>\n<h3>Accessibility Guidance</h3>\n<ul>\n<li>Do: preserve native semantics, visible focus, accessible names/descriptions, and documented APG behavior.</li>\n<li>Do not: import unrelated APG roles or keyboard models; do not use color/icon/position as the only semantic signal.</li>\n<li>Author responsibilities: provide labels, descriptions, alt text, and valid child structure where this spec requires author input.</li>\n<li>Known tradeoffs:</li>\n<li><code>fit=&quot;none&quot;</code> can overflow its box — document that the parent owns clipping (<code>overflow</code>) and sizing.</li>\n<li>Circular shape assumes a square aspect; with non-square sources combine <code>shape=&quot;circular&quot;</code> + <code>fit=&quot;cover&quot;</code>.</li>\n<li>If using shadow DOM, <code>alt</code> still works (it's on the inner img) but page CSS can't reach the img except via <code>::part(img)</code> — that's the intended public surface.</li>\n<li>Broken <code>src</code> shows the native broken-image UI by contract; products wanting a custom placeholder opt in explicitly.</li>\n</ul>\n<h2>Motion Contract</h2>\n<ul>\n<li>Motion tokens: component-specific transition tokens; no literal durations/easings in generated output.</li>\n<li>CSS-only motion: preferred unless popup/composite lifecycle requires JS state coordination.</li>\n<li>Reduced-motion behavior: inline/block replaced element with aspect-ratio and object-fit/object-position contract remains understandable with motion disabled.</li>\n<li>Delayed unmount behavior: if visually present after logical close, it is non-interactive and hidden from accessibility APIs.</li>\n<li>Interaction behavior during motion: state and focus follow logical open/closed/selected values, not animation progress.</li>\n<li>Motion can be disabled by: <code>prefers-reduced-motion</code> and component/theme motion tokens.</li>\n</ul>\n<h2>Icons And Media</h2>\n<ul>\n<li>Icon source: design-system icon components or plain slotted media documented by API.</li>\n<li>Icon accessible name policy: requires alt for meaningful images; empty alt or aria-hidden for decorative images.</li>\n<li>Decorative icon policy: hide from assistive tech when duplicate of text.</li>\n<li>Media slot behavior: requires alt for meaningful images; empty alt or aria-hidden for decorative images.</li>\n<li>Media cloning behavior: do not clone slotted media; preserve author-provided semantics.</li>\n<li>Image fallback behavior: native image fallback or explicit fallback slot only.</li>\n</ul>\n<h2>Agent Guidance</h2>\n<ul>\n<li><strong>Selection guidance</strong> (<code>ai/components/image.md</code>): &quot;Use for content imagery. For interactive images, wrap in <code>tyui-link</code>/<code>tyui-button</code>; never make the image itself focusable.&quot;</li>\n<li><strong>Alternatives map</strong>: <code>icon → tyui-icon</code>, <code>decorative bg → CSS background</code>, <code>avatar → tyui-image shape=circular</code> (or future <code>tyui-avatar</code>).</li>\n<li><strong>Layout ownership</strong>: component owns shape/fit/intrinsic ratio; <strong>parent owns dimensions and clipping</strong>. DESIGN.md layout policy: images size from <code>width</code>/<code>height</code> attrs or container, not fixed token geometry (layout.md intrinsic-sizing rule).</li>\n<li><strong>Token usage</strong>: radius/border/shadow via <code>--ty-image-*</code>; reject literal values.</li>\n<li><strong>Anti-patterns to reject</strong>: missing <code>alt</code> on meaningful images; &quot;image of …&quot; alt text; <code>tabindex</code> on the image; using Image for icons.</li>\n<li><strong>x-design-system metadata</strong>:</li>\n<p>  ```json</p>\n<p>  {</p>\n<p>    &quot;intent&quot;: &quot;media&quot;,</p>\n<p>    &quot;focusable&quot;: false,</p>\n<p>    &quot;accessibility&quot;: { &quot;requiresAlt&quot;: &quot;unless decorative&quot;, &quot;decorativeOptOut&quot;: &quot;alt=\\&quot;\\&quot;&quot; }</p>\n<p>  }</p>\n<p>  ```</p>\n<li><strong>Validation gates</strong>: flag generated <code>tyui-image</code> without <code>alt</code> (and without explicit decorative opt-out); flag focusable images.</li>\n</ul>\n<h2>Tests</h2>\n<h3>Unit / Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Meaningful image requires <code>alt</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Decorative image uses <code>alt=&quot;&quot;</code></td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td><code>loading</code>, <code>decoding</code>, <code>srcset</code>, and <code>sizes</code> forward</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Native load/error cross the host boundary if shadowed</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td><code>fit</code>/<code>shape</code> token styling</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>No tab stop</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Forced-colors border/focus absence</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr><tr><td>Design validation rejects icon misuse</td><td>Render direct fixture.</td><td>Exercise public API and user interaction.</td><td>DOM, events, focus, and ARIA match this contract.</td></tr></tbody></table>\n<h3>Browser E2E Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Keyboard and focus contract</td><td>Render the component in a direct Vite fixture with realistic surrounding focus targets.</td><td>Use trusted browser input for Tab, Shift+Tab, Enter, Space, Escape, and arrow keys where applicable.</td><td>Focus target, state, events, and default prevention match the Keyboard Contract.</td></tr><tr><td>Pointer and target ownership</td><td>Render primary and secondary targets documented by the Composition Contract.</td><td>Click/tap/hover each target and inert region.</td><td>Only the intended target acts; hover does not mutate state unless documented.</td></tr></tbody></table>\n<h3>Accessibility Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>APG: Meaningful image has alt</td><td>Render a content image without <code>alt</code>.</td><td>Run accessibility validation.</td><td>Validation fails unless an explicit decorative opt-out is present</td></tr><tr><td>APG: Decorative image opt-out</td><td>Render a decorative image.</td><td>Inspect accessibility tree.</td><td>Image has empty alt or equivalent presentation semantics and is skipped by assistive tech</td></tr><tr><td>APG: No image action</td><td>Render <code>tyui-image tabindex=&quot;0&quot;</code> or with click handler.</td><td>Run design validation.</td><td>Validation rejects focusable/clickable image and suggests wrapping Link/Button</td></tr><tr><td>APG: Native fallback preserved</td><td>Render a broken <code>src</code>.</td><td>Observe browser behavior.</td><td>Native broken-image fallback/load error path remains available; component does not invent an inaccessible placeholder</td></tr><tr><td>Fluent UI: Alt quality lint</td><td>Render images with <code>alt=&quot;image of Allan's avatar&quot;</code> and duplicate adjacent text.</td><td>Run docs/example validation.</td><td>Validation flags redundant wording and duplicated page text</td></tr><tr><td>Fluent UI: Fit and shape are visual only</td><td>Render all <code>fit</code> and <code>shape</code> combinations.</td><td>Inspect DOM and accessibility tree.</td><td>No ARIA role/name changes are introduced by visual props</td></tr><tr><td>Fluent UI: Block sizing</td><td>Render <code>block</code> inside a constrained parent.</td><td>Measure layout at zoom.</td><td>Image fills container width without fixed token geometry; parent owns dimensions</td></tr><tr><td>Fluent UI: Fallback path</td><td>Render invalid <code>src</code> with border/shape tokens.</td><td>Wait for error.</td><td>Native fallback appears and any re-dispatched event crosses the host boundary if shadowed</td></tr></tbody></table>\n<h3>Visual And Contrast Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Focus/hover/active distinction</td><td>Render default, hovered, focused, active/selected/pressed where applicable.</td><td>Compare states in normal and forced-colors modes.</td><td>Keyboard focus is visible and distinct; contrast is preserved.</td></tr><tr><td>Tokenized variants</td><td>Render every documented appearance/size/orientation variant.</td><td>Inspect computed styles.</td><td>Public tokens drive variants; no literal color dependency is required.</td></tr><tr><td>Reduced motion</td><td>Enable <code>prefers-reduced-motion</code>.</td><td>Trigger state transitions.</td><td>Motion is removed or simplified without changing logical behavior.</td></tr></tbody></table>\n<h3>Generated Design / AI Contract Tests</h3>\n<table><thead><tr><th>Requirement</th><th>Setup</th><th>Action</th><th>Validation</th></tr></thead><tbody><tr><td>Valid generation</td><td>Ask the generator for the valid use case from Selection Guidance.</td><td>Inspect generated markup.</td><td>Output uses allowed children, slots, roles, labels, and target ownership.</td></tr><tr><td>Anti-pattern rejection</td><td>Ask the generator for the invalid example or overloaded-target case.</td><td>Run design validation.</td><td>Validation rejects the misuse and suggests the preferred component.</td></tr><tr><td>Reference guardrails</td><td>Ask the generator for a nearby but wrong APG/Fluent pattern.</td><td>Run design validation.</td><td>Validation keeps the documented reference pattern or explicit N/A rationale.</td></tr></tbody></table>\n<h3>Coverage Checklist</h3>\n<table><thead><tr><th>Area</th><th>Required coverage</th></tr></thead><tbody><tr><td>behavior.md</td><td>Native semantics first → real <code>&lt;img&gt;</code>, native load/fallback/alt.</td></tr><tr><td>behavior.md</td><td>Not focusable; wrapping control owns any action (one target = one action).</td></tr><tr><td>behavior.md</td><td>No undocumented events (native load/error only).</td></tr><tr><td>behavior.md</td><td>No interactive state; disabled N/A.</td></tr><tr><td>behavior.md</td><td>Motion optional, reduced-motion safe.</td></tr></tbody></table>",
    )}
  </article>`,
};
