{
  "name": "elements",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/elements/src",
  "implicitDependencies": ["core"],
  "targets": {
    "build": {
      "executor": "@tyui/tools-executors:build"
    },
    "test": {
      "executor": "@tyui/tools-executors:test",
      "options": {
        "browser": true,
        "watch": false,
        "coverage": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/elements",
        "command": "tsc -p tsconfig.lib.json --noEmit"
      }
    },
    "storybook": {
      "executor": "@tyui/tools-executors:storybook",
      "options": {
        "framework": "web-components",
        "port": 6006,
        "mode": "dev"
      }
    },
    "visual": {
      "executor": "@tyui/tools-executors:storybook-visual",
      "options": {
        "port": 6010,
        "themes": ["light", "dark"],
        "viewports": ["narrow", "mobile", "tablet", "desktop"],
        "updateSnapshots": false
      }
    },
    "e2e": {
      "executor": "@tyui/tools-executors:storybook-e2e",
      "options": {
        "port": 6011
      }
    }
  },
  "tags": ["scope:libs", "type:library"]
}
