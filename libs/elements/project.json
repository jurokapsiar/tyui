{
  "name": "elements",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/elements/src",
  "implicitDependencies": ["core"],
  "targets": {
    "build": {
      "executor": "@tyui/tools-executors:build",
      "dependsOn": ["cem", "skills:validate", "ai-context"]
    },
    "test": {
      "executor": "@tyui/tools-executors:test",
      "options": {
        "browser": true,
        "watch": false,
        "coverage": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/elements",
        "command": "tsc -p tsconfig.lib.json --noEmit"
      }
    },
    "storybook": {
      "executor": "@tyui/tools-executors:storybook",
      "dependsOn": ["storybook-docs"],
      "options": {
        "framework": "web-components",
        "port": 6006,
        "mode": "dev"
      }
    },
    "storybook-docs": {
      "executor": "@tyui/tools-executors:storybook-docs",
      "outputs": ["{projectRoot}/src/generated/spec-docs"]
    },
    "visual": {
      "executor": "@tyui/tools-executors:storybook-visual",
      "dependsOn": ["storybook-docs"],
      "options": {
        "port": 6010,
        "themes": ["light", "dark"],
        "viewports": ["narrow", "mobile", "tablet", "desktop"],
        "updateSnapshots": false
      }
    },
    "e2e": {
      "executor": "@tyui/tools-executors:component-e2e",
      "options": {
        "port": 6011
      }
    },
    "cem": {
      "executor": "@tyui/tools-executors:cem"
    },
    "skills:validate": {
      "executor": "@tyui/tools-executors:skills-validate"
    },
    "ai-context": {
      "executor": "@tyui/tools-executors:ai-context",
      "dependsOn": ["cem", "skills:validate"],
      "options": {
        "includeRootLlms": true
      }
    }
  },
  "tags": ["scope:libs", "type:library"]
}
