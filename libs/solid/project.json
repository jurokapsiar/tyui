{
  "name": "solid",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/solid/src",
  "implicitDependencies": ["define", "elements"],
  "targets": {
    "build": {
      "executor": "@toyu-ui/tools-executors:build",
      "dependsOn": ["^build", "skills:validate"]
    },
    "test": {
      "executor": "@toyu-ui/tools-executors:test",
      "options": {
        "browser": true,
        "watch": false,
        "coverage": false
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/solid",
        "command": "tsc -p tsconfig.lib.json --noEmit"
      }
    },
    "storybook": {
      "executor": "@toyu-ui/tools-executors:storybook",
      "options": {
        "framework": "solid",
        "port": 6007,
        "mode": "dev"
      }
    },
    "skills:validate": {
      "executor": "@toyu-ui/tools-executors:skills-validate"
    }
  },
  "tags": ["scope:libs", "type:library"]
}
